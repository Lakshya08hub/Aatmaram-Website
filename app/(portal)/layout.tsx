// Portal layout stub — auth guard added in Phase 4
export default function PortalLayout({
  children
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}

---
phase: 06-staff-management
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - app/(portal)/staff/page.tsx
  - app/(portal)/staff/StaffClient.tsx
  - app/(portal)/staff/layout.tsx
autonomous: true
requirements: [STAFF-01, STAFF-02, STAFF-03, STAFF-04, STAFF-05]
must_haves:
  truths:
    - "app/(portal)/staff/page.tsx fetches staff list server-side via getStaffList() and renders StaffClient"
    - "StaffClient renders a table with columns: Name, Email, Role, Phone, Join Date, Status, Actions"
    - "StaffClient has Add Staff dialog with fields: full_name, email, temp_password, role, phone, salary, join_date"
    - "StaffClient has Edit Staff sheet with same fields minus email and temp_password"
    - "StaffClient has Activate/Deactivate toggle button per row (covers STAFF-03 and STAFF-04)"
    - "StaffClient has Delete button with AlertDialog confirmation"
    - "Staff table shows a 'Pending' badge for rows where is_active = false (STAFF-04)"
    - "Staff table filters by Active / Pending tabs (STAFF-04 pending view)"
    - "For doctor-role staff rows, Edit sheet shows a Doctor Profile Link field (select from doctors table)"
    - "Selecting a doctor in the link dropdown updates doctors.staff_user_id = profile.id"
    - "Editing a doctor's linked profile fields calls the existing content/doctors server action flow (or links to /content/doctors)"
    - "All mutations call toast() on success/error using sonner"
    - "app/(portal)/staff/layout.tsx role-guards: only super_admin and admin can access /staff"
  artifacts:
    - path: "app/(portal)/staff/page.tsx"
      provides: "Server Component — fetches staff list + passes to StaffClient"
    - path: "app/(portal)/staff/StaffClient.tsx"
      provides: "Client Component — full staff CRUD UI"
    - path: "app/(portal)/staff/layout.tsx"
      provides: "Role guard — redirects non-admin roles to /dashboard"
  key_links:
    - from: "app/(portal)/staff/page.tsx"
      to: "lib/db/staff.ts getStaffList"
      via: "import getStaffList"
      pattern: "getStaffList"
    - from: "app/(portal)/staff/StaffClient.tsx"
      to: "app/(portal)/actions/staff.ts"
      via: "import createStaffAction, updateStaffAction, toggleActiveAction, deleteStaffAction"
      pattern: "createStaffAction"
    - from: "app/(portal)/staff/layout.tsx"
      to: "profiles table role column"
      via: "getUser() + supabase.from('profiles').select('role')"
      pattern: "getUser"
---

<objective>
Build the Staff Management portal page: replace the stub with a full CRUD UI using the server actions from Plan 06-01. Covers add staff (STAFF-01), edit staff (STAFF-02), deactivate/delete (STAFF-03), pending account view and activate/reject (STAFF-04), and doctor profile linkage (STAFF-05).

Pattern mirrors Phase 5's Departments page (Server Component + Client Component + existing Server Actions).
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<task id="T-06-02-01" type="execute" autonomous="true">
  <title>Create app/(portal)/staff/layout.tsx — role guard</title>

  <read_first>
    - app/(portal)/content/layout.tsx (exact role-guard layout pattern to replicate)
    - lib/portal/roles.ts (ROLE_SECTIONS — 'staff' key visible to super_admin + admin)
  </read_first>

  <action>
    Create app/(portal)/staff/layout.tsx as a Server Component with the exact pattern from app/(portal)/content/layout.tsx:
    - createClient() → getUser() → redirect('/login') if no user
    - Query profiles for role + is_active
    - If profile missing or !is_active → signOut + redirect('/login')
    - If role not in ['super_admin', 'admin'] → redirect('/dashboard')
    - Otherwise render children
  </action>

  <acceptance_criteria>
    - app/(portal)/staff/layout.tsx exists
    - File calls supabase.auth.getUser() (not getSession)
    - File redirects to /dashboard if role not in ['super_admin', 'admin']
    - File does not render children if role check fails
  </acceptance_criteria>
</task>

<task id="T-06-02-02" type="execute" autonomous="true">
  <title>Create app/(portal)/staff/page.tsx — Server Component data fetcher</title>

  <read_first>
    - app/(portal)/content/departments/page.tsx (exact Server Component page pattern)
    - lib/db/staff.ts (getStaffList function signature)
    - lib/db/doctors.ts (getDoctors function — needed to pass unlinked doctors to StaffClient for STAFF-05 dropdown)
  </read_first>

  <action>
    Replace app/(portal)/staff/page.tsx with a Server Component:
    - Import getStaffList from '@/lib/db/staff'
    - Import getDoctors from '@/lib/db/doctors' (for the doctor-link dropdown in the edit sheet)
    - Call both in parallel: const [staff, doctors] = await Promise.all([getStaffList(), getDoctors()])
    - Pass both as props to StaffClient: &lt;StaffClient initialStaff={staff} doctors={doctors} /&gt;
    - Add export const dynamic = 'force-dynamic'
  </action>

  <acceptance_criteria>
    - app/(portal)/staff/page.tsx imports getStaffList and getDoctors
    - Page uses Promise.all for parallel fetch
    - Page renders StaffClient with initialStaff and doctors props
    - File contains export const dynamic = 'force-dynamic'
  </acceptance_criteria>
</task>

<task id="T-06-02-03" type="execute" autonomous="true">
  <title>Create app/(portal)/staff/StaffClient.tsx — full CRUD UI</title>

  <read_first>
    - app/(portal)/content/departments/DepartmentsClient.tsx (full CRUD client component to replicate pattern)
    - app/(portal)/actions/staff.ts (actions to call: createStaffAction, updateStaffAction, toggleActiveAction, deleteStaffAction)
    - lib/db/staff.ts (StaffMember interface)
    - lib/db/doctors.ts (Doctor interface — for linkage dropdown)
    - lib/portal/roles.ts (StaffRole type and ALL_SECTIONS for role dropdown options)
  </read_first>

  <action>
    Create app/(portal)/staff/StaffClient.tsx ('use client') with:

    **Props:** `{ initialStaff: StaffMember[], doctors: Doctor[] }`

    **State:** staff list (useState with initialStaff), dialog open (add/edit), selected staff for edit

    **Tabs (STAFF-04):** Two tabs above the table using shadcn Tabs: "Active" and "Pending". Active tab shows `is_active = true` rows; Pending tab shows `is_active = false` rows. Badge "Pending" in the Status column for inactive rows.

    **Table columns:** Name, Email, Role (badge), Phone, Join Date, Status (Active/Pending badge), Actions

    **Actions column per row:**
    - Edit button → opens Edit sheet
    - Activate/Deactivate button → calls toggleActiveAction(id, !is_active), refreshes via router.refresh()
    - Delete button → opens AlertDialog confirmation → calls deleteStaffAction(id, user_id)

    **Add Staff Dialog (STAFF-01):**
    - Fields: full_name (required), email (required), temp_password (required, type="password"), role (Select dropdown with options: admin/doctor/receptionist), phone, salary (number), join_date (date input)
    - On submit: call createStaffAction(input) → toast success/error → router.refresh() → close dialog

    **Edit Staff Sheet (STAFF-02):**
    - Same fields as Add except email and temp_password (not editable)
    - Extra field for doctor-role only: "Doctor Profile" — a Select dropdown populated from `doctors` prop, showing only doctors where `staff_user_id IS NULL` OR `staff_user_id === profile.id`. Label is doctor's full_name.
    - On selecting a doctor and saving: call updateStaffAction(id, fields) AND update the doctor's staff_user_id by calling updateDoctorStaffLink(doctorId, profileId) — a simple Server Action that does: `supabase.from('doctors').update({ staff_user_id: profileId }).eq('id', doctorId)`
    - On submit: call updateStaffAction(id, input) → toast → router.refresh() → close sheet

    **Delete AlertDialog (STAFF-03/04):** "This will permanently delete the account. This action cannot be undone." — Confirm calls deleteStaffAction.

    **Zod schema for Add form:**
    ```
    z.object({
      full_name: z.string().min(2),
      email: z.string().email(),
      temp_password: z.string().min(8),
      role: z.enum(['admin', 'doctor', 'receptionist']),
      phone: z.string().optional(),
      salary: z.number().optional(),
      join_date: z.string().optional(),
    })
    ```

    Use the same shadcn/ui components as Phase 5: Table, Dialog, Sheet, AlertDialog, Button, Input, Select, Badge, Tabs. Import toast from sonner. Import useRouter from next/navigation for router.refresh().
  </action>

  <acceptance_criteria>
    - app/(portal)/staff/StaffClient.tsx exists and starts with 'use client'
    - Component renders a Table with columns Name, Email, Role, Status, Actions
    - Component has Active/Pending tabs (two separate filtered views of the staff list)
    - Add Staff dialog has full_name, email, temp_password, role, phone, salary, join_date fields
    - Edit Staff sheet does NOT have email or temp_password fields
    - Edit sheet shows Doctor Profile Select when role === 'doctor'
    - Deactivate button calls toggleActiveAction
    - Delete button opens AlertDialog then calls deleteStaffAction
    - File imports createStaffAction from '@/app/(portal)/actions/staff'
    - All actions followed by toast() and router.refresh()
  </acceptance_criteria>
</task>

<task id="T-06-02-04" type="execute" autonomous="true">
  <title>Add updateDoctorStaffLink Server Action (STAFF-05)</title>

  <read_first>
    - app/(portal)/actions/content.ts (requireCmsRole pattern — replicate as requireAdminRole using same pattern)
    - app/(portal)/actions/staff.ts (requireAdminRole already exists here — import from there or inline)
    - supabase/migrations/20260612_cms_tables.sql (doctors.staff_user_id column definition)
  </read_first>

  <action>
    Add to app/(portal)/actions/staff.ts (append to existing file):

    `updateDoctorStaffLinkAction(doctorId: string, profileId: string | null): Promise<{ error?: string }>`:
    - Call requireAdminRole()
    - Update doctors set { staff_user_id: profileId } where id = doctorId
    - Return { error? }

    This links a doctor record to a staff profile (or unlinks if profileId is null), satisfying STAFF-05's requirement that doctor profile changes feed the public site (the doctors table row is what the public Doctors page reads from — already done in Phase 5).
  </action>

  <acceptance_criteria>
    - app/(portal)/actions/staff.ts exports updateDoctorStaffLinkAction
    - Function updates doctors.staff_user_id where doctors.id = doctorId
    - Function accepts null for profileId (to unlink)
  </acceptance_criteria>
</task>

<task id="T-06-02-05" type="execute" autonomous="true">
  <title>TypeScript check + dev server smoke test</title>

  <read_first>
    - app/(portal)/staff/page.tsx
    - app/(portal)/staff/StaffClient.tsx
    - app/(portal)/staff/layout.tsx
  </read_first>

  <action>
    1. Run: npx tsc --noEmit
       Fix any TypeScript errors in the staff files.
    2. Run: npm run dev (background) and verify localhost:3000/staff loads without runtime error (redirects to /login for unauthenticated access is expected and correct).
  </action>

  <acceptance_criteria>
    - npx tsc --noEmit exits 0
    - npm run dev starts without compilation error
    - GET /staff returns 302 redirect to /login (unauthenticated request) — confirms route is protected
  </acceptance_criteria>
</task>

<verification>
## Phase 6 Acceptance Criteria

1. **STAFF-01**: Admin can add staff via portal form — form has full_name, email, temp_password, role, phone, salary, join_date; submitting creates Auth user + profile row
2. **STAFF-02**: Admin can edit staff details via Edit sheet — full_name, role, phone, salary, join_date all editable
3. **STAFF-03**: Admin can deactivate a staff member — toggleActiveAction sets is_active=false; deactivated user is blocked by portal layout (Phase 4 T-04-05 mitigation already handles this)
4. **STAFF-04**: Admin sees Pending tab showing is_active=false accounts; can Activate (approve) or Delete (reject) from that view
5. **STAFF-05**: Doctor-role staff row has Doctor Profile Select in Edit sheet; selecting a doctor writes doctors.staff_user_id = profile.id; public /doctors page reads from doctors table (Phase 5 dynamic — reflects immediately)

### Verification Commands
- `npx tsc --noEmit` → exits 0
- `GET /staff` (unauthenticated) → 302 to /login
- TypeScript: `app/(portal)/staff/StaffClient.tsx` imports `createStaffAction` from `@/app/(portal)/actions/staff`
- TypeScript: `app/(portal)/actions/staff.ts` exports `updateDoctorStaffLinkAction`
- DB: `supabase/migrations/20260612_staff_schema.sql` contains `ADD COLUMN IF NOT EXISTS full_name`
</verification>

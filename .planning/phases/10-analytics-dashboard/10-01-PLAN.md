---
phase: "10"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/[locale]/layout.tsx
  - .env.local
autonomous: false
requirements:
  - ANA-01
user_setup:
  - service: google-analytics
    why: "GA4 property must be created in Google Analytics to obtain a Measurement ID (G-XXXXXXXXXX). Claude cannot create a GA4 property — this requires a browser sign-in to analytics.google.com."
    env_vars:
      - name: NEXT_PUBLIC_GA_MEASUREMENT_ID
        source: "Google Analytics → Admin → Property Settings → Measurement ID (format: G-XXXXXXXXXX)"
    dashboard_config:
      - task: "Create a GA4 property for the hospital website"
        location: "analytics.google.com → Admin → Create Property → choose Web stream, enter site URL"

must_haves:
  truths:
    - "The public site sends GA4 pageview events on every page visit"
    - "NEXT_PUBLIC_GA_MEASUREMENT_ID is set in .env.local and is NOT undefined at runtime"
    - "The gtag script loads after page content (afterInteractive) — does not block page paint"
  artifacts:
    - path: "app/[locale]/layout.tsx"
      provides: "GA4 gtag snippet injected via next/script"
      contains: "NEXT_PUBLIC_GA_MEASUREMENT_ID"
    - path: ".env.local"
      provides: "GA4 Measurement ID env var"
      contains: "NEXT_PUBLIC_GA_MEASUREMENT_ID=G-"
  key_links:
    - from: "app/[locale]/layout.tsx"
      to: "googletagmanager.com/gtag/js"
      via: "next/script src prop"
      pattern: "googletagmanager\\.com/gtag/js"
---

<objective>
Set up GA4 tracking on the public hospital website.

Purpose: Enable the hospital to see website traffic in Google Analytics. The GA4 Measurement ID must exist before the gtag snippet can be added — so this plan has a human checkpoint first (create GA4 property) then automates the code changes.

Output: GA4 property exists; gtag snippet is live in the public site root layout using Next.js `<Script strategy="afterInteractive">`; Measurement ID is stored in `.env.local`.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-analytics-dashboard/10-CONTEXT.md
@.planning/phases/10-analytics-dashboard/10-RESEARCH.md

<interfaces>
From app/[locale]/layout.tsx (existing root layout — executor must read full file before editing):
- Root layout wraps all public pages inside `<html lang={locale}>` and `<body>`
- Import path for Script: `import Script from 'next/script'`
- Strategy to use: `strategy="afterInteractive"` (does not block hydration)
- Two Script tags required: one for the gtag JS loader, one for the inline init call

GA4 env var: `NEXT_PUBLIC_GA_MEASUREMENT_ID` — prefix is mandatory for client-side exposure in Next.js App Router. Value format: `G-XXXXXXXXXX`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Create GA4 property and add Measurement ID to .env.local</name>
  <what-built>Nothing yet — this is the prerequisite human step. Claude cannot access analytics.google.com to create a GA4 property.</what-built>
  <how-to-verify>
    1. Go to https://analytics.google.com
    2. Sign in with the Google account that will own the hospital's analytics
    3. Click Admin (gear icon, bottom-left)
    4. Under "Account", click "+ Create" → "Property"
    5. Enter property name: "Atmaram Child Care" (or similar), set timezone to Asia/Kolkata, currency INR
    6. Under "Platform", choose "Web"
    7. Enter the website URL (e.g., https://atmaramchildcareandcriticalcare.com or current dev URL)
    8. Click "Create stream"
    9. Copy the Measurement ID — it looks like G-XXXXXXXXXX (10 characters after the G-)
    10. Open .env.local in the project root and add the line:
        NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
        (replace G-XXXXXXXXXX with your actual Measurement ID)
    11. Save .env.local
  </how-to-verify>
  <resume-signal>Type "done" after adding NEXT_PUBLIC_GA_MEASUREMENT_ID to .env.local, or "skip" if you want to add the gtag code now and fill in the Measurement ID later</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add GA4 gtag snippet to public site root layout</name>
  <files>app/[locale]/layout.tsx</files>
  <read_first>
    - app/[locale]/layout.tsx — read the FULL file to understand current structure before editing; find where to add Script tags (inside the return, after body children, before closing body/html)
    - .planning/phases/10-analytics-dashboard/10-RESEARCH.md — Pattern 4 (GA4 gtag Snippet in Next.js App Router) for exact Script tag shape
  </read_first>
  <action>
    Add two next/script Script tags inside the root layout return, after existing body content, before the closing body/html tags. Import `Script` from `'next/script'` at the top of the file if not already imported.

    First Script tag — gtag loader:
    - src: `https://www.googletagmanager.com/gtag/js?id=${process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID}`
    - strategy: `"afterInteractive"`

    Second Script tag — gtag init:
    - id: `"ga4-init"`
    - strategy: `"afterInteractive"`
    - dangerouslySetInnerHTML with the inline init block: set `window.dataLayer`, define `function gtag`, call `gtag('js', new Date())`, call `gtag('config', '${process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID}')`.

    Use template literal for the inline script to interpolate `process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID`. Both Script tags must be inside the JSX return of the root layout function, NOT outside it.

    Only add these two Script tags — do not change any other part of the file.
  </action>
  <verify>
    Run `npm run build` — build must complete without TypeScript errors. Then run `npm run dev`, open http://localhost:3000 in a browser, open DevTools → Network tab, filter by "gtag" — the request to googletagmanager.com/gtag/js must appear (if NEXT_PUBLIC_GA_MEASUREMENT_ID is set in .env.local).

    Source assertion: `grep -n "googletagmanager.com/gtag/js" "app/[locale]/layout.tsx"` must return at least 1 match.
  </verify>
  <done>
    Two Script tags with strategy="afterInteractive" are present in app/[locale]/layout.tsx; the build compiles without errors; the gtag network request fires on page load in dev when the env var is set.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public site → Google servers | gtag.js fetched from googletagmanager.com at page load (third-party script) |
| .env.local → Next.js build | NEXT_PUBLIC_ env vars are baked into client JS — intentionally public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | NEXT_PUBLIC_GA_MEASUREMENT_ID in client JS | accept | GA Measurement IDs are intentionally public (they appear in every page's source); no secret exposure |
| T-10-02 | Tampering | Third-party gtag.js loaded from CDN | accept | Standard industry practice; Google controls the CDN; no PII sent via gtag in this setup |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; next/script is already part of Next.js |
</threat_model>

<verification>
After Task 2 completes:
- `grep -n "afterInteractive" "app/[locale]/layout.tsx"` returns 2 matches (one per Script tag)
- `grep -n "NEXT_PUBLIC_GA_MEASUREMENT_ID" "app/[locale]/layout.tsx"` returns at least 2 matches
- `npm run build` exits 0
- In browser DevTools on any public page, Network tab shows request to `www.googletagmanager.com/gtag/js?id=G-...`
</verification>

<success_criteria>
- GA4 property exists in Google Analytics with a Measurement ID of format G-XXXXXXXXXX
- NEXT_PUBLIC_GA_MEASUREMENT_ID is set in .env.local
- Both gtag Script tags are present in app/[locale]/layout.tsx with strategy="afterInteractive"
- Public site build compiles without errors
- gtag fires on every public page load (confirmed in browser Network tab)
</success_criteria>

<output>
Create `.planning/phases/10-analytics-dashboard/10-01-SUMMARY.md` when done.
</output>

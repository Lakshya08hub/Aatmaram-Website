---
phase: "10"
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - lib/db/analytics.ts
  - app/(portal)/analytics/page.tsx
autonomous: true
requirements:
  - ANA-02
  - ANA-03
  - ANA-04

must_haves:
  truths:
    - "Admins can visit /analytics and see four stacked sections without a JS error"
    - "Appointment Operations section shows this-week and this-month counts, a 4-status breakdown (pending/contacted/confirmed/cancelled), and a by-doctor table"
    - "Staff & Payroll Summary section shows active staff count by role and the current month's total payroll"
    - "Patient Volume section shows new patients registered this week and this month"
    - "GA4 link card section shows a description and a 'View in Google Analytics' button that opens analytics.google.com in a new tab"
    - "Receptionist and doctor roles are redirected away — page is admin/super_admin only"
    - "If appointment_requests or patient_records tables do not exist, the page renders with zeroed stats instead of crashing"
  artifacts:
    - path: "lib/db/analytics.ts"
      provides: "getAppointmentStats(now: Date) and getPatientVolumeStats(now: Date) query functions"
      exports:
        - "getAppointmentStats"
        - "getPatientVolumeStats"
        - "AppointmentStats"
        - "PatientVolumeStats"
    - path: "app/(portal)/analytics/page.tsx"
      provides: "Async Server Component page — requireAdminRole + Promise.all data fetch + four-section render"
      contains: "requireAdminRole"
  key_links:
    - from: "app/(portal)/analytics/page.tsx"
      to: "lib/db/analytics.ts"
      via: "named import"
      pattern: "getAppointmentStats|getPatientVolumeStats"
    - from: "app/(portal)/analytics/page.tsx"
      to: "lib/db/payroll.ts"
      via: "named import"
      pattern: "getActiveStaffWithPaymentStatus|getMonthlyPayrollTotal"
    - from: "app/(portal)/analytics/page.tsx"
      to: "Supabase admin client"
      via: "createAdminClient() inside lib/db/analytics.ts"
      pattern: "createAdminClient"
---

<objective>
Build the /analytics portal page: two new DB query functions and a pure Server Component page with four stacked data sections.

Purpose: Give hospital admins a single-page operational overview — appointment activity, staff headcount by role, payroll total, and patient registration volume. GA4 traffic section uses a link card (iframe is blocked by X-Frame-Options: SAMEORIGIN on analytics.google.com).

Output: lib/db/analytics.ts with getAppointmentStats + getPatientVolumeStats; app/(portal)/analytics/page.tsx that fetches all four data slices in parallel and renders them with p-8 padding matching the payroll page style.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-analytics-dashboard/10-CONTEXT.md
@.planning/phases/10-analytics-dashboard/10-RESEARCH.md

<interfaces>
From lib/db/payroll.ts (reused in analytics page):
  export interface StaffPayrollRow {
    id: string;
    full_name: string | null;
    role: StaffRole;           // StaffRole = 'super_admin' | 'admin' | 'doctor' | 'receptionist'
    salary: number | null;
    isPaid: boolean;
    paidAt: string | null;
    amountPaid: number | null;
  }
  export async function getActiveStaffWithPaymentStatus(month: string): Promise<StaffPayrollRow[]>
  export async function getMonthlyPayrollTotal(month: string): Promise<number>

From app/(portal)/actions/payroll.ts (requireAdminRole pattern to duplicate inline):
  const ADMIN_ROLES: StaffRole[] = ['super_admin', 'admin'];
  async function requireAdminRole() — uses createClient() (session client), calls supabase.auth.getUser(), checks profiles.role, calls redirect('/login') if no user, throws Error('Forbidden') if wrong role.

From lib/supabase/admin.ts:
  export function createAdminClient(): SupabaseClient — used for all analytics DB reads (bypasses RLS).

Appointment schema (confirmed from migration):
  Table: appointment_requests
  Columns used: created_at (TIMESTAMPTZ), status (enum: pending|contacted|confirmed|cancelled), preferred_doctor (TEXT free-text)

Patient schema (confirmed from migration):
  Table: patient_records
  Columns used: created_at (TIMESTAMPTZ)

New interfaces to create in lib/db/analytics.ts:
  export interface AppointmentStats {
    thisWeek: number;
    thisMonth: number;
    byStatus: Record<string, number>;   // keys: 'pending' | 'contacted' | 'confirmed' | 'cancelled'
    byDoctor: { doctor: string; count: number }[];
  }
  export interface PatientVolumeStats {
    thisWeek: number;
    thisMonth: number;
  }

Shadcn components already installed:
  components/ui/card.tsx — Card, CardHeader, CardTitle, CardContent
  components/ui/badge.tsx — Badge (for status chips)

Navigation: /analytics is already in lib/portal/roles.ts ROLE_SECTIONS for admin and super_admin — no sidebar changes needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/db/analytics.ts with getAppointmentStats and getPatientVolumeStats</name>
  <files>lib/db/analytics.ts</files>
  <read_first>
    - lib/db/payroll.ts — read in full to understand createAdminClient usage, error handling pattern (throw new Error with message), and return type shapes
    - .planning/phases/10-analytics-dashboard/10-RESEARCH.md — sections "Code Examples" and "Confirmed Schema Details" for exact column names, query patterns, and the try/catch fallback requirement for missing tables
  </read_first>
  <action>
    Create lib/db/analytics.ts. Import createAdminClient from '@/lib/supabase/admin'.

    Export interface AppointmentStats with fields: thisWeek (number), thisMonth (number), byStatus (Record of string to number), byDoctor (array of {doctor: string; count: number}).

    Export interface PatientVolumeStats with fields: thisWeek (number), thisMonth (number).

    Export async function getAppointmentStats(now: Date): Promise<AppointmentStats>:
    - Compute sevenDaysAgo (now minus 7 days) and startOfMonth (new Date(year, month, 1)) from the now param.
    - Call createAdminClient(), then query appointment_requests selecting created_at, status, preferred_doctor. No date filter on the DB query — fetch all rows, compute week/month/byStatus/byDoctor in TypeScript. This avoids multiple round-trips.
    - Wrap the entire DB call in try/catch: if the table does not exist (error.code === '42P01' or error.message contains "relation"), return zeroed AppointmentStats: {thisWeek: 0, thisMonth: 0, byStatus: {}, byDoctor: []}.
    - For all other errors: throw new Error('Failed to fetch appointment stats: ' + error.message).
    - Iterate rows: for each row, check if created_at timestamp falls within [sevenDaysAgo, now] (increment thisWeek) and within [startOfMonth, now] (increment thisMonth). Also accumulate byStatus and byDoctor maps using exact string values from the row. For the by-doctor table, sort descending by count.
    - byStatus counts should be all-time (not date-scoped) so the admin sees the full status distribution.

    Export async function getPatientVolumeStats(now: Date): Promise<PatientVolumeStats>:
    - Compute sevenDaysAgo and startOfMonth from the now param.
    - Call createAdminClient(), query patient_records selecting created_at, filtered with .gte('created_at', startOfMonth.toISOString()) — this limits the DB scan to records from this month onward.
    - Wrap in try/catch: if table does not exist (same error detection as above), return {thisWeek: 0, thisMonth: 0}.
    - Iterate rows: count thisWeek (created_at >= sevenDaysAgo) and thisMonth (all rows — the DB filter already scoped to this month).

    Do not add any default export. Do not import anything from 'next/navigation' or Next.js. This file is a pure data layer.
  </action>
  <verify>
    Run `npx tsc --noEmit` — must exit 0 with no errors in lib/db/analytics.ts.
    Source assertions:
    - `grep -c "getAppointmentStats" lib/db/analytics.ts` returns at least 2 (declaration + export)
    - `grep -c "getPatientVolumeStats" lib/db/analytics.ts` returns at least 2
    - `grep -c "try {" lib/db/analytics.ts` returns at least 2 (one try/catch per function)
    - `grep -c "42P01" lib/db/analytics.ts` returns at least 1 (missing-table detection)
  </verify>
  <done>
    lib/db/analytics.ts exists; exports AppointmentStats, PatientVolumeStats, getAppointmentStats, getPatientVolumeStats; both functions have try/catch with zeroed fallback when appointment_requests or patient_records table is absent; TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create app/(portal)/analytics/page.tsx — Server Component with four-section render</name>
  <files>app/(portal)/analytics/page.tsx</files>
  <read_first>
    - app/(portal)/payroll/page.tsx — read in full; this is the exact model to follow for the page structure (async function, toFirstOfMonth helper, Promise.all, p-8 wrapper)
    - app/(portal)/actions/payroll.ts — read the requireAdminRole function (lines 21-42) to copy the exact implementation including the ADMIN_ROLES const
    - lib/db/analytics.ts — the file created in Task 1 (verify exports before importing)
    - .planning/phases/10-analytics-dashboard/10-CONTEXT.md — decisions D-02 (link card, NOT iframe), D-04 (section order), D-05 (p-8 padding), D-06 (both week+month shown simultaneously), D-07 (4 statuses: pending/contacted/confirmed/cancelled), D-09 (admin gate)
  </read_first>
  <action>
    Create app/(portal)/analytics/page.tsx as an async Server Component. No 'use client' directive.

    Imports needed:
    - redirect from 'next/navigation'
    - createClient from '@/lib/supabase/server'
    - StaffRole from '@/lib/portal/roles'
    - getActiveStaffWithPaymentStatus, getMonthlyPayrollTotal from '@/lib/db/payroll'
    - getAppointmentStats, getPatientVolumeStats from '@/lib/db/analytics'
    - Card, CardContent, CardHeader, CardTitle from '@/components/ui/card'
    - Badge from '@/components/ui/badge'

    Define inline: const ADMIN_ROLES: StaffRole[] = ['super_admin', 'admin']

    Define inline: toFirstOfMonth(d: Date): string — same implementation as payroll page (returns 'YYYY-MM-01' string).

    Define inline: async function requireAdminRole() — exact same implementation as in app/(portal)/actions/payroll.ts. Uses createClient() (session client), calls supabase.auth.getUser(), queries profiles for role, calls redirect('/login') if no user, throws Error('Forbidden') if role not in ADMIN_ROLES.

    In the default export async function AnalyticsPage():
    1. await requireAdminRole() at the very top.
    2. const now = new Date(); const currentMonth = toFirstOfMonth(now);
    3. const [appointmentStats, staff, payrollTotal, patientStats] = await Promise.all([getAppointmentStats(now), getActiveStaffWithPaymentStatus(currentMonth), getMonthlyPayrollTotal(currentMonth), getPatientVolumeStats(now)]) — parallel fetch.
    4. Derive roleCount: reduce staff array to Record of role to count (group by s.role, increment counter).

    Render a div with className="p-8 space-y-8" containing four Card sections in order:

    Section 1 — GA4 Website Traffic (D-02 link card):
    - CardTitle: "Website Traffic (Google Analytics)"
    - CardContent: a short description paragraph: "View your website's traffic data, audience demographics, and page performance in Google Analytics."
    - A link element with href="https://analytics.google.com" target="_blank" rel="noopener noreferrer" styled as a button: text "View in Google Analytics →"
    - Helper text below the link (small/muted): "Sign in to Google with the account that owns the GA4 property to see data."

    Section 2 — Appointment Operations (D-07):
    - CardTitle: "Appointment Requests"
    - Two stat chips side by side: "This week: {appointmentStats.thisWeek}" and "This month: {appointmentStats.thisMonth}"
    - By-status breakdown: render a Badge for each of the four statuses — pending (gray/secondary), contacted (yellow/warning), confirmed (green), cancelled (red/destructive) — showing status name and count from appointmentStats.byStatus[status] ?? 0
    - By-doctor table: a simple two-column table (Doctor | Count) listing appointmentStats.byDoctor entries. If byDoctor is empty, show "No appointment data yet."

    Section 3 — Staff & Payroll Summary:
    - CardTitle: "Staff & Payroll"
    - Staff counts by role: for each key in roleCount, show "role: count". If roleCount is empty, show "No active staff."
    - Payroll total line: "Current month payroll: ₹{payrollTotal.toLocaleString('en-IN')}"

    Section 4 — Patient Volume:
    - CardTitle: "Patient Volume"
    - Two stat chips side by side: "This week: {patientStats.thisWeek}" and "This month: {patientStats.thisMonth}"
    - If both are 0, show "No patient records this month yet."

    Use className strings matching the existing portal style: headings with "text-2xl font-bold" or similar, stat values with "text-3xl font-semibold", muted labels with "text-sm text-muted-foreground".
  </action>
  <verify>
    Run `npx tsc --noEmit` — must exit 0.
    Run `npm run build` — must exit 0 with no type errors.
    Source assertions:
    - `grep -c "requireAdminRole" "app/(portal)/analytics/page.tsx"` returns at least 2 (definition + call)
    - `grep -c "Promise.all" "app/(portal)/analytics/page.tsx"` returns at least 1
    - `grep -c "analytics.google.com" "app/(portal)/analytics/page.tsx"` returns at least 1 (link card href)
    - `grep -c "appointmentStats\|patientStats\|payrollTotal\|roleCount" "app/(portal)/analytics/page.tsx"` returns at least 4 distinct matches
    Manual check: visit http://localhost:3000/analytics as an admin user — all four sections render without a runtime error. Visit as a non-admin user — should redirect to /login or throw Forbidden.
  </verify>
  <done>
    app/(portal)/analytics/page.tsx exists as a pure Server Component; all four sections render with live data; admin gate redirects non-admins; TypeScript and build compile clean; appointment, staff, payroll, and patient stats display correctly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /analytics route | Only admin/super_admin must reach this page |
| Server Component → Supabase admin client | Uses service-role key that bypasses RLS — must not leak data to wrong roles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Elevation of Privilege | /analytics page | mitigate | requireAdminRole() called at top of page function; uses getUser() (not getSession()) per T-04-08 project-wide rule; ROLE_SECTIONS in roles.ts hides nav item from non-admins |
| T-10-04 | Information Disclosure | createAdminClient() in analytics queries | mitigate | Admin client used only server-side in lib/db/analytics.ts; client JS never receives the service-role key; data is fetched in Server Component and rendered as HTML |
| T-10-05 | Denial of Service | Missing appointment_requests / patient_records tables | accept | Queries wrapped in try/catch with zeroed fallback — missing table returns zeros, page renders normally |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan — all imports (Card, Badge, createAdminClient, getUser) already installed in the project |
</threat_model>

<verification>
After both tasks complete:
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0
- `npm run dev` starts without errors
- Navigate to http://localhost:3000/analytics as admin — four Card sections render
- Section 1: "Website Traffic" card with "View in Google Analytics →" link opening analytics.google.com in new tab
- Section 2: Appointment stats showing week/month counts + 4-status breakdown badges + by-doctor table (or "No appointment data yet")
- Section 3: Staff counts by role + "Current month payroll: ₹X"
- Section 4: Patient volume week/month counts
- Navigate to /analytics as a receptionist or doctor — redirect to /login or Forbidden error
</verification>

<success_criteria>
- lib/db/analytics.ts exports getAppointmentStats and getPatientVolumeStats with missing-table try/catch fallback
- app/(portal)/analytics/page.tsx is a pure Server Component with requireAdminRole gate
- All four sections render on page load with data from Promise.all parallel fetch
- GA4 section is a link card (no iframe — X-Frame-Options blocks it per D-02 revision)
- Appointment section shows all 4 status values: pending, contacted, confirmed, cancelled
- Staff section derives role counts from existing getActiveStaffWithPaymentStatus() result (no extra DB call)
- Payroll total is formatted with toLocaleString('en-IN') showing ₹ prefix
- TypeScript and Next.js build are clean
</success_criteria>

<output>
Create `.planning/phases/10-analytics-dashboard/10-02-SUMMARY.md` when done.
</output>

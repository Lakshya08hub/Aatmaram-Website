---
phase: 02-public-website
plan: 06
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-03
  - 02-04
  - 02-05
files_modified:
  - app/[locale]/(public)/appointment/page.tsx
  - components/public/AppointmentForm.tsx
autonomous: false
requirements:
  - PUB-07

must_haves:
  truths:
    - "Visitor navigating to /hi/appointment sees the appointment request form with all 5 required fields"
    - "Submitting with invalid phone number shows inline error: 'Enter a valid 10-digit Indian mobile number.'"
    - "Submitting with a past date shows inline error: 'Please select a date from today onwards.'"
    - "Submitting with all valid data shows a Sonner toast: 'Your appointment request has been received. Our team will call you shortly.'"
    - "Form does NOT submit data to any API or Supabase — client-side only"
    - "The page statically pre-renders for both locales; only the AppointmentForm component is 'use client'"
  artifacts:
    - path: "app/[locale]/(public)/appointment/page.tsx"
      provides: "Appointment request page (Server Component wrapping client form)"
      contains: "AppointmentForm"
    - path: "components/public/AppointmentForm.tsx"
      provides: "Client component with react-hook-form + Zod + Sonner"
      contains: "'use client'"
  key_links:
    - from: "components/public/AppointmentForm.tsx"
      to: "sonner"
      via: "import { toast } from 'sonner'"
      pattern: "toast\\.success"
    - from: "components/public/AppointmentForm.tsx"
      to: "lib/data/doctors.ts"
      via: "import doctors for select options"
      pattern: "import.*doctors.*from.*lib/data/doctors"
---

<objective>
Build the Appointment Request page — a server component page that renders the AppointmentForm client component, which implements react-hook-form + Zod validation and Sonner toast success feedback.

Purpose: PUB-07 requires the appointment form with client-side validation. This is the only client component page in Phase 2. The form must match the 5-field spec from D-07 and show the exact success toast message from D-06. No Supabase write in Phase 2 — that is Phase 7 scope.

Output: Appointment page with form in a centered Card layout; AppointmentForm client component with Zod schema validation for all 5 fields and Sonner toast on successful submission.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-public-website/02-CONTEXT.md
@.planning/phases/02-public-website/02-UI-SPEC.md
@.planning/phases/02-public-website/02-RESEARCH.md
@.planning/phases/02-public-website/02-01-SUMMARY.md
@.planning/phases/02-public-website/02-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts for the appointment form -->

From lib/data/doctors.ts:
  export interface Doctor { id: string; name: string; specialty: string; initials: string; }
  export const doctors: Doctor[] — 6 items (used for select dropdown options)

Zod schema (from RESEARCH.md Pattern 2 — AppointmentForm):
  patientName: z.string().min(2, 'This field is required.')
  phone: z.string().regex(/^[6-9]\d{9}$/, 'Enter a valid 10-digit Indian mobile number.')
  preferredDoctor: z.string().min(1, 'This field is required.')
  preferredDate: z.string().refine(
    (val) => !val || new Date(val) >= new Date(new Date().toISOString().split('T')[0]),
    'Please select a date from today onwards.'
  )
  reason: z.string().min(10, 'This field is required.')

Toast (from D-06, UI-SPEC Copywriting Contract):
  toast.success('Request Received', {
    description: 'Your appointment request has been received. Our team will call you shortly.',
    duration: 6000,
  })

shadcn Form pattern (from RESEARCH.md shadcn Form + Zod Schema Pattern):
  import { Form, FormControl, FormField, FormItem, FormLabel, FormMessage } from '@/components/ui/form'
  NOTE: The 'form' shadcn component was NOT in the install list in Plan 01.
  It must be installed in this plan: npx shadcn@latest add form

Select dropdown (from UI-SPEC Form Fields table):
  Preferred doctor field uses shadcn Select with options: "No preference" + 6 doctor names from doctors array
  Select must have placeholder text: "Select a doctor"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn form component and build AppointmentForm client component</name>
  <files>components/public/AppointmentForm.tsx, components/ui/form.tsx (auto-generated)</files>
  <read_first>
    - .planning/phases/02-public-website/02-RESEARCH.md — Pattern 2 (Client Component with Translations), Pattern 3 note on Sonner (Pitfall 2), the full AppointmentForm code example in the Code Examples section
    - .planning/phases/02-public-website/02-UI-SPEC.md — Form Fields specification table (all 5 fields with component types, validation rules), Form Errors copywriting, Toast Success copywriting
    - lib/data/doctors.ts — Doctor type to import for select options
    - messages/en.json — "appointment" namespace keys to confirm t() key names
  </read_first>
  <action>
Step 1 — Install the shadcn form component (not installed in Plan 01):
```
npx shadcn@latest add form
```
This generates components/ui/form.tsx which wraps react-hook-form with shadcn styling for FormField, FormItem, FormLabel, FormControl, and FormMessage.

Step 2 — Create components/public/AppointmentForm.tsx as a 'use client' component.

Import list:
- 'use client' directive at top
- import { useForm } from 'react-hook-form'
- import { zodResolver } from '@hookform/resolvers/zod'
- import { z } from 'zod'
- import { toast } from 'sonner'
- import { useTranslations } from 'next-intl'
- import { Button } from '@/components/ui/button'
- import { Input } from '@/components/ui/input'
- import { Textarea } from '@/components/ui/textarea'
- import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
- import { Form, FormControl, FormField, FormItem, FormLabel, FormMessage } from '@/components/ui/form'
- import { Card, CardContent } from '@/components/ui/card'
- import { doctors } from '@/lib/data/doctors'

Zod schema (define outside the component to avoid re-creation on render):
const appointmentSchema = z.object({
  patientName: z.string().min(2, 'This field is required.'),
  phone: z.string().regex(/^[6-9]\d{9}$/, 'Enter a valid 10-digit Indian mobile number.'),
  preferredDoctor: z.string().min(1, 'This field is required.'),
  preferredDate: z.string().refine(
    (val) => !val || new Date(val) >= new Date(new Date().toISOString().split('T')[0]),
    'Please select a date from today onwards.'
  ),
  reason: z.string().min(10, 'This field is required.'),
});

type AppointmentFormValues = z.infer<typeof appointmentSchema>;

Component body:
- const t = useTranslations('appointment')
- const form = useForm<AppointmentFormValues>({ resolver: zodResolver(appointmentSchema) })
- const onSubmit = (data: AppointmentFormValues) => {
    toast.success('Request Received', {
      description: 'Your appointment request has been received. Our team will call you shortly.',
      duration: 6000,
    });
    form.reset();
  }

Render structure:
- Card with className="shadow-sm border border-slate-200 rounded-xl"
- CardContent className="p-8"
- Form component (shadcn Form wrapper): <Form {...form}>
- form element with onSubmit={form.handleSubmit(onSubmit)} className="space-y-6"
- 5 FormField blocks in order:

  Field 1 — Patient Name:
  FormField name="patientName" control={form.control} render={({ field }) => (
    FormItem: FormLabel (text: "Patient Name" + required asterisk as span className="text-red-500 ml-1" "*") + FormControl wrapping Input type="text" placeholder="Your full name" {...field} + FormMessage
  )}

  Field 2 — Phone Number:
  FormField name="phone" control={form.control} render={({ field }) => (
    FormItem: FormLabel ("Phone Number" + required *) + FormControl wrapping Input type="tel" placeholder="10-digit mobile number" {...field} + FormMessage
  )}

  Field 3 — Preferred Doctor:
  FormField name="preferredDoctor" control={form.control} render={({ field }) => (
    FormItem: FormLabel ("Preferred Doctor" + required *) + FormControl wrapping shadcn Select:
      onValueChange={field.onChange} defaultValue={field.value}
      SelectTrigger: SelectValue placeholder="Select a doctor"
      SelectContent:
        SelectItem value="no-preference": "No preference"
        doctors.map(doc => SelectItem value={doc.id}: doc.name)
    + FormMessage
  )}

  Field 4 — Preferred Date:
  FormField name="preferredDate" control={form.control} render={({ field }) => (
    FormItem: FormLabel ("Preferred Date" + required *) + FormControl wrapping Input type="date" min={new Date().toISOString().split('T')[0]} {...field} + FormMessage
  )}

  Field 5 — Reason:
  FormField name="reason" control={form.control} render={({ field }) => (
    FormItem: FormLabel ("Reason / Chief Complaint" + required *) + FormControl wrapping Textarea placeholder="Briefly describe your symptoms or reason for visit" rows={4} {...field} + FormMessage
  )}

Submit button (below form fields):
  Button type="submit" className="w-full bg-blue-800 hover:bg-blue-900 text-white font-semibold min-h-[44px]" size="lg"
  Text: "Submit Request"

Important: The onSubmit handler must NOT call any API or Supabase function. It only calls toast.success() and form.reset(). A comment in the code should read: "TODO Phase 7: Add POST /api/appointments call here"
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "^$" | head -10 && echo "TSC_OK"</automated>
  </verify>
  <acceptance_criteria>
    - components/public/AppointmentForm.tsx exists with 'use client' directive
    - File imports useForm from 'react-hook-form' and zodResolver from '@hookform/resolvers/zod'
    - File imports toast from 'sonner' (NOT from '@/components/ui/sonner' — the Toaster is imported from there, not the toast function)
    - appointmentSchema defines all 5 fields with correct validations including phone regex /^[6-9]\d{9}$/
    - onSubmit calls toast.success with description "Your appointment request has been received. Our team will call you shortly."
    - Select dropdown includes a "No preference" option and one SelectItem per doctor from the doctors array
    - Submit button has text "Submit Request"
    - File does NOT contain any fetch() call or Supabase import
    - npx tsc --noEmit exits 0
    - components/ui/form.tsx exists (installed by shadcn add form)
  </acceptance_criteria>
  <done>AppointmentForm client component built with react-hook-form + Zod validation for all 5 fields; success toast fires on valid submit; no API calls</done>
</task>

<task type="auto">
  <name>Task 2: Build Appointment page (server component) and run full build</name>
  <files>app/[locale]/(public)/appointment/page.tsx</files>
  <read_first>
    - .planning/phases/02-public-website/02-UI-SPEC.md — Appointment Request page layout (section 7): page header with subtitle, centered max-w-lg form card
    - components/public/AppointmentForm.tsx — confirm it exists after Task 1 (import path: '@/components/public/AppointmentForm')
    - messages/en.json — "appointment" namespace: pageTitle, pageSubtitle keys
  </read_first>
  <action>
Create app/[locale]/(public)/appointment/page.tsx. Create the appointment/ directory. This is a Server Component (no 'use client'). AppointmentForm is the only client component — it is imported and rendered inside this server component.

Import list:
- import { setRequestLocale, getTranslations } from 'next-intl/server'
- import { routing } from '@/i18n/routing'
- import type { Metadata } from 'next'
- import AppointmentForm from '@/components/public/AppointmentForm'

Export metadata:
  title: "Request an Appointment | Atmaram Child Care and Critical Care"
  description: "Request a doctor's appointment at Atmaram Child Care and Critical Care, Kanpur. Our team will call you to confirm."

Export generateStaticParams returning routing.locales.map((locale) => ({ locale })).

Page component body:
1. Await params, call setRequestLocale(locale)
2. Await getTranslations('appointment')

Section 1 — Page Header:
- section className="py-12 bg-white px-4"
- div className="max-w-7xl mx-auto"
- p className="text-sm text-slate-500 mb-2" — "Home › Request an Appointment"
- h1 className="text-xl font-semibold text-slate-900" — "Request an Appointment"
- p className="text-base text-slate-500 mt-2" — "Fill in the form below and our team will call you to confirm."

Section 2 — Form:
- section className="py-12 px-4"
- div className="max-w-7xl mx-auto"
- div className="max-w-lg mx-auto" — centers the form card
- AppointmentForm component (no props needed)

After the file is created, run the full build to confirm all 7 pages build cleanly:
npm run build

Then run lint:
npm run lint
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/[locale]/(public)/appointment/page.tsx exists
    - File does NOT contain 'use client'
    - File imports AppointmentForm from '@/components/public/AppointmentForm'
    - File exports generateStaticParams
    - File exports metadata with title "Request an Appointment | Atmaram Child Care and Critical Care"
    - npm run build exits 0 — all 7 public pages compile
    - npm run lint exits 0 (or exits with only warnings, not errors)
    - Build output shows 7 routes under /(public): /, /about, /departments, /doctors, /services, /contact, /appointment
  </acceptance_criteria>
  <done>All 7 public pages exist and build successfully; appointment page is a server component that renders the AppointmentForm client component; full build and lint pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All 7 public pages are built. The full site runs at http://localhost:3000 (start with: npm run dev).

What was built in Phase 2:
- Home page (/hi/) with hero (PM-JAY badge), trust signals band, departments preview (8 cards), doctors preview (3 cards), appointment CTA
- About page (/hi/about) with founding story, stat cards, mission/values
- Departments page (/hi/departments) with 8 department cards
- Doctors page (/hi/doctors) with 6 doctor cards and placeholder avatars
- Services page (/hi/services) with services list, Ayushman Bharat section, facilities grid
- Contact page (/hi/contact) with address, OPD timings, emergency, map placeholder
- Appointment page (/hi/appointment) with 5-field form + validation + success toast
  </what-built>
  <how-to-verify>
1. Start dev server: npm run dev
2. Open http://localhost:3000 (should redirect to /hi/ — default locale)
3. Verify Home page:
   - Header visible (dark navy, "Atmaram Child Care" text, "Book Appointment" green button)
   - Hero shows "Quality Care for Every Child and Family" headline
   - Green PM-JAY badge visible below tagline (CRITICAL — PUB-08)
   - Blue trust signals band visible below hero (90 Beds, 25+ Doctors, 8 Specialties, 24x7 Emergency)
   - 8 department cards in a grid
   - 3 doctor cards in a grid
   - Footer visible at bottom (dark navy, 3 columns, PM-JAY small badge in column 1)
4. Click hamburger menu on mobile viewport (375px) — sheet drawer should open from right with nav links
5. Navigate to /hi/about — verify stat cards (90, 25+, 8 specialties, 10+ years) and 3 mission/values cards
6. Navigate to /hi/departments — verify 8 cards with lucide icons
7. Navigate to /hi/doctors — verify 6 cards with initials avatars (e.g., "RS" for Dr. Rajesh Sharma)
8. Navigate to /hi/services — verify services list with checkmarks, PM-JAY section with green badge, 6 facility cards
9. Navigate to /hi/contact — verify 4 contact cards and map placeholder ("Map loading...")
10. Navigate to /hi/appointment:
    - Submit empty form — expect "This field is required." on patient name
    - Enter phone "12345" — expect "Enter a valid 10-digit Indian mobile number."
    - Enter a past date — expect "Please select a date from today onwards."
    - Fill all fields validly (name: "Test Patient", phone: "9876543210", doctor: any, date: tomorrow, reason: "Headache and fever") — submit — expect green Sonner toast at bottom-right: "Your appointment request has been received. Our team will call you shortly."
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. If issues found, describe which page or behavior failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → nowhere | AppointmentForm submits to no endpoint — client-side only in Phase 2 |
| User input → Zod schema | Form data passes through Zod before onSubmit fires |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Appointment form (client-side Zod only) | accept | No data stored or transmitted in Phase 2; Zod validates format client-side. Phase 7 adds server-side validation, rate limiting, and honeypot field before the Supabase write is added. TODO comment in onSubmit documents the Phase 7 wiring point. |
| T-02-SPAM | Tampering | Form spam / bot submission | accept | Phase 2: No backend — spam has zero effect; no data is captured. Phase 7 scope: CAPTCHA or honeypot field added alongside Supabase write. |
| T-02-INPUT | Tampering | XSS via form input display | accept | React escapes all JSX string values; react-hook-form renders field values into controlled inputs — no innerHTML or dangerouslySetInnerHTML used |
</threat_model>

<verification>
After all tasks complete:
1. npm run build exits 0
2. npm run lint exits 0 or warnings-only
3. All 7 routes appear in build output under /(public)
4. Human verification checkpoint passed: form validation works, success toast fires, all pages render correctly
</verification>

<success_criteria>
- All 7 public pages exist and render without errors (PUB-01 through PUB-08)
- Appointment form has 5 fields matching D-07: patient name, phone, preferred doctor (select), preferred date, reason
- Zod validation fires on invalid phone (regex /^[6-9]\d{9}$/) and past date
- Success toast shows exact text from D-06 on valid submission
- Form makes no API calls or Supabase writes (Phase 7 scope)
- Full build and lint pass
- Human verification checkpoint passed
</success_criteria>

<output>
Create .planning/phases/02-public-website/02-06-SUMMARY.md when done
</output>

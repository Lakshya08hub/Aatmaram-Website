---
phase: 02-public-website
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - app/[locale]/(public)/page.tsx
  - app/[locale]/(public)/about/page.tsx
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-08

must_haves:
  truths:
    - "Visitor navigating to /hi/ sees the Home page with hero headline, PM-JAY badge, trust signals band, departments preview, doctors preview, and appointment CTA band"
    - "Visitor navigating to /hi/about sees the About page with hospital story, stat cards, and mission/values section"
    - "Ayushman Bharat PM-JAY badge is visible in the hero section of the Home page — green pill below the tagline"
    - "Smoke test code (Supabase connection div) is removed from the homepage"
    - "Both pages statically pre-render for both 'hi' and 'en' locales"
  artifacts:
    - path: "app/[locale]/(public)/page.tsx"
      provides: "Home page — replaces smoke test"
      contains: "PMJAYBadge"
    - path: "app/[locale]/(public)/about/page.tsx"
      provides: "About Us page"
      contains: "generateStaticParams"
  key_links:
    - from: "app/[locale]/(public)/page.tsx"
      to: "lib/data/departments.ts"
      via: "import departments"
      pattern: "import.*departments.*from.*lib/data/departments"
    - from: "app/[locale]/(public)/page.tsx"
      to: "lib/data/doctors.ts"
      via: "import doctors (first 3 for preview)"
      pattern: "import.*doctors.*from.*lib/data/doctors"
    - from: "app/[locale]/(public)/page.tsx"
      to: "components/public/PMJAYBadge.tsx"
      via: "import PMJAYBadge"
      pattern: "import.*PMJAYBadge"
---

<objective>
Build the Home page (replacing the Phase 1 smoke test) and the About page as fully structured server components with seeded content, per the UI-SPEC layout specifications.

Purpose: PUB-01 and PUB-02 require these pages to exist and render correctly. The PM-JAY badge requirement (PUB-08) is satisfied by its prominent placement in the Home hero. Both pages are static server components that import from the seeded data constants and use the translation keys populated in Plan 01.

Output: A complete Home page with hero, trust signals band, departments preview, doctors preview, and appointment CTA band; a complete About page with founding story, stat cards, and mission/values section.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-public-website/02-CONTEXT.md
@.planning/phases/02-public-website/02-UI-SPEC.md
@.planning/phases/02-public-website/02-01-SUMMARY.md
@.planning/phases/02-public-website/02-02-SUMMARY.md

<interfaces>
<!-- Types and components available after Plans 01 and 02 -->

From lib/data/departments.ts:
  export interface Department { id: string; name: string; description: string; icon: LucideIcon; }
  export const departments: Department[] — 8 items

From lib/data/doctors.ts:
  export interface Doctor { id: string; name: string; specialty: string; initials: string; }
  export const doctors: Doctor[] — 6 items

From components/public/DepartmentCard.tsx:
  export default DepartmentCard — props: { department: Department }

From components/public/DoctorCard.tsx:
  export default DoctorCard — props: { doctor: Doctor }

From components/public/PMJAYBadge.tsx:
  export default PMJAYBadge — props: { size?: 'default' | 'small' }

From components/public/SectionHeading.tsx:
  export default SectionHeading — props: { title: string; subtitle?: string }

Next.js page pattern (must follow in every new page):
  import { setRequestLocale } from 'next-intl/server';
  import { getTranslations } from 'next-intl/server';
  import { routing } from '@/i18n/routing';

  export function generateStaticParams() {
    return routing.locales.map((locale) => ({ locale }));
  }

  export default async function PageName({ params }: { params: Promise<{ locale: string }> }) {
    const { locale } = await params;
    setRequestLocale(locale);
    const t = await getTranslations('namespace');
    // ...
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Home page (replace smoke test, implement 6-section layout)</name>
  <files>app/[locale]/(public)/page.tsx</files>
  <read_first>
    - app/[locale]/(public)/page.tsx — read the current file; you are replacing it entirely (it has Supabase smoke test code that must be removed per RESEARCH.md Pitfall 6)
    - .planning/phases/02-public-website/02-UI-SPEC.md — Home page layout specification (section 1 of Page-by-Page Layout Specifications) — all 6 sections, exact classNames, and copy strings
    - messages/en.json — "home" and "common" namespaces to confirm exact key names for t() calls
  </read_first>
  <action>
Replace app/[locale]/(public)/page.tsx entirely. Remove all Supabase import and smoke test code. This is a Server Component (no 'use client').

Import list:
- import { setRequestLocale, getTranslations } from 'next-intl/server'
- import { routing } from '@/i18n/routing'
- import type { Metadata } from 'next'
- import Link from 'next/link'
- import { BedDouble, Users, Stethoscope, Clock } from 'lucide-react'
- import { Button } from '@/components/ui/button'
- import PMJAYBadge from '@/components/public/PMJAYBadge'
- import DepartmentCard from '@/components/public/DepartmentCard'
- import DoctorCard from '@/components/public/DoctorCard'
- import SectionHeading from '@/components/public/SectionHeading'
- import { departments } from '@/lib/data/departments'
- import { doctors } from '@/lib/data/doctors'

Export metadata:
  title: "Atmaram Child Care and Critical Care — Kanpur"
  description: "A 90-bed super-specialty hospital in Kanpur offering paediatrics, critical care, surgery and more. Ayushman Bharat PM-JAY empanelled."

Export generateStaticParams returning routing.locales.map((locale) => ({ locale })).

Page component body:
1. Await params, call setRequestLocale(locale)
2. Await getTranslations('home') — use namespace 'home'

Section 1 — Hero:
- Outer: section with className="bg-white py-16 md:py-24 px-4"
- Inner container: div with className="max-w-4xl mx-auto text-center"
- Headline: h1 with className="text-4xl md:text-5xl font-bold text-slate-900" — text: "Quality Care for Every Child and Family"
- Tagline: p with className="text-lg text-slate-500 mt-4" — text: "Kanpur's trusted super-specialty hospital"
- PM-JAY badge: div className="mt-6 flex justify-center" wrapping PMJAYBadge (default size)
- CTA: div className="mt-8" with Link href="/appointment" wrapping Button with className="bg-green-600 hover:bg-green-700 text-white font-semibold min-h-[44px]" size="lg" — text: "Request an Appointment"
- Secondary link: div className="mt-4" with Link href="/services" className="text-blue-800 underline text-sm" — text: "Learn about our services →"

Section 2 — Trust Signals Band:
- section className="bg-blue-800 py-8"
- Inner div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"
- Grid div className="grid grid-cols-2 md:grid-cols-4 gap-6 text-center"
- 4 items, each: div className="flex flex-col items-center gap-2" containing the icon (w-6 h-6 text-white), a span with text-xl font-semibold text-white (the number/stat), and a span with text-sm text-white/70 (the label)
  - Item 1: BedDouble icon, "90", "Beds"
  - Item 2: Users icon, "25+", "Doctors"
  - Item 3: Stethoscope icon, "8", "Specialties"
  - Item 4: Clock icon, "24x7", "Emergency"

Section 3 — Departments Preview:
- section className="py-16 px-4"
- div className="max-w-7xl mx-auto"
- SectionHeading with title="Our Departments" subtitle="Expert care across 8 medical specialties"
- Grid div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mt-8"
- Map all 8 departments to DepartmentCard — use departments.map(dept => <DepartmentCard key={dept.id} department={dept} />)

Section 4 — Doctors Preview:
- section className="py-16 px-4 bg-slate-50"
- div className="max-w-7xl mx-auto"
- SectionHeading with title="Our Doctors" subtitle="Our team of specialists is here to help."
- Grid div className="grid grid-cols-1 sm:grid-cols-3 gap-6 mt-8"
- Map only the first 3 doctors: doctors.slice(0, 3).map(...)
- Below grid: div className="mt-8 text-center" with Link href="/doctors" className="text-blue-800 underline text-sm" — text: "Meet All Doctors →"

Section 5 — Appointment CTA Band:
- section className="bg-blue-50 py-16 px-4"
- div className="max-w-4xl mx-auto text-center"
- h2 className="text-xl font-semibold text-slate-900" — text: "Ready to visit us?"
- p className="text-slate-500 mt-2 text-base" — text: "Our team is here to help. Request an appointment today."
- div className="mt-8": Link href="/appointment" wrapping Button size="lg" className="bg-green-600 hover:bg-green-700 text-white font-semibold min-h-[44px]" — text: "Request an Appointment"

Note: The (public) layout (app/[locale]/(public)/layout.tsx) already renders Header and Footer around {children}. Do NOT include Header or Footer in this page component.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - app/[locale]/(public)/page.tsx does NOT contain any Supabase import or createClient call
    - app/[locale]/(public)/page.tsx does NOT contain 'use client' directive
    - File contains PMJAYBadge component render call
    - File contains departments.map and doctors.slice(0, 3).map
    - generateStaticParams is exported
    - metadata object is exported with correct title
    - npm run build exits 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Home page replaces smoke test; renders hero with PM-JAY badge, trust signals band, departments preview (8 cards), doctors preview (3 cards), appointment CTA band; statically generates for hi and en locales</done>
</task>

<task type="auto">
  <name>Task 2: Build About page with hospital story, stat cards, and mission/values</name>
  <files>app/[locale]/(public)/about/page.tsx</files>
  <read_first>
    - .planning/phases/02-public-website/02-UI-SPEC.md — About page layout specification (section 2 of Page-by-Page Layout Specifications): page header, 2-column hospital story + stat cards, mission/values 3-card section
    - messages/en.json — "about" namespace keys to use with t() calls (created in Plan 01 Task 3)
  </read_first>
  <action>
Create app/[locale]/(public)/about/page.tsx. Create the about/ directory if it does not exist. This is a Server Component (no 'use client').

Import list:
- import { setRequestLocale, getTranslations } from 'next-intl/server'
- import { routing } from '@/i18n/routing'
- import type { Metadata } from 'next'
- import { HeartPulse, Shield, Users } from 'lucide-react'
- import { Card, CardContent } from '@/components/ui/card'
- import SectionHeading from '@/components/public/SectionHeading'

Export metadata:
  title: "About Us | Atmaram Child Care and Critical Care"
  description: "Learn about Atmaram Child Care and Critical Care — Kanpur's trusted super-specialty hospital with 90 beds and 25+ doctors."

Export generateStaticParams returning routing.locales.map((locale) => ({ locale })).

Page component body:
1. Await params, call setRequestLocale(locale)
2. Await getTranslations('about')

Section 1 — Page Header:
- section className="py-12 bg-white px-4"
- div className="max-w-7xl mx-auto"
- p className="text-sm text-slate-500 mb-2" — breadcrumb text: "Home › About Us"
- h1 className="text-xl font-semibold text-slate-900" — text: "About Us"

Section 2 — Hospital Story (2-column):
- section className="py-12 px-4"
- div className="max-w-7xl mx-auto"
- Grid: div className="grid grid-cols-1 md:grid-cols-2 gap-12 items-start"
- Left column (story text):
  - h2 className="text-xl font-semibold text-slate-900" — text: "Caring for Kanpur Since Day One"
  - div className="mt-2 w-12 h-1 bg-blue-800 rounded-full"
  - p className="text-base text-slate-600 mt-6 leading-relaxed" — text: "Atmaram Child Care and Critical Care is a 90-bed super-specialty hospital in Kanpur, Uttar Pradesh. Empanelled under Ayushman Bharat PM-JAY, we provide accessible, high-quality care to every patient who walks through our doors."
- Right column (stat cards): div className="grid grid-cols-2 gap-4"
  - 4 stat cards, each as a shadcn Card with CardContent className="p-4 text-center"
  - Stat 1: span className="text-2xl font-bold text-blue-800" "90" + p className="text-sm text-slate-500 mt-1" "Beds"
  - Stat 2: span "25+" + p "Doctors"
  - Stat 3: span "8" + p "Specialties"
  - Stat 4: span "10+" + p "Years of Service"

Section 3 — Mission & Values:
- section className="py-12 px-4 bg-slate-50"
- div className="max-w-7xl mx-auto"
- SectionHeading with title="Our Mission"
- p className="text-base text-slate-600 mt-4 max-w-2xl" — text: "To provide compassionate, evidence-based medical care to children and families in Kanpur and the surrounding region, with a special commitment to serving patients covered under Ayushman Bharat PM-JAY."
- Values grid: div className="grid grid-cols-1 md:grid-cols-3 gap-6 mt-10"
  - 3 value cards, each as shadcn Card with CardContent className="p-6":
    - Card 1: HeartPulse icon className="w-8 h-8 text-blue-800 mb-4", h3 "Patient-First Care" className="text-xl font-semibold text-slate-900", p "Every decision centers on patient outcomes and family wellbeing." className="text-sm text-slate-500 mt-2"
    - Card 2: Shield icon, "Clinical Safety", "Rigorous protocols and trained staff at every level of care."
    - Card 3: Users icon, "Community Trust", "Serving Kanpur families with transparency and compassion."
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - app/[locale]/(public)/about/page.tsx exists
    - File does NOT contain 'use client'
    - File exports metadata with title "About Us | Atmaram Child Care and Critical Care"
    - File exports generateStaticParams
    - File contains "90-bed super-specialty hospital" text
    - File contains HeartPulse, Shield, Users icon imports from lucide-react
    - npm run build exits 0
  </acceptance_criteria>
  <done>About page renders hospital story, 4 stat cards (90 beds, 25+ doctors, 8 specialties, 10+ years), and mission/values section with 3 cards; statically generates for both locales</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Browser | These pages are fully server-rendered; no user input, no client state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | XSS via page content | accept | All content is hardcoded TypeScript constants and static strings; React escapes JSX values by default |
| T-02-SMOKE | Tampering | Supabase smoke test in page.tsx | mitigate | Remove entirely in Task 1 — the connection test code runs a database query and renders connection state; it must not appear in production pages |
</threat_model>

<verification>
After both tasks complete:
1. npm run build exits 0 — both pages statically generated (output shows ○ for /hi/ and /hi/about)
2. app/[locale]/(public)/page.tsx contains no Supabase imports
3. app/[locale]/(public)/page.tsx renders PMJAYBadge component
4. app/[locale]/(public)/about/page.tsx exists with generateStaticParams
</verification>

<success_criteria>
- Home page renders all 6 sections per UI-SPEC: hero with PM-JAY badge (PUB-08), trust signals band, departments preview (8 cards), doctors preview (3 cards), appointment CTA band
- About page renders founding story, 4 stat cards, and 3 mission/values cards
- Smoke test Supabase code removed from homepage
- Both pages export generateStaticParams and call setRequestLocale
- npm run build exits 0
</success_criteria>

<output>
Create .planning/phases/02-public-website/02-03-SUMMARY.md when done
</output>

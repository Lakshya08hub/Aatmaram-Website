---
phase: 02-public-website
plan: 05
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - app/[locale]/(public)/services/page.tsx
  - app/[locale]/(public)/contact/page.tsx
autonomous: true
requirements:
  - PUB-05
  - PUB-06
  - PUB-08

must_haves:
  truths:
    - "Visitor navigating to /hi/services sees the services list, prominent PM-JAY Ayushman Bharat section, and facilities grid"
    - "Visitor navigating to /hi/contact sees address, phone, OPD timings, emergency info, and a map placeholder div"
    - "PM-JAY badge appears on the Services page (Ayushman Bharat section) — satisfying PUB-08 on a second page beyond the homepage"
    - "Both pages statically pre-render for both locales"
  artifacts:
    - path: "app/[locale]/(public)/services/page.tsx"
      provides: "Services & Facilities page with PM-JAY section"
      contains: "PMJAYBadge"
    - path: "app/[locale]/(public)/contact/page.tsx"
      provides: "Contact page with address and map placeholder"
      contains: "generateStaticParams"
  key_links:
    - from: "app/[locale]/(public)/services/page.tsx"
      to: "lib/data/services.ts"
      via: "import services, facilities"
      pattern: "import.*services.*facilities.*from.*lib/data/services"
    - from: "app/[locale]/(public)/services/page.tsx"
      to: "components/public/PMJAYBadge.tsx"
      via: "import PMJAYBadge"
      pattern: "import.*PMJAYBadge"
---

<objective>
Build the Services/Facilities page and the Contact page as server components with seeded content.

Purpose: PUB-05 requires the services/facilities page with the Ayushman Bharat PM-JAY section prominent (this is a secondary placement for PUB-08). PUB-06 requires the contact page with address, phone, OPD timings, and a map placeholder (Google Maps API key is not configured in Phase 2 — per RESEARCH.md deferred items).

Output: Services page with 8-item services list (checkmark icons), Ayushman Bharat section with large PM-JAY badge, and 6-facility grid; Contact page with 4 contact detail cards and a map placeholder div.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-public-website/02-CONTEXT.md
@.planning/phases/02-public-website/02-UI-SPEC.md
@.planning/phases/02-public-website/02-01-SUMMARY.md
@.planning/phases/02-public-website/02-02-SUMMARY.md

<interfaces>
<!-- Available after Plans 01 and 02 -->

From lib/data/services.ts:
  export const services: Service[] — 8 items, each { id: string, name: string }
  export const facilities: Facility[] — 6 items, each { id: string, name: string, icon: LucideIcon }

From components/public/PMJAYBadge.tsx:
  export default PMJAYBadge — props: { size?: 'default' | 'small' }

From components/public/SectionHeading.tsx:
  export default SectionHeading — props: { title: string; subtitle?: string }

Standard page pattern:
  export function generateStaticParams() { return routing.locales.map((locale) => ({ locale })); }
  const { locale } = await params;
  setRequestLocale(locale);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Services page with Ayushman Bharat section and facilities grid</name>
  <files>app/[locale]/(public)/services/page.tsx</files>
  <read_first>
    - .planning/phases/02-public-website/02-UI-SPEC.md — Services page layout (section 5): services list with CheckCircle2 icons, Ayushman Bharat section with PM-JAY badge, facilities grid
    - messages/en.json — "services" namespace keys (pageTitle, pageSubtitle, ayushman.*, facilities.heading)
  </read_first>
  <action>
Create app/[locale]/(public)/services/page.tsx. Create the services/ directory. Server Component (no 'use client').

Import list:
- import { setRequestLocale, getTranslations } from 'next-intl/server'
- import { routing } from '@/i18n/routing'
- import type { Metadata } from 'next'
- import { CheckCircle2 } from 'lucide-react'
- import { Card, CardContent } from '@/components/ui/card'
- import PMJAYBadge from '@/components/public/PMJAYBadge'
- import SectionHeading from '@/components/public/SectionHeading'
- import { services, facilities } from '@/lib/data/services'

Export metadata:
  title: "Services & Facilities | Atmaram Child Care and Critical Care"
  description: "Comprehensive hospital services including ICU, NICU, emergency care, and Ayushman Bharat PM-JAY empanelled treatment."

Export generateStaticParams returning routing.locales.map((locale) => ({ locale })).

Page component body:
1. Await params, call setRequestLocale(locale)
2. Await getTranslations('services')

Section 1 — Page Header:
- section className="py-12 bg-white px-4"
- div className="max-w-7xl mx-auto"
- p className="text-sm text-slate-500 mb-2" — "Home › Services & Facilities"
- h1 className="text-xl font-semibold text-slate-900" — "Services & Facilities"

Section 2 — Services List (two-column):
- section className="py-12 px-4"
- div className="max-w-7xl mx-auto"
- SectionHeading with title="Services & Facilities" subtitle="Comprehensive care for every patient"
- div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-8"
- Map services array: each item rendered as div className="flex items-center gap-3" containing:
  - CheckCircle2 with className="w-5 h-5 text-green-600 flex-shrink-0"
  - span className="text-base text-slate-700" — service.name

Section 3 — Ayushman Bharat Section:
- section className="py-12 px-4 bg-blue-50"
- div className="max-w-7xl mx-auto"
- SectionHeading with title="Ayushman Bharat PM-JAY"
- div className="mt-6 flex justify-start" wrapping PMJAYBadge (default size — no size prop needed)
- p className="text-base text-slate-600 mt-6 max-w-2xl leading-relaxed" — text: "Atmaram Child Care and Critical Care is empanelled under the Ayushman Bharat Pradhan Mantri Jan Arogya Yojana (PM-JAY) scheme. Eligible patients receive cashless treatment for covered procedures."
- h3 className="text-xl font-semibold text-slate-900 mt-8" — text: "What is PM-JAY?"
- p className="text-base text-slate-600 mt-2 max-w-2xl leading-relaxed" — text: "PM-JAY is India's flagship public health insurance scheme providing coverage of up to ₹5 lakh per family per year for secondary and tertiary hospitalisation."

Section 4 — Facilities Grid:
- section className="py-12 px-4"
- div className="max-w-7xl mx-auto"
- SectionHeading with title="Our Facilities"
- div className="grid grid-cols-2 md:grid-cols-3 gap-4 mt-8"
- Map facilities array: each as shadcn Card with CardContent className="p-4 flex flex-col items-center text-center gap-3"
  - Render facility.icon as: const Icon = facility.icon; then <Icon className="w-8 h-8 text-blue-800" />
  - p className="text-sm font-semibold text-slate-900" — facility.name
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - app/[locale]/(public)/services/page.tsx exists
    - File does NOT contain 'use client'
    - File imports and maps services array (should render 8 items with CheckCircle2)
    - File imports and maps facilities array (should render 6 facility cards)
    - File contains PMJAYBadge render call (for the Ayushman Bharat section)
    - File exports generateStaticParams
    - npm run build exits 0
  </acceptance_criteria>
  <done>Services page renders 8 services (checkmark list), Ayushman Bharat section with large PM-JAY badge, and 6 facility cards; statically generates for both locales</done>
</task>

<task type="auto">
  <name>Task 2: Build Contact page with contact cards and map placeholder</name>
  <files>app/[locale]/(public)/contact/page.tsx</files>
  <read_first>
    - .planning/phases/02-public-website/02-UI-SPEC.md — Contact page layout (section 6): two-column layout with 4 contact cards (address, phone, OPD timings, emergency) and map placeholder div
    - messages/en.json — "contact" namespace keys for all card content
  </read_first>
  <action>
Create app/[locale]/(public)/contact/page.tsx. Create the contact/ directory. Server Component (no 'use client').

Import list:
- import { setRequestLocale, getTranslations } from 'next-intl/server'
- import { routing } from '@/i18n/routing'
- import type { Metadata } from 'next'
- import { MapPin, Phone, Clock, AlertCircle } from 'lucide-react'
- import { Card, CardContent } from '@/components/ui/card'
- import SectionHeading from '@/components/public/SectionHeading'

Export metadata:
  title: "Contact Us | Atmaram Child Care and Critical Care"
  description: "Find us at Naubasta, Kanpur-208021. OPD timings, emergency number, and location."

Export generateStaticParams returning routing.locales.map((locale) => ({ locale })).

Page component body:
1. Await params, call setRequestLocale(locale)
2. Await getTranslations('contact')

Section 1 — Page Header:
- section className="py-12 bg-white px-4"
- div className="max-w-7xl mx-auto"
- p className="text-sm text-slate-500 mb-2" — "Home › Contact Us"
- h1 className="text-xl font-semibold text-slate-900" — "Contact Us"

Section 2 — Contact Details + Map (2-column):
- section className="py-12 px-4"
- div className="max-w-7xl mx-auto"
- SectionHeading with title="Get in Touch"
- Grid: div className="grid grid-cols-1 md:grid-cols-2 gap-12 mt-8 items-start"
- Left column (4 contact cards, stacked): div className="flex flex-col gap-4"
  - Card 1 (Address): shadcn Card with CardContent className="p-4 flex items-start gap-3". MapPin icon className="w-5 h-5 text-blue-800 mt-0.5 flex-shrink-0". div with h3 className="text-sm font-semibold text-slate-900" "Address" + p className="text-sm text-slate-600 mt-1" "Naubasta / Kidwai Nagar, Kanpur - 208021, Uttar Pradesh"
  - Card 2 (Phone): Phone icon, "Phone", "+91-XXXXXXXXXX (placeholder)"
  - Card 3 (OPD Timings): Clock icon, "OPD Timings", "Mon–Sat: 9:00 AM – 5:00 PM"
  - Card 4 (Emergency): AlertCircle icon className="w-5 h-5 text-red-600 mt-0.5 flex-shrink-0", "24x7 Emergency", "+91-XXXXXXXXXX"

- Right column (map placeholder): div className="h-64 bg-slate-100 rounded-xl flex flex-col items-center justify-center gap-2" containing:
  - MapPin icon className="w-8 h-8 text-slate-400"
  - p className="text-sm text-slate-400" — "Map loading..."
  - p className="text-xs text-slate-400 text-center px-4" — "Interactive map will be available soon"

Note: The map placeholder matches the UI-SPEC Map Placeholder specification exactly — no Google Maps embed in Phase 2 (API key not configured).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - app/[locale]/(public)/contact/page.tsx exists
    - File does NOT contain 'use client'
    - File contains the address "Naubasta / Kidwai Nagar, Kanpur - 208021, Uttar Pradesh"
    - File contains map placeholder div with bg-slate-100 and "Map loading..." text
    - File exports generateStaticParams
    - File exports metadata with title containing "Contact Us"
    - npm run build exits 0
  </acceptance_criteria>
  <done>Contact page renders 4 contact cards (address, phone, OPD timings, emergency) and a map placeholder; statically generates for both locales</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Browser | Pure server-rendered pages; no user input; contact info is hardcoded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | XSS via contact/services content | accept | All content is hardcoded TypeScript constants and static strings; React escapes JSX values by default |
| T-02-MAP | Information Disclosure | Map placeholder (no real location pin) | accept | No GPS coordinates or live map in Phase 2; placeholder text only. Phase 2.x or Phase 5 adds real map embed with API key. No privacy risk. |
</threat_model>

<verification>
After both tasks complete:
1. npm run build exits 0
2. Build output shows ○ (static) for /hi/services and /hi/contact
3. Services page renders PMJAYBadge in Ayushman Bharat section
4. Contact page contains "Naubasta" address text
</verification>

<success_criteria>
- Services page renders 8 services with CheckCircle2 icons, Ayushman Bharat section with prominent PM-JAY badge (PUB-08), and 6 facility cards
- Contact page renders 4 contact detail cards and a map placeholder div (no broken iframe, no API key required)
- Both pages have breadcrumb, page header, generateStaticParams, correct SEO metadata
- npm run build exits 0
</success_criteria>

<output>
Create .planning/phases/02-public-website/02-05-SUMMARY.md when done
</output>

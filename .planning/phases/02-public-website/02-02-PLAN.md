---
phase: 02-public-website
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - lib/data/departments.ts
  - lib/data/doctors.ts
  - lib/data/services.ts
  - components/layout/Header.tsx
  - components/layout/Footer.tsx
  - components/public/DepartmentCard.tsx
  - components/public/DoctorCard.tsx
  - components/public/PMJAYBadge.tsx
  - components/public/SectionHeading.tsx
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-03
  - PUB-04
  - PUB-05
  - PUB-06
  - PUB-07
  - PUB-08

must_haves:
  truths:
    - "Seeded data constants for all 8 departments, 6 doctors, and services list exist in lib/data/ — importable by any page"
    - "Header renders with dark navy background, hospital name, nav links, language toggle placeholder, and green Book Appointment CTA"
    - "Header has a mobile hamburger button that opens a shadcn Sheet drawer with nav links"
    - "Footer renders 3-column layout with hospital info, quick links, and contact details"
    - "DepartmentCard, DoctorCard, PMJAYBadge, and SectionHeading shared components exist and accept typed props"
  artifacts:
    - path: "lib/data/departments.ts"
      provides: "8 department constants with id, name, description, icon"
      exports: ["Department", "departments"]
    - path: "lib/data/doctors.ts"
      provides: "6 doctor constants with id, name, specialty, initials"
      exports: ["Doctor", "doctors"]
    - path: "lib/data/services.ts"
      provides: "Services list and facilities list"
      exports: ["Service", "Facility", "services", "facilities"]
    - path: "components/layout/Header.tsx"
      provides: "Sticky header with nav + mobile sheet"
      contains: "data-testid=\"header\""
    - path: "components/layout/Footer.tsx"
      provides: "3-column footer"
      contains: "data-testid=\"footer\""
    - path: "components/public/DepartmentCard.tsx"
      provides: "Card component accepting Department prop"
      exports: ["DepartmentCard"]
    - path: "components/public/DoctorCard.tsx"
      provides: "Card component accepting Doctor prop"
      exports: ["DoctorCard"]
    - path: "components/public/PMJAYBadge.tsx"
      provides: "Reusable PM-JAY badge with size variant"
      exports: ["PMJAYBadge"]
    - path: "components/public/SectionHeading.tsx"
      provides: "Reusable h2 + subtitle + blue underline accent"
      exports: ["SectionHeading"]
  key_links:
    - from: "components/layout/Header.tsx"
      to: "lib/data/departments.ts"
      via: "no link — nav items are hardcoded strings, not from data"
      pattern: "nav.*links.*hardcoded"
    - from: "components/public/DepartmentCard.tsx"
      to: "lib/data/departments.ts"
      via: "Department type import"
      pattern: "import.*Department.*departments"
    - from: "components/public/DoctorCard.tsx"
      to: "lib/data/doctors.ts"
      via: "Doctor type import"
      pattern: "import.*Doctor.*doctors"
---

<objective>
Build the seeded data constants (Phase 5 migration points) and all shared UI components that every public page depends on.

Purpose: Pages built in Plans 03, 04, and 05 must import from these files. Creating them first means those plans can import cleanly without creating files mid-execution. The lib/data/ constants pattern also ensures Phase 5 can swap hardcoded data for Supabase queries by changing a single import per file.

Output: lib/data/ with 3 typed constants files; full Header and Footer implementations (replacing Plan 01 placeholders); DepartmentCard, DoctorCard, PMJAYBadge, and SectionHeading shared components.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-public-website/02-CONTEXT.md
@.planning/phases/02-public-website/02-UI-SPEC.md
@.planning/phases/02-public-website/02-01-SUMMARY.md

<interfaces>
<!-- Seeded data structures the executor must implement -->

Department type (to define in lib/data/departments.ts):
- id: string
- name: string
- description: string
- icon: LucideIcon (from lucide-react)

Doctor type (to define in lib/data/doctors.ts):
- id: string
- name: string
- specialty: string
- initials: string  (e.g., "RS" for Rajesh Sharma)

Service type (to define in lib/data/services.ts):
- id: string
- name: string  (service/capability name)

Facility type (to define in lib/data/services.ts):
- id: string
- name: string
- icon: LucideIcon

Seeded department data (8 records — per D-04 and UI-SPEC):
1. id: "paediatrics", name: "Paediatrics & Neonatology", icon: Baby, description: "Comprehensive care for newborns, infants, and children up to 18 years."
2. id: "paediatric-surgery", name: "Paediatric Surgery", icon: Scissors, description: "Surgical care for children with congenital and acquired conditions."
3. id: "critical-care", name: "Critical Care & ICU", icon: Activity, description: "24x7 intensive care unit with advanced life support for critically ill patients."
4. id: "general-surgery", name: "General Surgery", icon: Stethoscope, description: "Expert surgical management of abdominal, gastrointestinal, and trauma conditions."
5. id: "orthopaedics", name: "Orthopaedics", icon: Bone, description: "Diagnosis and treatment of musculoskeletal disorders, fractures, and joint conditions."
6. id: "obstetrics", name: "Obstetrics & Gynaecology", icon: Heart, description: "Complete maternity care, delivery services, and women's health consultations."
7. id: "general-medicine", name: "General Medicine", icon: Pill, description: "Diagnosis and treatment of a broad range of adult medical conditions."
8. id: "emergency", name: "Emergency & Trauma", icon: AlertCircle, description: "Round-the-clock emergency care and trauma management."

Seeded doctor data (6 records — per UI-SPEC Doctor Card section):
1. id: "dr-sharma", name: "Dr. Rajesh Sharma", specialty: "Paediatrics & Neonatology", initials: "RS"
2. id: "dr-singh", name: "Dr. Priya Singh", specialty: "Paediatric Surgery", initials: "PS"
3. id: "dr-kumar", name: "Dr. Anil Kumar", specialty: "Critical Care & ICU", initials: "AK"
4. id: "dr-verma", name: "Dr. Sunita Verma", specialty: "Obstetrics & Gynaecology", initials: "SV"
5. id: "dr-gupta", name: "Dr. Vikram Gupta", specialty: "Orthopaedics", initials: "VG"
6. id: "dr-agarwal", name: "Dr. Meena Agarwal", specialty: "General Medicine", initials: "MA"

Component props (define these interfaces in the component files):
- DepartmentCard: props { department: Department }
- DoctorCard: props { doctor: Doctor }
- PMJAYBadge: props { size?: 'default' | 'small' } (default is hero size, small is footer size)
- SectionHeading: props { title: string; subtitle?: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seeded data constants in lib/data/</name>
  <files>lib/data/departments.ts, lib/data/doctors.ts, lib/data/services.ts</files>
  <read_first>
    - .planning/phases/02-public-website/02-UI-SPEC.md — Department Card section (8 departments with icon assignments), Doctor Card section (6 seeded doctors), Services page layout (services list and facilities list)
    - .planning/phases/02-public-website/02-RESEARCH.md — Seeded Data Constants File Pattern code example for the exact TypeScript module structure
    - .planning/phases/02-public-website/02-CONTEXT.md — D-04 and D-05 decisions for the seeded content requirements
  </read_first>
  <action>
Create three TypeScript constants files in lib/data/. These files export typed arrays. Phase 5 will swap the hardcoded arrays for Supabase queries without changing the consumer components.

lib/data/departments.ts:
- Import LucideIcon type from lucide-react
- Import the 8 lucide icons: Baby, Scissors, Activity, Stethoscope, Bone, Heart, Pill, AlertCircle
- Export interface Department with fields: id (string), name (string), description (string), icon (LucideIcon)
- Export const departments: Department[] with all 8 records using data from the interfaces block above

lib/data/doctors.ts:
- Export interface Doctor with fields: id (string), name (string), specialty (string), initials (string)
- Export const doctors: Doctor[] with all 6 records using data from the interfaces block above
- No lucide icons needed for doctors

lib/data/services.ts:
- Import LucideIcon type from lucide-react
- Import icons for facilities: BedDouble, FlaskConical, Building2, Siren, TestTube2, ShoppingBag
- Export interface Service with fields: id (string), name (string)
- Export const services: Service[] with these 8 entries (aligned with departments):
  1. { id: "paediatric-care", name: "Paediatric & Neonatal Care" }
  2. { id: "paediatric-surgery", name: "Paediatric Surgery" }
  3. { id: "critical-care", name: "Critical Care & ICU" }
  4. { id: "general-surgery", name: "General Surgery" }
  5. { id: "orthopaedic-care", name: "Orthopaedic Care" }
  6. { id: "maternity-care", name: "Maternity & Women's Health" }
  7. { id: "general-medicine", name: "General Medicine" }
  8. { id: "emergency-trauma", name: "Emergency & Trauma Care" }
- Export interface Facility with fields: id (string), name (string), icon (LucideIcon)
- Export const facilities: Facility[] with these 6 entries:
  1. { id: "icu", name: "Intensive Care Unit (ICU)", icon: Activity }
  2. { id: "nicu", name: "NICU (Neonatal ICU)", icon: Baby }
  3. { id: "operation-theatre", name: "Operation Theatre", icon: Scissors }
  4. { id: "emergency", name: "24x7 Emergency", icon: AlertCircle }
  5. { id: "laboratory", name: "Diagnostic Laboratory", icon: FlaskConical }
  6. { id: "pharmacy", name: "Pharmacy", icon: ShoppingBag }

Note: Re-import the icons needed in services.ts (Activity, Baby, Scissors, AlertCircle are already in lucide-react). FlaskConical and ShoppingBag are valid lucide-react icon names.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "^$" | head -10 && echo "TSC_OK"</automated>
  </verify>
  <acceptance_criteria>
    - lib/data/departments.ts exports Department interface and departments array with exactly 8 items
    - lib/data/doctors.ts exports Doctor interface and doctors array with exactly 6 items
    - lib/data/services.ts exports Service, Facility interfaces and services (8 items) and facilities (6 items) arrays
    - All icon imports resolve (Baby, Scissors, Activity, Stethoscope, Bone, Heart, Pill, AlertCircle, FlaskConical, ShoppingBag from lucide-react)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All seeded data constants created — departments (8), doctors (6), services (8), facilities (6) — typed and ready to import into page components</done>
</task>

<task type="auto">
  <name>Task 2: Build Header and Footer layout components</name>
  <files>components/layout/Header.tsx, components/layout/Footer.tsx</files>
  <read_first>
    - components/layout/Header.tsx — read the current placeholder (created in Plan 01, Task 2) before replacing it
    - components/layout/Footer.tsx — read the current placeholder before replacing it
    - .planning/phases/02-public-website/02-UI-SPEC.md — Header specification (64px height, dark navy #1E3A5F, nav links, mobile Sheet, language toggle placeholder) and Footer specification (3-column layout, quick links, contact details)
    - .planning/phases/02-public-website/02-RESEARCH.md — Pattern 4 (Header/Footer in (public) layout) and Anti-Patterns (Header/Footer must NOT be in app/[locale]/layout.tsx)
    - messages/en.json — nav namespace keys to confirm what t('nav.home') etc. resolve to
  </read_first>
  <action>
Replace the placeholder Header.tsx with the full Header implementation. Header is a Server Component (no 'use client') that renders the main navigation. The mobile Sheet (hamburger menu) requires a thin Client Component wrapper for the open/close state.

Header structure:
- Outer element: nav with data-testid="header", className="sticky top-0 z-50 h-16 bg-[#1E3A5F] shadow-sm"
- Inner container: div with className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-full flex items-center justify-between"
- Logo area (left): Link href="/hi" (temporary — Phase 3 makes locale-aware) with text "Atmaram Child Care" in className="text-lg font-semibold text-white"
- Desktop nav links (center/right): ul with className="hidden md:flex items-center gap-6". Each nav item is a Link with className="text-sm font-semibold text-white/80 hover:text-white transition-colors". Nav links: Home → /, About Us → /about, Departments → /departments, Our Doctors → /doctors, Services → /services, Contact → /contact
- Language toggle placeholder: span with className="hidden md:block text-sm text-white/60 cursor-not-allowed select-none" and text "EN/HI". Add title="Language toggle coming soon" attribute.
- Desktop CTA button: Link href="/appointment" wrapped in a shadcn Button with className="hidden md:flex bg-green-600 hover:bg-green-700 text-white text-sm font-semibold min-h-[44px]". Text: "Book Appointment"
- Mobile hamburger: div with className="md:hidden". This requires a Client Component for Sheet state. Create a separate file components/layout/MobileNav.tsx as a 'use client' component that contains the shadcn Sheet + SheetTrigger + SheetContent. MobileNav renders a Button variant="ghost" with text-white, aria-label="Open navigation menu", and the lucide Menu icon (24px). SheetContent side="right" className="w-72 bg-white". Sheet interior: stacked nav links as Link elements with full-width padding, plus Book Appointment button at bottom. Import MobileNav in Header.tsx.

For all nav link hrefs in Header, use plain paths (/about, /departments, etc.) without locale prefix — in Phase 3, these will be updated to use the usePathname + locale pattern from next-intl. For Phase 2 with localePrefix: 'always', the default locale 'hi' is always prefixed, so links should work when visiting /hi/... routes. Add a note comment in the file: "TODO Phase 3: Replace hrefs with locale-aware Link from next-intl/navigation"

Footer structure:
- Element: footer with data-testid="footer", className="bg-[#1E3A5F] text-white"
- Inner container: div with className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12"
- Grid: div with className="grid grid-cols-1 md:grid-cols-3 gap-8"
  - Column 1: Hospital name ("Atmaram Child Care and Critical Care") in text-lg font-semibold, tagline ("Kanpur's trusted super-specialty hospital") in text-sm text-white/70 mt-2, then the PMJAYBadge component with size="small" (import from @/components/public/PMJAYBadge)
  - Column 2: Heading "Quick Links" in text-sm font-semibold text-white mb-4, then ul with nav links (same as header) using className="text-sm text-white/70 hover:text-white transition-colors" on each Link
  - Column 3: Heading "Contact" in text-sm font-semibold text-white mb-4, then: Address (Naubasta / Kidwai Nagar, Kanpur - 208021, Uttar Pradesh), Phone (+91-XXXXXXXXXX (placeholder)), OPD (Mon–Sat: 9:00 AM – 5:00 PM) — each as div with text-sm text-white/70
- Bottom bar: div with className="border-t border-white/10 mt-8 pt-4 text-sm text-white/50 text-center" containing "© 2025 Atmaram Child Care and Critical Care. All rights reserved."

Do NOT add 'use client' to Header.tsx or Footer.tsx — only MobileNav.tsx is a client component.

Important: PMJAYBadge is created in Task 3 of this plan. Create the Footer file but import PMJAYBadge — if it doesn't exist yet, the TypeScript compiler will error. Complete Task 3 before running npm run build.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "^$" | head -10 && echo "TSC_OK"</automated>
  </verify>
  <acceptance_criteria>
    - components/layout/Header.tsx has data-testid="header" and className includes "bg-[#1E3A5F]"
    - components/layout/Header.tsx is a Server Component (no 'use client' directive at top of file)
    - components/layout/MobileNav.tsx exists with 'use client' directive and imports Sheet, SheetTrigger, SheetContent from @/components/ui/sheet
    - components/layout/MobileNav.tsx has a Button with aria-label="Open navigation menu"
    - components/layout/Footer.tsx has data-testid="footer" and className includes "bg-[#1E3A5F]"
    - Footer renders "© 2025 Atmaram Child Care and Critical Care. All rights reserved."
    - npx tsc --noEmit exits 0 after Task 3 completes
    - npm run build exits 0
  </acceptance_criteria>
  <done>Full Header (with mobile Sheet nav) and Footer components replace Plan 01 placeholders; Header is sticky dark navy; Footer is 3-column with PM-JAY badge</done>
</task>

<task type="auto">
  <name>Task 3: Build shared public components (DepartmentCard, DoctorCard, PMJAYBadge, SectionHeading)</name>
  <files>components/public/DepartmentCard.tsx, components/public/DoctorCard.tsx, components/public/PMJAYBadge.tsx, components/public/SectionHeading.tsx</files>
  <read_first>
    - .planning/phases/02-public-website/02-UI-SPEC.md — Department Card specification, Doctor Card specification, PM-JAY Badge specification (both sizes), Section Headings reusable pattern
    - lib/data/departments.ts — Department type (created in Task 1) to confirm import path
    - lib/data/doctors.ts — Doctor type (created in Task 1) to confirm import path
  </read_first>
  <action>
Create four component files in components/public/. All are Server Components (no 'use client'). Create the components/public/ directory if it does not exist.

components/public/DepartmentCard.tsx:
- Import Department from '@/lib/data/departments'
- Import Card, CardHeader, CardContent from '@/components/ui/card'
- Props: { department: Department }
- Render using shadcn Card with className="bg-white border border-slate-200 rounded-xl shadow-sm hover:shadow-md transition-shadow"
- CardHeader: 48px circle div with className="w-12 h-12 rounded-full bg-blue-50 flex items-center justify-center". Inside: render department.icon as a React element with className="w-6 h-6 text-blue-800". Since icon is a LucideIcon component type, render it as: const Icon = department.icon; then <Icon className="w-6 h-6 text-blue-800" />
- Department name: h3 with className="text-xl font-semibold text-slate-900 mt-4"
- Description: p with className="text-sm text-slate-500 mt-2 leading-relaxed"

components/public/DoctorCard.tsx:
- Import Doctor from '@/lib/data/doctors'
- Import Card, CardContent from '@/components/ui/card'
- Import Separator from '@/components/ui/separator'
- Import Button from '@/components/ui/button'
- Import Link from 'next/link'
- Props: { doctor: Doctor }
- Render shadcn Card with className="bg-white border border-slate-200 rounded-xl shadow-sm"
- CardContent with className="p-6 flex flex-col items-center text-center"
- Avatar: div with className="w-20 h-20 rounded-full bg-blue-100 flex items-center justify-center" aria-label={"Doctor avatar for " + doctor.name}. Inside: span with className="text-blue-800 font-semibold text-xl" showing doctor.initials
- Doctor name: h3 with className="text-xl font-semibold text-slate-900 mt-4"
- Specialty: p with className="text-sm text-slate-500"
- Separator with className="mt-4"
- Link href="/appointment": Button variant="outline" size="sm" className="mt-4 border-blue-800 text-blue-800 hover:bg-blue-50 min-h-[44px]" with text "Book Appointment"

components/public/PMJAYBadge.tsx:
- Import ShieldCheck from 'lucide-react'
- Props: { size?: 'default' | 'small' }
- The badge must have role="img" aria-label="Ayushman Bharat PM-JAY Empanelled"
- Default size (hero placement): div with className="inline-flex items-center gap-2 rounded-full bg-green-600 px-4 py-2". ShieldCheck with className="w-4 h-4 text-white flex-shrink-0". Text span: "Ayushman Bharat PM-JAY Empanelled" with className="text-sm font-semibold text-white"
- Small size (footer/services): same but with className="inline-flex items-center gap-1.5 rounded-full bg-green-600 px-3 py-1". ShieldCheck with className="w-3 h-3 text-white flex-shrink-0". Text span with className="text-xs font-semibold text-white"
- Use conditional rendering based on size prop (default is 'default')

components/public/SectionHeading.tsx:
- Props: { title: string; subtitle?: string }
- Render: div wrapper with className="mb-8"
- h2 with className="text-xl font-semibold text-slate-900"
- Conditional p (if subtitle) with className="text-slate-500 mt-2 text-base"
- Decorative underline: div with className="mt-2 w-12 h-1 bg-blue-800 rounded-full"
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - components/public/DepartmentCard.tsx exists, imports Department type, renders shadcn Card with "hover:shadow-md transition-shadow"
    - components/public/DoctorCard.tsx exists, imports Doctor type, doctor avatar div has aria-label starting with "Doctor avatar for"
    - components/public/PMJAYBadge.tsx exists, rendered element has role="img" aria-label="Ayushman Bharat PM-JAY Empanelled", green background bg-green-600
    - components/public/SectionHeading.tsx exists, renders h2 with text-xl font-semibold and blue underline div with bg-blue-800
    - npm run build exits 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>All 4 shared public components built; Header and Footer TypeScript errors (from missing PMJAYBadge) resolve; full build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Browser | Header nav links and mobile Sheet are the only interactive elements; Sheet state stays in MobileNav client component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm/lucide-react icon imports | accept | lucide-react is installed by shadcn init; well-established package with 100M+ weekly downloads; already in package.json after Plan 01 |
| T-02-NAV | Spoofing | Header nav link hrefs (plain paths, no locale prefix) | accept | Phase 2 only; next-intl middleware handles locale prefix routing; Phase 3 replaces with locale-aware Link from next-intl/navigation. TODO comment added to Header.tsx to track this. |
| T-02-XSS | Tampering | DoctorCard aria-label interpolation | accept | doctor.name is a hardcoded TypeScript constant, not user input; React escapes JSX attribute values |
</threat_model>

<verification>
After all 3 tasks complete:
1. npm run build exits 0
2. npx tsc --noEmit exits 0
3. lib/data/departments.ts exports 8 departments with LucideIcon types
4. lib/data/doctors.ts exports 6 doctors
5. components/layout/Header.tsx is a Server Component (no 'use client') with sticky dark nav
6. components/layout/MobileNav.tsx is 'use client' with Sheet-based drawer
7. components/public/PMJAYBadge.tsx renders with role="img" aria-label="Ayushman Bharat PM-JAY Empanelled"
</verification>

<success_criteria>
- All seeded data is in lib/data/ TypeScript files ready for Phase 5 Supabase migration swap
- Header renders sticky dark navy bar with hamburger on mobile and nav links + CTA on desktop
- Footer renders 3-column layout with PM-JAY badge (small variant)
- DepartmentCard and DoctorCard accept typed props and render shadcn Card layouts per UI-SPEC
- PMJAYBadge component is reusable with default and small size variants
- SectionHeading component provides consistent h2 + blue underline pattern
- npm run build exits 0
</success_criteria>

<output>
Create .planning/phases/02-public-website/02-02-SUMMARY.md when done
</output>

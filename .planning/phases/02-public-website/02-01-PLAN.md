---
phase: 02-public-website
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/[locale]/layout.tsx
  - app/[locale]/(public)/layout.tsx
  - messages/en.json
  - messages/hi.json
  - components.json
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-03
  - PUB-04
  - PUB-05
  - PUB-06
  - PUB-07
  - PUB-08

must_haves:
  truths:
    - "shadcn/ui is initialized — components.json exists and lib/utils.ts exports cn()"
    - "Geist font is loaded via next/font/google and --font-geist-sans resolves on every page"
    - "The (public) route group has a layout that wraps children with Header and Footer placeholders"
    - "All page translation namespaces exist in messages/en.json so t('key') never returns empty string"
    - "Design system color tokens exist in globals.css @theme inline block"
  artifacts:
    - path: "components.json"
      provides: "shadcn/ui config — confirms Tailwind v4 path (config: '')"
      contains: "\"config\": \"\""
    - path: "lib/utils.ts"
      provides: "cn() helper for class merging"
      exports: ["cn"]
    - path: "app/[locale]/layout.tsx"
      provides: "Geist font loaded, Toaster mounted"
      contains: "geist.variable"
    - path: "app/[locale]/(public)/layout.tsx"
      provides: "Header + Footer wrap public pages"
      contains: "PublicLayout"
    - path: "app/globals.css"
      provides: "shadcn theme + project color tokens"
      contains: "--color-primary"
    - path: "messages/en.json"
      provides: "All translation namespaces: nav, common, home, about, departments, doctors, services, contact, appointment"
  key_links:
    - from: "app/[locale]/layout.tsx"
      to: "globals.css"
      via: "--font-geist-sans CSS variable"
      pattern: "geist\\.variable"
    - from: "app/[locale]/(public)/layout.tsx"
      to: "components/layout/Header.tsx"
      via: "import + JSX"
      pattern: "import.*Header"
---

<objective>
Bootstrap the design system, fix the Geist font gap, create the (public) route group layout with Header/Footer shell, and seed all translation namespaces that every page in Phase 2 depends on.

Purpose: Nothing in Phase 2 can be built correctly until shadcn/ui is initialized (components need cn(), CSS variables, and the component files), the font is actually wired (it resolves to nothing today), the (public) layout exists (Header and Footer must wrap all public pages but not the portal), and translation keys exist in messages/en.json (next-intl throws on missing keys).

Output: components.json, lib/utils.ts, updated globals.css with shadcn theme + project color tokens, updated app/[locale]/layout.tsx with Geist font + Toaster, new app/[locale]/(public)/layout.tsx, and complete messages/en.json + messages/hi.json with placeholder English values for all Phase 2 namespaces.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-public-website/02-CONTEXT.md
@.planning/phases/02-public-website/02-RESEARCH.md
@.planning/phases/02-public-website/02-UI-SPEC.md

<interfaces>
<!-- Existing files executor must understand before modifying -->

From app/[locale]/layout.tsx (current state):
```typescript
import { NextIntlClientProvider } from 'next-intl';
import { getMessages } from 'next-intl/server';
import { notFound } from 'next/navigation';
import { routing } from '@/i18n/routing';

export default async function LocaleLayout({ children, params }: {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  if (!routing.locales.includes(locale as 'hi' | 'en')) { notFound(); }
  const messages = await getMessages();
  return (
    <html lang={locale}>
      <body>
        <NextIntlClientProvider messages={messages}>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

From app/globals.css (current state — @theme inline has no project color tokens):
```css
@import "tailwindcss";
:root { --background: #ffffff; --foreground: #171717; }
@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}
body { font-family: Arial, Helvetica, sans-serif; }
```

From i18n/routing.ts:
```typescript
export const routing = defineRouting({
  locales: ['hi', 'en'],
  defaultLocale: 'hi',
  localePrefix: 'always'
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui and merge project design tokens into globals.css</name>
  <files>components.json, lib/utils.ts, app/globals.css, components/ui/ (auto-generated by shadcn init)</files>
  <read_first>
    - app/globals.css — read current @theme inline block before running init; you will need to merge project tokens back in after init rewrites it
    - .planning/phases/02-public-website/02-UI-SPEC.md — Color section for exact CSS variable names and hex values; Component Mapping section for the full list of shadcn components to install
    - .planning/phases/02-public-website/02-RESEARCH.md — Pattern 3 (shadcn init on Tailwind v4) and Pitfall 1 (CSS variable merge) for the exact init sequence and merge procedure
  </read_first>
  <action>
Run shadcn init and install all required components. The init sequence must be:

Step 1 — Run shadcn init:
```
npx shadcn@latest init
```
When prompted, select: TypeScript yes, App Router yes, src/ directory: No, import alias @/*, CSS variables: Yes, baseColor: neutral. This auto-detects Tailwind v4 (no tailwind.config.js present) and generates components.json with "config": "" (the Tailwind v4 signal). It will also update app/globals.css by injecting @import "tw-animate-css", @import "shadcn/tailwind.css", and a :root + @theme inline block with HSL CSS variables.

Step 2 — Immediately after init, open app/globals.css and add the project color tokens to the @theme inline block that shadcn just generated. Do NOT overwrite what shadcn added — merge the following into the existing @theme inline block:
  --color-bg: #F8FAFC;
  --color-surface: #FFFFFF;
  --color-primary: #1E40AF;
  --color-accent: #16A34A;
  --color-nav: #1E3A5F;
  --color-text: #0F172A;
  --color-muted: #64748B;
  --color-border: #E2E8F0;

Also ensure the body rule sets font-family using the CSS variable: font-family: var(--font-sans), Arial, Helvetica, sans-serif; (replacing the hardcoded Arial).

Step 3 — Install shadcn UI components (one command):
```
npx shadcn@latest add button card badge input label textarea select sheet separator sonner
```
This installs components into components/ui/ and will install lucide-react and tw-animate-css as dependencies if not already present.

Step 4 — Install form packages:
```
npm install react-hook-form @hookform/resolvers zod
```

Do not manually create components/ui/ files — shadcn add generates them. Do not use npx shadcn-ui@latest (deprecated package name); use npx shadcn@latest.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const cj=JSON.parse(fs.readFileSync('components.json','utf8')); if(cj.tailwind.config!=='') process.exit(1); const css=fs.readFileSync('app/globals.css','utf8'); if(!css.includes('--color-primary')) process.exit(1); if(!css.includes('--color-accent')) process.exit(1); if(!fs.existsSync('lib/utils.ts')) process.exit(1); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - components.json exists and contains "config": "" (Tailwind v4 path confirmed)
    - lib/utils.ts exists and exports cn() function
    - app/globals.css contains @import "tw-animate-css" (added by shadcn init)
    - app/globals.css @theme inline block contains --color-primary: #1E40AF and --color-accent: #16A34A
    - components/ui/button.tsx, components/ui/card.tsx, components/ui/badge.tsx, components/ui/input.tsx, components/ui/label.tsx, components/ui/textarea.tsx, components/ui/select.tsx, components/ui/sheet.tsx, components/ui/separator.tsx, components/ui/sonner.tsx all exist
    - package.json dependencies include react-hook-form, @hookform/resolvers, zod
    - npm run build exits 0
  </acceptance_criteria>
  <done>shadcn/ui initialized, all Phase 2 UI components installed, project color tokens merged into globals.css, form libraries installed</done>
</task>

<task type="auto">
  <name>Task 2: Wire Geist font in locale layout and add Toaster; create (public) route group layout</name>
  <files>app/[locale]/layout.tsx, app/[locale]/(public)/layout.tsx</files>
  <read_first>
    - app/[locale]/layout.tsx — read the current file in full before editing; you will modify this file, not replace it from scratch
    - app/globals.css — confirm @theme inline still has --font-sans: var(--font-geist-sans) after shadcn merge (Task 1 must be complete)
    - .planning/phases/02-public-website/02-RESEARCH.md — Critical Pre-Execution Finding section for exact Geist font wiring pattern; Pattern 4 for (public) layout structure; Pattern 5 for Toaster placement
    - .planning/phases/02-public-website/02-UI-SPEC.md — Header and Footer specifications (you are creating shell/placeholder versions here; full implementation is in Plan 02)
  </read_first>
  <action>
Modify app/[locale]/layout.tsx to:
1. Import Geist font from next/font/google: import { Geist } from 'next/font/google'
2. Instantiate: const geist = Geist({ subsets: ['latin'], variable: '--font-geist-sans' })
3. Import Toaster from @/components/ui/sonner
4. Apply geist.variable to the html element className: className={geist.variable}
5. Place the Toaster component inside NextIntlClientProvider, after {children}

The html element must have both lang={locale} and className={geist.variable}. The body element should not set font-family inline (globals.css handles it via the CSS variable now). Keep all existing logic (locale validation, notFound(), getMessages()).

Create app/[locale]/(public)/layout.tsx as a new file. This layout wraps all public pages (/, /about, /departments, /doctors, /services, /contact, /appointment) with Header and Footer. At this stage, create shell components for Header and Footer:

Create components/layout/Header.tsx as a placeholder that renders a nav element with the text "Atmaram Child Care" and a data-testid="header" attribute. It must be a Server Component (no 'use client') for now — Plan 02 builds it out fully.

Create components/layout/Footer.tsx as a placeholder that renders a footer element with data-testid="footer".

The (public) layout should call setRequestLocale(locale) from next-intl/server and export generateStaticParams returning routing.locales.map((locale) => ({ locale })). It receives params: Promise<{ locale: string }> and awaits it.

Structure of (public) layout:
- import Header from '@/components/layout/Header'
- import Footer from '@/components/layout/Footer'
- Render: Header + main wrapping children + Footer
- Call setRequestLocale(locale) before rendering
- Export generateStaticParams
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && echo "TYPE_CHECK_OK"</automated>
  </verify>
  <acceptance_criteria>
    - app/[locale]/layout.tsx imports Geist from 'next/font/google' and applies geist.variable to html className
    - app/[locale]/layout.tsx has import { Toaster } from '@/components/ui/sonner' and renders <Toaster /> inside NextIntlClientProvider
    - app/[locale]/(public)/layout.tsx exists and imports Header and Footer from components/layout/
    - components/layout/Header.tsx exists with data-testid="header"
    - components/layout/Footer.tsx exists with data-testid="footer"
    - app/[locale]/(public)/layout.tsx calls setRequestLocale(locale) and exports generateStaticParams
    - npx tsc --noEmit exits 0 (no TypeScript errors)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Geist font wired (--font-geist-sans resolves), Toaster mounted in locale layout, (public) layout exists with Header/Footer placeholders, TypeScript clean</done>
</task>

<task type="auto">
  <name>Task 3: Populate translation namespaces in messages/en.json and messages/hi.json</name>
  <files>messages/en.json, messages/hi.json</files>
  <read_first>
    - messages/en.json — read current file; it has only the "site" namespace; you are expanding it, not replacing it
    - messages/hi.json — read current file; same structure as en.json currently
    - .planning/phases/02-public-website/02-UI-SPEC.md — Copywriting Contract table for all exact English copy strings
    - .planning/phases/02-public-website/02-RESEARCH.md — Pitfall 5 (translation keys must exist) and Open Question 2 (namespace structure recommendation: one per page + "nav" + "common")
    - .planning/phases/02-public-website/02-CONTEXT.md — Specific Ideas section for copy strings
  </read_first>
  <action>
Replace the contents of messages/en.json with the complete Phase 2 translation structure. All namespaces and all keys that any page in Phase 2 will use must exist before pages are built. Use the following namespace structure (per Open Question 2 recommendation):

Top-level namespaces: "site" (keep existing), "nav", "common", "home", "about", "departments", "doctors", "services", "contact", "appointment"

"nav" namespace — keys: home ("Home"), about ("About Us"), departments ("Departments"), doctors ("Our Doctors"), services ("Services"), contact ("Contact"), cta ("Book Appointment")

"common" namespace — keys: bookAppointment ("Book Appointment"), requestAppointment ("Request an Appointment"), ayushmanBadge ("Ayushman Bharat PM-JAY Empanelled"), learnMoreServices ("Learn about our services"), contactUs ("Contact us"), meetAllDoctors ("Meet All Doctors")

"home" namespace — keys: hero.headline ("Quality Care for Every Child and Family"), hero.tagline ("Kanpur's trusted super-specialty hospital"), hero.cta ("Request an Appointment"), hero.secondaryLink ("Learn about our services →"), stats.beds ("90 Beds"), stats.bedsLabel ("Beds"), stats.doctors ("25+"), stats.doctorsLabel ("Doctors"), stats.specialties ("8"), stats.specialtiesLabel ("Specialties"), stats.emergency ("24x7"), stats.emergencyLabel ("Emergency"), sections.departments ("Our Departments"), sections.doctors ("Our Doctors"), sections.appointmentCta ("Ready to visit us?"), sections.appointmentCtaDesc ("Our team is here to help. Request an appointment today.")

"about" namespace — keys: pageTitle ("About Us"), intro.heading ("Caring for Kanpur Since Day One"), intro.body ("Atmaram Child Care and Critical Care is a 90-bed super-specialty hospital in Kanpur, Uttar Pradesh. Empanelled under Ayushman Bharat PM-JAY, we provide accessible, high-quality care to every patient who walks through our doors."), stats.beds ("90"), stats.bedsLabel ("Beds"), stats.doctors ("25+"), stats.doctorsLabel ("Doctors"), stats.specialties ("8"), stats.specialtiesLabel ("Specialties"), stats.years ("10+"), stats.yearsLabel ("Years of Service"), mission.heading ("Our Mission"), mission.body ("To provide compassionate, evidence-based medical care to children and families in Kanpur and the surrounding region, with a special commitment to serving patients covered under Ayushman Bharat PM-JAY."), values.care ("Patient-First Care"), values.careDesc ("Every decision centers on patient outcomes and family wellbeing."), values.safety ("Clinical Safety"), values.safetyDesc ("Rigorous protocols and trained staff at every level of care."), values.community ("Community Trust"), values.communityDesc ("Serving Kanpur families with transparency and compassion.")

"departments" namespace — keys: pageTitle ("Our Departments"), pageSubtitle ("Expert care across 8 medical specialties"), and for each department: paediatrics.name, paediatrics.description, paediatricSurgery.name, paediatricSurgery.description, criticalCare.name, criticalCare.description, generalSurgery.name, generalSurgery.description, orthopaedics.name, orthopaedics.description, obstetrics.name, obstetrics.description, generalMedicine.name, generalMedicine.description, emergency.name, emergency.description (use the descriptions from lib/data/departments.ts — see Plan 02 for data values)

"doctors" namespace — keys: pageTitle ("Our Doctors"), pageSubtitle ("Our team of specialists is here to help."), cta ("Have questions? Contact us →"), bookAppointment ("Book Appointment"), comingSoon ("Doctor profiles coming soon.")

"services" namespace — keys: pageTitle ("Services & Facilities"), pageSubtitle ("Comprehensive care for every patient"), ayushman.heading ("Ayushman Bharat PM-JAY"), ayushman.body ("Atmaram Child Care and Critical Care is empanelled under the Ayushman Bharat Pradhan Mantri Jan Arogya Yojana (PM-JAY) scheme. Eligible patients receive cashless treatment for covered procedures."), ayushman.whatIs ("What is PM-JAY?"), ayushman.whatIsBody ("PM-JAY is India's flagship public health insurance scheme providing coverage of up to ₹5 lakh per family per year for secondary and tertiary hospitalisation."), facilities.heading ("Our Facilities")

"contact" namespace — keys: pageTitle ("Contact Us"), address.heading ("Address"), address.value ("Naubasta / Kidwai Nagar, Kanpur - 208021, Uttar Pradesh"), phone.heading ("Phone"), phone.value ("+91-XXXXXXXXXX (placeholder)"), opd.heading ("OPD Timings"), opd.value ("Mon–Sat: 9:00 AM – 5:00 PM"), emergency.heading ("24x7 Emergency"), emergency.value ("+91-XXXXXXXXXX"), map.placeholder ("Map loading...")

"appointment" namespace — keys: pageTitle ("Request an Appointment"), pageSubtitle ("Fill in the form below and our team will call you to confirm."), fields.patientName ("Patient Name"), fields.phone ("Phone Number"), fields.preferredDoctor ("Preferred Doctor"), fields.preferredDoctorPlaceholder ("Select a doctor"), fields.noPreference ("No preference"), fields.preferredDate ("Preferred Date"), fields.reason ("Reason / Chief Complaint"), fields.reasonPlaceholder ("Briefly describe your symptoms or reason for visit"), submit ("Submit Request"), success.title ("Request Received"), success.description ("Your appointment request has been received. Our team will call you shortly."), errors.required ("This field is required."), errors.phone ("Enter a valid 10-digit Indian mobile number."), errors.pastDate ("Please select a date from today onwards.")

For messages/hi.json: mirror the exact same structure and keys as messages/en.json but use the same English values for now. Phase 3 will replace values with Hindi. Keep the existing "site" namespace Hindi values (आत्माराम...) intact — only add the new namespaces with English placeholder values.
  </action>
  <verify>
    <automated>node -e "const en=JSON.parse(require('fs').readFileSync('messages/en.json','utf8')); const hi=JSON.parse(require('fs').readFileSync('messages/hi.json','utf8')); const required=['nav','common','home','about','departments','doctors','services','contact','appointment']; const missing=required.filter(k=>!en[k]); if(missing.length>0){console.error('Missing namespaces in en.json:',missing);process.exit(1);} const hiMissing=required.filter(k=>!hi[k]); if(hiMissing.length>0){console.error('Missing namespaces in hi.json:',hiMissing);process.exit(1);} console.log('OK - all namespaces present');"</automated>
  </verify>
  <acceptance_criteria>
    - messages/en.json contains all 9 namespaces: nav, common, home, about, departments, doctors, services, contact, appointment (plus the existing "site" namespace)
    - messages/hi.json mirrors the same top-level namespace keys as messages/en.json
    - nav.cta value is "Book Appointment" (per UI-SPEC Copywriting Contract)
    - appointment.success.description value is "Your appointment request has been received. Our team will call you shortly." (per D-06)
    - common.ayushmanBadge value is "Ayushman Bharat PM-JAY Empanelled" (per UI-SPEC — must be exact)
    - npm run build exits 0
  </acceptance_criteria>
  <done>All Phase 2 translation namespaces exist in both en.json and hi.json; next-intl will not throw on any t('key') call in Phase 2 page components</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → filesystem | npx shadcn@latest init writes to globals.css and creates files — review output before continuing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npx shadcn@latest init (auto-installed transitive deps) | accept | All 5 new packages rated [OK] by slopcheck v0.6.1 on 2026-06-10; transitive deps (clsx, tailwind-merge, class-variance-authority) have 100M+ weekly downloads and 5+ year history — low risk |
| T-02-01 | Tampering | Appointment form (client-side only in Phase 2) | accept | No data stored or transmitted in Phase 2; form data never leaves the browser; server-side validation and rate limiting are Phase 7 scope |
| T-02-02 | Tampering | XSS via seeded content strings | accept | React escapes JSX values by default; all strings are hardcoded TypeScript constants — no user-supplied content |
| T-02-03 | Spoofing | next-intl locale redirects | accept | Locale routing handled entirely by next-intl middleware (proxy.ts); no custom redirect logic added in this phase |
</threat_model>

<verification>
After all 3 tasks complete:
1. npm run build exits 0
2. components.json exists at project root with "config": ""
3. app/globals.css contains --color-primary: #1E40AF
4. app/[locale]/layout.tsx contains geist.variable applied to html className
5. app/[locale]/(public)/layout.tsx exists and imports Header + Footer
6. messages/en.json has all 9 Phase 2 namespaces (nav, common, home, about, departments, doctors, services, contact, appointment)
</verification>

<success_criteria>
- shadcn/ui initialized and all 10 UI components installed (button, card, badge, input, label, textarea, select, sheet, separator, sonner)
- react-hook-form, @hookform/resolvers, zod installed
- Geist font wired via next/font/google — --font-geist-sans CSS variable resolves
- (public) route group layout exists and wraps children with Header/Footer shell
- All Phase 2 translation namespaces populated in both locale files
- npm run build exits 0 after all tasks
</success_criteria>

<output>
Create .planning/phases/02-public-website/02-01-SUMMARY.md when done
</output>

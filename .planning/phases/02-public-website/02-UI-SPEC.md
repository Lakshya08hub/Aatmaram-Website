---
phase: 2
slug: public-website
status: approved
shadcn_initialized: false
preset: none
created: 2026-06-10
reviewed_at: 2026-06-10
---

# Phase 2 — UI Design Contract: Public Website

> Visual and interaction contract for the Atmaram Child Care and Critical Care public website.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker before execution begins.

---

## Pre-Execution Requirement: shadcn/ui Initialization

shadcn/ui is NOT yet installed (`components.json` absent, no shadcn packages in `package.json`).
Decision D-03 locked shadcn/ui on Tailwind v4. Before any component work in Phase 2:

```bash
npx shadcn@latest init
```

Select: TypeScript, App Router, `src/` directory: No, import alias `@/*`, CSS variables: Yes.
Components directory: `components/ui/`. After init, confirm `components.json` exists.

Then install required components:
```bash
npx shadcn@latest add button card badge input label textarea select sheet toast
```

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui | D-03 (locked) |
| Preset | CLI init — no preset URL | Default — shadcn CLI interactive |
| Component library | Radix UI (via shadcn) | shadcn default |
| Icon library | lucide-react (bundled with shadcn) | shadcn default |
| Font | Geist Sans (already wired via `--font-geist-sans` in globals.css) | Phase 1 output |
| Font fallback | Arial, Helvetica, sans-serif | Existing globals.css |

---

## Spacing Scale

Standard 8-point scale. All values are multiples of 4.

| Token | Value | Tailwind Class | Usage |
|-------|-------|----------------|-------|
| xs | 4px | `p-1` / `gap-1` | Icon gaps, inline badge padding |
| sm | 8px | `p-2` / `gap-2` | Compact button padding, list item gaps |
| md | 16px | `p-4` / `gap-4` | Default card padding, form field spacing |
| lg | 24px | `p-6` / `gap-6` | Section inner padding, card body |
| xl | 32px | `p-8` / `gap-8` | Section gaps on desktop |
| 2xl | 48px | `py-12` / `gap-12` | Major section breaks (hero bottom, section dividers) |
| 3xl | 64px | `py-16` | Page-level top/bottom padding for hero and footer |

Exceptions:
- Mobile touch targets: minimum 44px height for all interactive elements (buttons, nav links, form inputs). Use `min-h-[44px]` where native Tailwind classes fall short.
- Header height: 64px (`h-16`) on all breakpoints for consistent nav clearance.

---

## Typography

Font: Geist Sans via CSS variable `--font-geist-sans`. Applied at `body` level in globals.css.

| Role | Size | Tailwind | Weight | Tailwind | Line Height | Usage |
|------|------|----------|--------|----------|-------------|-------|
| Body | 16px | `text-base` | 400 Regular | `font-normal` | 1.5 | Paragraph text, card descriptions, form labels |
| Label / Small | 14px | `text-sm` | 600 Semibold | `font-semibold` | 1.4 | Form labels, badge text, nav links, captions |
| Heading | 20px | `text-xl` | 600 Semibold | `font-semibold` | 1.3 | Card headings, section titles, page-level section headings, department names |
| Display | 36px mobile / 48px desktop | `text-4xl` / `text-5xl` | 700 Bold | `font-bold` | 1.2 | Hero headline only |

Rules:
- Exactly 2 declared global weights: Regular (400) `font-normal` for body text and captions; Semibold (600) `font-semibold` for labels, headings, CTAs, and nav items.
- Bold (700) `font-bold` is a hero-only exception — used exclusively on the hero headline (`text-4xl`/`text-5xl`). It is NOT a separate type scale entry.
- Hindi text in Phase 3 will use the same scale; Geist Sans supports Devanagari via system fallback — acceptable for v1.
- No italic, no underline on non-link text.

---

## Color

| Role | Hex | CSS Variable | Tailwind | Usage |
|------|-----|--------------|----------|-------|
| Dominant (60%) | `#F8FAFC` | `--color-bg` | `bg-slate-50` | Page background, section backgrounds |
| Surface (part of dominant) | `#FFFFFF` | `--color-surface` | `bg-white` | Card backgrounds, header, modal surfaces |
| Secondary (30%) | `#1E3A5F` | `--color-nav` | custom or `bg-blue-900` | Header/nav background, footer background |
| Primary Interactive | `#1E40AF` | `--color-primary` | `bg-blue-800` | Primary buttons, links, active nav state |
| Primary Hover | `#1D3FAD` | — | `hover:bg-blue-900` | Button hover state |
| Accent (10%) | `#16A34A` | `--color-accent` | `bg-green-600` | PM-JAY badge, "Book Appointment" CTA button ONLY |
| Accent Hover | `#15803D` | — | `hover:bg-green-700` | PM-JAY badge hover, CTA hover |
| Text Primary | `#0F172A` | `--color-text` | `text-slate-900` | All body text, headings |
| Text Muted | `#64748B` | `--color-muted` | `text-slate-500` | Secondary descriptions, placeholder text, captions |
| Border | `#E2E8F0` | `--color-border` | `border-slate-200` | Card borders, dividers, input borders |
| Destructive | `#DC2626` | — | `bg-red-600` | Form validation error messages only |
| Success | `#16A34A` | — | `text-green-600` | Toast success message icon |

Accent (`#16A34A`) reserved for:
1. PM-JAY / Ayushman Bharat badge background
2. "Book Appointment" primary CTA button in header
3. "Request Appointment" CTA button on Home hero
4. Success toast icon

Primary blue (`#1E40AF`) reserved for:
1. Secondary buttons (outlined, ghost)
2. Text links
3. Active nav underline
4. Department card icon backgrounds

No accent on decorative elements, dividers, or backgrounds.

CSS custom properties to add to `globals.css` `@theme inline` block:

```css
@theme inline {
  --color-bg: #F8FAFC;
  --color-surface: #FFFFFF;
  --color-primary: #1E40AF;
  --color-accent: #16A34A;
  --color-nav: #1E3A5F;
  --color-text: #0F172A;
  --color-muted: #64748B;
  --color-border: #E2E8F0;
}
```

---

## Copywriting Contract

| Element | Copy | Notes |
|---------|------|-------|
| Primary CTA (hero) | "Request an Appointment" | Green accent button — home hero only |
| Header CTA | "Book Appointment" | Green accent — header, all pages |
| Empty state — Doctors | "Doctor profiles coming soon." | Phase 5 replaces with real data |
| Empty state — Departments | "Department information coming soon." | Phase 5 replaces |
| Form success toast | "Your appointment request has been received. Our team will call you shortly." | D-06 locked |
| Form error — required field | "This field is required." | Inline below field |
| Form error — invalid phone | "Enter a valid 10-digit Indian mobile number." | Inline below phone field |
| Form error — past date | "Please select a date from today onwards." | Inline below date picker |
| Form submit button | "Submit Request" | Appointment form page |
| Nav — Home | "Home" | — |
| Nav — About | "About Us" | — |
| Nav — Departments | "Departments" | — |
| Nav — Doctors | "Our Doctors" | — |
| Nav — Services | "Services" | — |
| Nav — Contact | "Contact" | — |
| Nav — CTA | "Book Appointment" | Accent button |
| Hero tagline | "Kanpur's trusted super-specialty hospital" | D-04 locked |
| PM-JAY badge label | "Ayushman Bharat PM-JAY Empanelled" | Must be exact text — government accreditation |
| Footer copyright | "© 2025 Atmaram Child Care and Critical Care. All rights reserved." | Update year at go-live |
| Contact — OPD label | "OPD Timings" | — |
| Contact — Emergency label | "24x7 Emergency" | — |
| About — stat 1 | "90 Beds" | D-04 locked |
| About — stat 2 | "25+ Doctors" | D-04 locked |
| About — stat 3 | "8 Specialties" | D-04 locked |
| 404 page heading | "Page not found" | Next.js default sufficient |

Destructive actions in Phase 2: None. No delete or destructive operations on the public website.

---

## Registry Safety

| Registry | Components Used | Safety Gate |
|----------|----------------|-------------|
| shadcn official (`ui.shadcn.com`) | Button, Card, CardHeader, CardContent, CardFooter, Badge, Input, Label, Textarea, Select, SelectTrigger, SelectContent, SelectItem, Sheet, SheetTrigger, SheetContent, SheetClose, Toaster, useToast | Not required — official registry |

No third-party registries. No vetting gate required.

---

## Component Specifications

### Header

- Height: 64px (`h-16`)
- Background: `#1E3A5F` (`bg-blue-900` or custom CSS variable)
- Logo area: Left — hospital name as text ("Atmaram Child Care") in white, `text-lg font-semibold`. Logo image placeholder slot for Phase 5.
- Nav links: Center/right — `text-sm font-semibold text-white/80 hover:text-white` — horizontal on `md+`, hidden on mobile
- CTA button: "Book Appointment" — shadcn `Button` variant `default` with `bg-green-600 hover:bg-green-700 text-white text-sm font-semibold`
- Language toggle: Placeholder slot — `text-sm text-white/60 cursor-not-allowed` with "(EN/HI)" text. Disabled in Phase 2; functional in Phase 3.
- Mobile: shadcn `Sheet` — hamburger icon (lucide `Menu`) triggers slide-in drawer from right. Hamburger trigger: shadcn `Button variant="ghost"` with `text-white` and `aria-label="Open navigation menu"`. Sheet contains full nav links stacked vertically + CTA button. Sheet background white.
- Sticky: `sticky top-0 z-50`
- Shadow: `shadow-sm`

### Footer

- Background: `#1E3A5F` (same as header)
- Text color: `text-white/70`, headings `text-white`
- 3-column layout on `md+`, stacked on mobile
- Column 1: Hospital name + tagline + PM-JAY badge (small version)
- Column 2: Quick links (Home, About, Departments, Doctors, Services, Contact)
- Column 3: Contact info (address, phone placeholder, OPD hours)
- Bottom bar: Copyright line, `border-t border-white/10 mt-8 pt-4 text-sm text-white/50`
- No decorative elements, no gradients

### PM-JAY Badge (Primary — Hero placement)

- Component: shadcn `Badge` customized, or `div` styled independently
- Shape: Rounded pill, `rounded-full`
- Background: `#16A34A` (accent green)
- Text: "Ayushman Bharat PM-JAY Empanelled" — `text-sm font-semibold text-white`
- Icon: Shield or checkmark (lucide `ShieldCheck`) — `16px`, left of text, `text-white`
- Padding: `px-4 py-2`
- Placement on Home: In hero section, below tagline, above CTA button — standalone full-width-on-mobile pill
- Placement on Footer: Small version — `text-xs`, icon `12px`, inside Column 1

### PM-JAY Badge (Secondary — Footer/Services)

- Same component, `scale-90` or reduced padding: `px-3 py-1 text-xs`

### Hero Section (Home page only)

- Full-width, no image background — clean white/near-white (`bg-white`)
- Content centered, max-width `max-w-4xl mx-auto`
- Padding: `py-16 px-4` mobile, `py-24 px-8` desktop
- Headline: `text-4xl md:text-5xl font-bold text-slate-900` — "Quality Care for Every Child and Family"
- Tagline: `text-lg text-slate-500 mt-4` — "Kanpur's trusted super-specialty hospital"
- PM-JAY Badge: `mt-6`
- Stats row: `mt-8` — 3 stats inline (90 Beds, 25+ Doctors, 8 Specialties) — `text-2xl font-bold text-blue-800` + `text-sm text-slate-500` label
- CTA button: `mt-8` — "Request an Appointment" — green accent, shadcn `Button`, `size="lg"`
- Secondary link: "Learn about our services →" — `text-blue-800 underline text-sm mt-4`
- No background image, no gradient, no decorative shapes

Note: Stats row uses `text-2xl font-bold` as a data-display one-off (numeric emphasis), not a type scale role. The hero headline `font-bold` is the documented hero-only exception.

### Department Card

- Component: shadcn `Card` — `CardHeader` + `CardContent`
- Background: `bg-white`
- Border: `border border-slate-200`
- Border radius: `rounded-xl`
- Shadow: `shadow-sm hover:shadow-md transition-shadow`
- Icon area: `48px` circle, `bg-blue-50`, department icon (lucide icon) in `text-blue-800 w-6 h-6`, centered
- Department name: `text-xl font-semibold text-slate-900 mt-4`
- Description: `text-sm text-slate-500 mt-2 leading-relaxed` — 1-2 sentence placeholder
- No CTA inside card in Phase 2 (Phase 5 adds "View Details")
- Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6`

Department icons (lucide):
- Paediatrics & Neonatology: `Baby`
- Paediatric Surgery: `Scissors`
- Critical Care & ICU: `Activity`
- General Surgery: `Stethoscope`
- Orthopaedics: `Bone`
- Obstetrics & Gynaecology: `Heart`
- General Medicine: `Pill`
- Emergency & Trauma: `Ambulance` (or `AlertCircle`)

### Doctor Card

- Component: shadcn `Card`
- Background: `bg-white`
- Border: `border border-slate-200`
- Border radius: `rounded-xl`
- Shadow: `shadow-sm`
- Avatar: `80px` circle — solid color `bg-blue-100` with initials (e.g., "DR") in `text-blue-800 font-semibold text-xl`. No external image service.
- Doctor name: `text-xl font-semibold text-slate-900 mt-4`
- Specialty: `text-sm text-slate-500`
- Separator: `border-t border-slate-100 mt-4`
- CTA inside card: "Book Appointment" — shadcn `Button` `variant="outline"` `size="sm"` — links to `/[locale]/appointment`
- Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6`

Seeded doctor data (6 cards):
1. Dr. Rajesh Sharma — Paediatrics & Neonatology
2. Dr. Priya Singh — Paediatric Surgery
3. Dr. Anil Kumar — Critical Care & ICU
4. Dr. Sunita Verma — Obstetrics & Gynaecology
5. Dr. Vikram Gupta — Orthopaedics
6. Dr. Meena Agarwal — General Medicine

### Trust Signals Band (Home page — below hero)

- Background: `bg-blue-800` (primary blue)
- Text: `text-white`
- Full-width, `py-8`
- 4 items in a row (`grid grid-cols-2 md:grid-cols-4`):
  1. "90 Beds" — lucide `BedDouble` icon
  2. "25+ Doctors" — lucide `Users` icon
  3. "8 Specialties" — lucide `Stethoscope` icon
  4. "24x7 Emergency" — lucide `Clock` icon
- Each item: icon `24px` + `text-xl font-semibold` number + `text-sm text-white/70` label, stacked, centered

### Section Headings (reusable pattern)

```
<h2 class="text-xl font-semibold text-slate-900">{Section Title}</h2>
<p class="text-slate-500 mt-2 text-base">{Section subtitle or description}</p>
<div class="mt-2 w-12 h-1 bg-blue-800 rounded-full"></div>
```

- Uses Heading role (`text-xl font-semibold`) — the Heading type role is used for all page-level section titles.
- Used on: Departments section preview (Home), full Departments page, Doctors page, About stats, Services page
- Underline accent: 48px wide, 4px tall, `bg-blue-800`, `rounded-full` — minimal decorative element (not a gradient)

### Button Variants

All buttons use shadcn `Button` component.

| Variant | Usage | Classes |
|---------|-------|---------|
| Primary Accent | "Book Appointment" CTA | `bg-green-600 hover:bg-green-700 text-white font-semibold` |
| Primary Blue | Secondary primary actions | shadcn `default` — customize to `bg-blue-800 hover:bg-blue-900 text-white` |
| Outline | Card CTAs, secondary actions | shadcn `outline` — `border-blue-800 text-blue-800 hover:bg-blue-50` |
| Ghost | Nav links on mobile sheet | shadcn `ghost` |
| Destructive | Not used in Phase 2 | shadcn `destructive` — available for Phase 7+ |

Sizes: `size="lg"` for hero CTA, `size="default"` for header CTA, `size="sm"` for card CTAs.
Minimum touch target: all buttons `min-h-[44px]`.

### Form Fields (Appointment Form)

All fields use shadcn components: `Input`, `Label`, `Textarea`, `Select`.

- Field wrapper: `div` with `flex flex-col gap-2` — label above input
- Label: shadcn `Label` — `text-sm font-semibold text-slate-700`
- Input: shadcn `Input` — `border-slate-200 focus:border-blue-800 focus:ring-blue-800`
- Error state: Red border `border-red-500`, error message `text-xs text-red-600 mt-1`
- Required marker: `*` in `text-red-500` after label text

Fields:

| Field | Component | Type | Validation |
|-------|-----------|------|------------|
| Patient name | `Input` | text | Required, min 2 chars |
| Phone number | `Input` | tel | Required, 10-digit Indian mobile regex: `/^[6-9]\d{9}$/` |
| Preferred doctor | `Select` | select | Required — populated from seeded list (6 doctors + "No preference") |
| Preferred date | `Input` | date | Required, min today's date (`new Date().toISOString().split('T')[0]`) |
| Reason / chief complaint | `Textarea` | textarea | Required, min 10 chars, rows=4 |

Submit button: "Submit Request" — full-width on mobile, `size="lg"`, primary blue `bg-blue-800`.

Form component must be `'use client'` — uses `useState` for field values, validation errors, and submission state.

### Toast (Success)

- Component: shadcn `Toaster` + `useToast` hook
- Placement: bottom-right on desktop, bottom-center on mobile
- Duration: 6000ms
- Title: "Request Received"
- Description: "Your appointment request has been received. Our team will call you shortly."
- Variant: `default` with green success icon override (lucide `CheckCircle2` in `text-green-600`)

### Map Placeholder (Contact page)

- No Google Maps API key in Phase 2
- Replace with: `div` `h-64 bg-slate-100 rounded-xl flex items-center justify-center`
- Content: lucide `MapPin` icon `text-slate-400 w-8 h-8` + `text-sm text-slate-400 mt-2` "Map loading..."
- Phase 5 or Phase 2.x will embed real map

---

## Page-by-Page Layout Specifications

### 1. Home (`/[locale]/`)

Sections (top to bottom):
1. `<Header />` — sticky
2. Hero — full-width, `py-24`, headline + tagline + PM-JAY badge + stats row + CTA
3. Trust Signals Band — blue `bg-blue-800`, 4 stats, `py-8`
4. Departments Preview — `py-16` section, heading + `grid grid-cols-2 md:grid-cols-4 gap-6` (8 department cards, abbreviated)
5. Doctors Preview — `py-16` section, heading + `grid grid-cols-1 sm:grid-cols-3 gap-6` (3 doctor cards), "Meet All Doctors →" link
6. Appointment CTA Band — `bg-blue-50 py-16`, centered text + "Request an Appointment" green button
7. `<Footer />`

### 2. About (`/[locale]/about`)

Sections:
1. `<Header />`
2. Page header — `py-12 bg-white`, page title "About Us" (`text-xl font-semibold`), breadcrumb `Home > About Us`
3. Hospital Story — `py-12`, two-column `md:grid-cols-2` — text left (founding story placeholder, mission), stat cards right (90 beds, 25+ doctors, 8 specialties, years in service)
4. Mission & Values — `py-12 bg-slate-50`, 3 cards with lucide icons (HeartPulse, Shield, Users)
5. `<Footer />`

### 3. Departments (`/[locale]/departments`)

Sections:
1. `<Header />`
2. Page header — `py-12`, title "Our Departments" (`text-xl font-semibold`), subtitle
3. Departments Grid — `py-12`, `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6`, all 8 department cards
4. `<Footer />`

### 4. Doctors (`/[locale]/doctors`)

Sections:
1. `<Header />`
2. Page header — `py-12`, title "Our Doctors" (`text-xl font-semibold`), subtitle "Our team of specialists is here to help."
3. Doctors Grid — `py-12`, `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6`, 6 doctor cards
4. CTA row — centered "Have questions? Contact us →" link
5. `<Footer />`

### 5. Services (`/[locale]/services`)

Sections:
1. `<Header />`
2. Page header — `py-12`, title "Services & Facilities" (`text-xl font-semibold`)
3. Services List — `py-12`, two columns `md:grid-cols-2` — services aligned with 8 departments, each as a list item with lucide `CheckCircle2` icon in `text-green-600`
4. Ayushman Bharat Section — `py-12 bg-blue-50`, prominent PM-JAY badge (large), description of empanelment, "What is PM-JAY?" short paragraph
5. Facilities — `py-12`, `grid grid-cols-2 md:grid-cols-3 gap-4` — facility cards (ICU, NICU, OT, Emergency, Laboratory, Pharmacy placeholder)
6. `<Footer />`

### 6. Contact (`/[locale]/contact`)

Sections:
1. `<Header />`
2. Page header — `py-12`, title "Contact Us" (`text-xl font-semibold`)
3. Contact Details + Map — `py-12`, two-column `md:grid-cols-2` gap-12:
   - Left: Address card (shadcn `Card`), Phone card, OPD Timings card, Emergency card
   - Right: Map placeholder `div`
4. `<Footer />`

Contact card details:
- Address: "Naubasta / Kidwai Nagar, Kanpur - 208021, Uttar Pradesh"
- Phone: "+91-XXXXXXXXXX (placeholder)"
- OPD Timings: "Mon–Sat: 9:00 AM – 5:00 PM"
- Emergency: "24x7 Emergency: +91-XXXXXXXXXX"

### 7. Appointment Request (`/[locale]/appointment`)

Sections:
1. `<Header />`
2. Page header — `py-12`, title "Request an Appointment" (`text-xl font-semibold`), subtitle "Fill in the form below and our team will call you to confirm."
3. Form — `py-12`, centered `max-w-lg mx-auto`, shadcn `Card` wrapping form fields
4. `<Footer />`

Form card: `shadow-sm border border-slate-200 rounded-xl p-8`

---

## Responsive Behavior

| Breakpoint | Tailwind | Behavior |
|------------|----------|----------|
| Mobile | default (`< 640px`) | Single column layouts, hamburger nav, full-width cards, stacked form |
| Tablet | `sm` (`640px+`) | 2-column grids begin (department cards, doctor cards) |
| Desktop | `md` (`768px+`) | 3–4 column grids, header nav visible, two-column page sections |
| Wide | `lg` (`1024px+`) | 4-column department grid, 3-column doctor grid, max-width content container |

Max content width: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8` — applied to all page sections.

Mobile navigation:
- Header nav links: `hidden md:flex` on header
- Hamburger trigger: `md:hidden` — lucide `Menu` icon, `Button variant="ghost"` with `text-white` and `aria-label="Open navigation menu"`
- Sheet: `SheetContent side="right" className="w-72"` — white background, stacked nav links as `Button variant="ghost"` full-width, CTA at bottom

Mobile card behavior:
- Department cards: 1 col on mobile, 2 on sm, 4 on lg
- Doctor cards: 1 col on mobile, 2 on sm, 3 on lg
- Trust signals: 2 col on mobile, 4 on md

---

## shadcn/ui Component Mapping

| shadcn Component | Installation Command | Used For |
|-----------------|---------------------|----------|
| Button | `npx shadcn@latest add button` | All CTAs, nav links, form submit |
| Card, CardHeader, CardContent, CardFooter | `npx shadcn@latest add card` | Department cards, Doctor cards, Contact cards, Form wrapper |
| Badge | `npx shadcn@latest add badge` | PM-JAY badge |
| Input | `npx shadcn@latest add input` | Appointment form text/tel/date fields |
| Label | `npx shadcn@latest add label` | All form labels |
| Textarea | `npx shadcn@latest add textarea` | Appointment form reason field |
| Select, SelectTrigger, SelectContent, SelectItem | `npx shadcn@latest add select` | Preferred doctor dropdown |
| Sheet, SheetTrigger, SheetContent, SheetClose | `npx shadcn@latest add sheet` | Mobile navigation drawer |
| Toaster, useToast | `npx shadcn@latest add sonner` (or toast) | Appointment form success message |
| Separator | `npx shadcn@latest add separator` | Doctor card divider |

Note: shadcn `sonner` (Sonner toast) is preferred over legacy `toast` for Next.js App Router compatibility. Install with `npx shadcn@latest add sonner`.

---

## What This Phase Does NOT Include

These are explicitly out of scope for Phase 2 — do not implement:

- Hindi translation strings (Phase 3 scope) — use English only; structure translation keys so Phase 3 can fill values without restructuring
- Functional language toggle (Phase 3) — render placeholder slot only
- Supabase reads for content (Phase 5) — all content is hardcoded
- Supabase write for appointment form (Phase 7) — client-side validation + toast only
- Real doctor photos, hospital logo, or photography (client content dependency)
- Google Maps embed (requires API key — not yet configured)
- Gemini chat widget (Phase 11)
- Dark mode (not in scope for v1)

---

## Accessibility Notes

- All interactive elements must have visible focus rings (shadcn provides these by default via Radix)
- Form inputs must have associated `<Label>` via `htmlFor` / `id` pairing
- Images: doctor avatar divs need `aria-label="Doctor avatar for {name}"`
- PM-JAY badge: `role="img" aria-label="Ayushman Bharat PM-JAY Empanelled"`
- Mobile hamburger button: `aria-label="Open navigation menu"`
- Mobile sheet: shadcn Sheet handles focus trap automatically
- Color contrast: `#1E40AF` on `#F8FAFC` = 7.2:1 (AAA), `#0F172A` on `#F8FAFC` = 18.9:1 (AAA), `#FFFFFF` on `#1E3A5F` = 9.6:1 (AAA), `#FFFFFF` on `#16A34A` = 4.8:1 (AA)

---

## SEO Metadata (per Next.js `metadata` export)

Each page exports a `metadata` object. Format:

```ts
export const metadata: Metadata = {
  title: '{Page Title} | Atmaram Child Care and Critical Care',
  description: '{Page description}',
}
```

| Page | Title | Description |
|------|-------|-------------|
| Home | "Atmaram Child Care and Critical Care — Kanpur" | "A 90-bed super-specialty hospital in Kanpur offering paediatrics, critical care, surgery and more. Ayushman Bharat PM-JAY empanelled." |
| About | "About Us | Atmaram Child Care and Critical Care" | "Learn about Atmaram Child Care and Critical Care — Kanpur's trusted super-specialty hospital with 90 beds and 25+ doctors." |
| Departments | "Departments | Atmaram Child Care and Critical Care" | "Explore 8 specialty departments including Paediatrics, Critical Care, Orthopaedics, and more." |
| Doctors | "Our Doctors | Atmaram Child Care and Critical Care" | "Meet our team of 25+ specialist doctors at Atmaram Child Care and Critical Care, Kanpur." |
| Services | "Services & Facilities | Atmaram Child Care and Critical Care" | "Comprehensive hospital services including ICU, NICU, emergency care, and Ayushman Bharat PM-JAY empanelled treatment." |
| Contact | "Contact Us | Atmaram Child Care and Critical Care" | "Find us at Naubasta, Kanpur-208021. OPD timings, emergency number, and location." |
| Appointment | "Request an Appointment | Atmaram Child Care and Critical Care" | "Request a doctor's appointment at Atmaram Child Care and Critical Care, Kanpur. Our team will call you to confirm." |

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved — 2026-06-10

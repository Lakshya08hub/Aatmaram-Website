---
phase: 02-public-website
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - app/[locale]/(public)/departments/page.tsx
  - app/[locale]/(public)/doctors/page.tsx
autonomous: true
requirements:
  - PUB-03
  - PUB-04

must_haves:
  truths:
    - "Visitor navigating to /hi/departments sees the full 8-department grid with cards"
    - "Visitor navigating to /hi/doctors sees all 6 doctor cards with initials avatars and Book Appointment CTAs"
    - "Both pages statically pre-render for both 'hi' and 'en' locales"
  artifacts:
    - path: "app/[locale]/(public)/departments/page.tsx"
      provides: "Departments page — 8 department cards grid"
      contains: "generateStaticParams"
    - path: "app/[locale]/(public)/doctors/page.tsx"
      provides: "Doctors page — 6 doctor cards grid"
      contains: "generateStaticParams"
  key_links:
    - from: "app/[locale]/(public)/departments/page.tsx"
      to: "lib/data/departments.ts"
      via: "import departments"
      pattern: "import.*departments.*from.*lib/data/departments"
    - from: "app/[locale]/(public)/doctors/page.tsx"
      to: "lib/data/doctors.ts"
      via: "import doctors"
      pattern: "import.*doctors.*from.*lib/data/doctors"
---

<objective>
Build the Departments page (8-card grid) and the Doctors page (6-card grid) as server components.

Purpose: PUB-03 requires all 8 specialty departments to be browsable; PUB-04 requires all 6 doctor cards with name, specialization, and avatar. Both pages are the simplest in the project — they import the shared components and seeded data constants from Plan 02 and render grids.

Output: Departments page rendering all 8 DepartmentCards; Doctors page rendering all 6 DoctorCards with placeholder avatars.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-public-website/02-CONTEXT.md
@.planning/phases/02-public-website/02-UI-SPEC.md
@.planning/phases/02-public-website/02-01-SUMMARY.md
@.planning/phases/02-public-website/02-02-SUMMARY.md

<interfaces>
<!-- Types and components available after Plans 01 and 02 -->

From lib/data/departments.ts:
  export const departments: Department[] — 8 items

From lib/data/doctors.ts:
  export const doctors: Doctor[] — 6 items

From components/public/DepartmentCard.tsx:
  export default DepartmentCard — props: { department: Department }

From components/public/DoctorCard.tsx:
  export default DoctorCard — props: { doctor: Doctor }

From components/public/SectionHeading.tsx:
  export default SectionHeading — props: { title: string; subtitle?: string }

Standard page pattern (must be in every page):
  export function generateStaticParams() {
    return routing.locales.map((locale) => ({ locale }));
  }
  export default async function PageName({ params }: { params: Promise<{ locale: string }> }) {
    const { locale } = await params;
    setRequestLocale(locale);
    // ...
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Departments page</name>
  <files>app/[locale]/(public)/departments/page.tsx</files>
  <read_first>
    - .planning/phases/02-public-website/02-UI-SPEC.md — Departments page layout (section 3 of Page-by-Page Layout Specifications): page header structure, full 8-card grid with className specs
    - messages/en.json — "departments" namespace to confirm available keys (pageTitle, pageSubtitle)
  </read_first>
  <action>
Create app/[locale]/(public)/departments/page.tsx. Create the departments/ directory. Server Component (no 'use client').

Import list:
- import { setRequestLocale, getTranslations } from 'next-intl/server'
- import { routing } from '@/i18n/routing'
- import type { Metadata } from 'next'
- import DepartmentCard from '@/components/public/DepartmentCard'
- import SectionHeading from '@/components/public/SectionHeading'
- import { departments } from '@/lib/data/departments'

Export metadata:
  title: "Departments | Atmaram Child Care and Critical Care"
  description: "Explore 8 specialty departments including Paediatrics, Critical Care, Orthopaedics, and more."

Export generateStaticParams returning routing.locales.map((locale) => ({ locale })).

Page component body:
1. Await params, call setRequestLocale(locale)
2. Await getTranslations('departments')

Section 1 — Page Header:
- section className="py-12 bg-white px-4"
- div className="max-w-7xl mx-auto"
- p className="text-sm text-slate-500 mb-2" — "Home › Departments"
- h1 className="text-xl font-semibold text-slate-900" — "Our Departments"

Section 2 — Departments Grid:
- section className="py-12 px-4"
- div className="max-w-7xl mx-auto"
- SectionHeading with title="Our Departments" subtitle="Expert care across 8 medical specialties"
- div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mt-8"
- Map departments array: departments.map(dept => <DepartmentCard key={dept.id} department={dept} />)

No CTA section on Departments page in Phase 2 (UI-SPEC says "No CTA inside card in Phase 2").
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - app/[locale]/(public)/departments/page.tsx exists
    - File does NOT contain 'use client'
    - File imports and maps all departments from '@/lib/data/departments'
    - File exports generateStaticParams
    - File exports metadata with title containing "Departments"
    - npm run build exits 0
  </acceptance_criteria>
  <done>Departments page renders 8 DepartmentCards in a responsive grid; statically generates for both locales</done>
</task>

<task type="auto">
  <name>Task 2: Build Doctors page</name>
  <files>app/[locale]/(public)/doctors/page.tsx</files>
  <read_first>
    - .planning/phases/02-public-website/02-UI-SPEC.md — Doctors page layout (section 4 of Page-by-Page Layout Specifications): page header, 3-column grid of 6 cards, "Contact us" CTA row
    - messages/en.json — "doctors" namespace keys (pageTitle, pageSubtitle, cta, comingSoon)
  </read_first>
  <action>
Create app/[locale]/(public)/doctors/page.tsx. Create the doctors/ directory. Server Component (no 'use client').

Import list:
- import { setRequestLocale, getTranslations } from 'next-intl/server'
- import { routing } from '@/i18n/routing'
- import type { Metadata } from 'next'
- import Link from 'next/link'
- import DoctorCard from '@/components/public/DoctorCard'
- import SectionHeading from '@/components/public/SectionHeading'
- import { doctors } from '@/lib/data/doctors'

Export metadata:
  title: "Our Doctors | Atmaram Child Care and Critical Care"
  description: "Meet our team of 25+ specialist doctors at Atmaram Child Care and Critical Care, Kanpur."

Export generateStaticParams returning routing.locales.map((locale) => ({ locale })).

Page component body:
1. Await params, call setRequestLocale(locale)
2. Await getTranslations('doctors')

Section 1 — Page Header:
- section className="py-12 bg-white px-4"
- div className="max-w-7xl mx-auto"
- p className="text-sm text-slate-500 mb-2" — "Home › Our Doctors"
- h1 className="text-xl font-semibold text-slate-900" — "Our Doctors"

Section 2 — Doctors Grid:
- section className="py-12 px-4"
- div className="max-w-7xl mx-auto"
- SectionHeading with title="Our Doctors" subtitle="Our team of specialists is here to help."
- div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 mt-8"
- Map all 6 doctors: doctors.map(doc => <DoctorCard key={doc.id} doctor={doc} />)

Section 3 — CTA row:
- div className="mt-10 text-center"
- Link href="/contact" className="text-blue-800 underline text-sm" — text: "Have questions? Contact us →"
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - app/[locale]/(public)/doctors/page.tsx exists
    - File does NOT contain 'use client'
    - File imports and maps all 6 doctors from '@/lib/data/doctors'
    - File exports generateStaticParams
    - File exports metadata with title containing "Doctors"
    - File contains Link to /contact for the "Contact us" CTA
    - npm run build exits 0
  </acceptance_criteria>
  <done>Doctors page renders all 6 DoctorCards (initials avatars, specialty, Book Appointment CTA per card) in a 3-column grid; statically generates for both locales</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Browser | Pure server-rendered pages; no user input or client state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | XSS via seeded doctor/department content | accept | All content is hardcoded TypeScript constants; React escapes JSX values by default |
</threat_model>

<verification>
After both tasks complete:
1. npm run build exits 0
2. Build output shows ○ (static) for /hi/departments and /hi/doctors routes
3. departments page maps all 8 departments
4. doctors page maps all 6 doctors
</verification>

<success_criteria>
- Departments page renders all 8 specialty department cards in responsive grid (1→2→4 columns)
- Doctors page renders all 6 doctor cards with initials avatars in responsive grid (1→2→3 columns)
- Both pages have breadcrumb navigation, page header, generateStaticParams, and correct SEO metadata
- npm run build exits 0
</success_criteria>

<output>
Create .planning/phases/02-public-website/02-04-SUMMARY.md when done
</output>

---
phase: 09-payroll-tracker
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/payroll.ts
autonomous: true
requirements:
  - PAY-01
  - PAY-03
  - PAY-04

must_haves:
  truths:
    - "getActiveStaffWithPaymentStatus(month) returns all active staff with isPaid/amountPaid for the given month"
    - "getMonthlyPayrollTotal(month) returns the sum of amount_paid from payroll_payments for paid records only"
  artifacts:
    - path: "lib/db/payroll.ts"
      provides: "Two exported async query functions and StaffPayrollRow type"
      exports: ["getActiveStaffWithPaymentStatus", "getMonthlyPayrollTotal", "StaffPayrollRow"]
  key_links:
    - from: "lib/db/payroll.ts"
      to: "lib/supabase/admin.ts"
      via: "createAdminClient() import"
      pattern: "createAdminClient"
    - from: "lib/db/payroll.ts"
      to: "lib/db/staff.ts"
      via: "StaffRole type import"
      pattern: "StaffRole"
---

<objective>
Create lib/db/payroll.ts with query functions for the payroll page data requirements.

Purpose: Provides typed data access for active-staff-with-payment-status and monthly-total queries — both needed by the payroll Server Component page.
Output: lib/db/payroll.ts with two exported functions and one exported interface.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-payroll-tracker/09-CONTEXT.md
@.planning/phases/09-payroll-tracker/09-RESEARCH.md
</context>

<interfaces>
From lib/db/staff.ts:
  export interface StaffMember {
    id: string;
    user_id: string;
    role: StaffRole;
    is_active: boolean;
    full_name: string | null;
    phone: string | null;
    salary: number | null;
    join_date: string | null;
    created_at: string;
    email: string;
  }

From lib/supabase/admin.ts:
  export function createAdminClient(): SupabaseClient

From lib/portal/roles.ts:
  export type StaffRole = 'super_admin' | 'admin' | 'doctor' | 'receptionist'
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/db/payroll.ts query module</name>
  <files>lib/db/payroll.ts</files>
  <read_first>
    - lib/db/staff.ts — two-query + merge pattern (fetch profiles via adminClient, merge additional data client-side)
    - lib/supabase/admin.ts — createAdminClient() import path
    - lib/portal/roles.ts — StaffRole type import path
    - .planning/phases/09-payroll-tracker/09-RESEARCH.md §Pattern 2 — exact function signatures and logic
    - .planning/phases/09-payroll-tracker/09-CONTEXT.md §D-06 — active staff only (is_active = true)
  </read_first>
  <action>
    Create lib/db/payroll.ts with:

    1. Import createAdminClient from '@/lib/supabase/admin'
    2. Import StaffRole from '@/lib/portal/roles'

    3. Export interface StaffPayrollRow:
       - id: string
       - full_name: string | null
       - role: StaffRole
       - salary: number | null
       - isPaid: boolean
       - paidAt: string | null  (ISO timestamptz string or null)
       - amountPaid: number | null

    4. Export async function getActiveStaffWithPaymentStatus(month: string): Promise<StaffPayrollRow[]>
       - month param is ISO date string for first of month, e.g. "2026-06-01"
       - Query 1: adminClient.from('profiles').select('id, full_name, role, salary').eq('is_active', true).order('created_at', { ascending: true })
       - Query 2: adminClient.from('payroll_payments').select('profile_id, amount_paid, paid_at').eq('payment_month', month)
       - Merge: build Map from paymentMap.get(profile.id); for each profile produce a StaffPayrollRow with isPaid = !!payment
       - Throw on error from either query

    5. Export async function getMonthlyPayrollTotal(month: string): Promise<number>
       - Query: adminClient.from('payroll_payments').select('amount_paid').eq('payment_month', month)
       - Return: reduce sum of amount_paid (coerce null to 0); return 0 if no rows
       - Throw on error

    Per pitfall 1 in RESEARCH.md: month string must always be zero-padded "YYYY-MM-01" — document this requirement in JSDoc.
    Per pitfall 3 in RESEARCH.md: getMonthlyPayrollTotal queries ONLY payroll_payments (paid records), not profiles.salary.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep "lib/db/payroll"</automated>
  </verify>
  <acceptance_criteria>
    - lib/db/payroll.ts exists
    - Exports: StaffPayrollRow (interface), getActiveStaffWithPaymentStatus (async function), getMonthlyPayrollTotal (async function)
    - getActiveStaffWithPaymentStatus queries profiles with .eq('is_active', true)
    - getActiveStaffWithPaymentStatus queries payroll_payments with .eq('payment_month', month)
    - getMonthlyPayrollTotal queries ONLY payroll_payments table (not profiles)
    - No TypeScript errors in lib/db/payroll.ts (tsc --noEmit output contains no errors for this file)
    - createAdminClient() called once per function (not shared across functions)
  </acceptance_criteria>
  <done>lib/db/payroll.ts exports StaffPayrollRow, getActiveStaffWithPaymentStatus, and getMonthlyPayrollTotal with no TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component → lib/db/payroll.ts | Server-side only; no user input flows into these query functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Information Disclosure | payroll data via session client | mitigate | Both query functions use createAdminClient() — profiles RLS "own read" policy blocks session-client access; adminClient bypasses RLS |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
- lib/db/payroll.ts exports StaffPayrollRow, getActiveStaffWithPaymentStatus, getMonthlyPayrollTotal
- TypeScript compilation: no errors in lib/db/payroll.ts
- getMonthlyPayrollTotal queries payroll_payments only (not profiles.salary)
</verification>

<success_criteria>
lib/db/payroll.ts is a typed query module with two exported functions that the payroll Server Component can call to hydrate the page.
</success_criteria>

<output>
Create `.planning/phases/09-payroll-tracker/09-02-SUMMARY.md` when done
</output>

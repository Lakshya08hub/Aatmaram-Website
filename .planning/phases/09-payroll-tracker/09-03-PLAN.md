---
phase: 09-payroll-tracker
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - app/(portal)/actions/payroll.ts
autonomous: true
requirements:
  - PAY-02

must_haves:
  truths:
    - "markAsPaidAction inserts a payroll_payments row with a salary snapshot"
    - "markAsPaidAction rejects callers who are not admin or super_admin (throws Forbidden)"
    - "markAsPaidAction rejects payment_month values after the current calendar month"
    - "markAsPaidAction handles duplicate payment gracefully (returns error, does not throw)"
    - "getPayrollDataAction re-fetches staff+payment data and total for a given month (for client-side month navigation)"
  artifacts:
    - path: "app/(portal)/actions/payroll.ts"
      provides: "markAsPaidAction and getPayrollDataAction Server Actions"
      exports: ["markAsPaidAction", "getPayrollDataAction"]
  key_links:
    - from: "app/(portal)/actions/payroll.ts requireAdminRole()"
      to: "app/(portal)/actions/staff.ts requireAdminRole()"
      via: "duplicated inline (not imported) — same logic"
      pattern: "ADMIN_ROLES.*super_admin.*admin"
---

<objective>
Create app/(portal)/actions/payroll.ts with the markAsPaidAction Server Action and a getPayrollDataAction for client-side month navigation re-fetching.

Purpose: Provides the mutation (mark as paid) and read (re-fetch month data) actions that PayrollClient calls. All writes are gated behind requireAdminRole().
Output: app/(portal)/actions/payroll.ts with two exported Server Actions.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-payroll-tracker/09-CONTEXT.md
@.planning/phases/09-payroll-tracker/09-RESEARCH.md
</context>

<interfaces>
From app/(portal)/actions/staff.ts (requireAdminRole pattern to DUPLICATE — do NOT import):
  const ADMIN_ROLES: StaffRole[] = ['super_admin', 'admin']
  async function requireAdminRole(): returns the authenticated supabase client
    - createClient() → getUser() → redirect('/login') if no user
    - query profiles for role → throw Error('Forbidden') if not in ADMIN_ROLES
    - return supabase client

From lib/db/payroll.ts (created in plan 02):
  export interface StaffPayrollRow { id, full_name, role, salary, isPaid, paidAt, amountPaid }
  export async function getActiveStaffWithPaymentStatus(month: string): Promise<StaffPayrollRow[]>
  export async function getMonthlyPayrollTotal(month: string): Promise<number>

From lib/supabase/admin.ts:
  export function createAdminClient(): SupabaseClient
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create app/(portal)/actions/payroll.ts with markAsPaidAction and getPayrollDataAction</name>
  <files>app/(portal)/actions/payroll.ts</files>
  <read_first>
    - app/(portal)/actions/staff.ts — requireAdminRole() function body to duplicate (lines 11-40); Server Action error return pattern { error?: string }
    - lib/db/payroll.ts — getActiveStaffWithPaymentStatus and getMonthlyPayrollTotal signatures
    - .planning/phases/09-payroll-tracker/09-RESEARCH.md §Pattern 3 — markAsPaidAction full logic including future-month guard and error code 23505
    - .planning/phases/09-payroll-tracker/09-RESEARCH.md §Pitfall 4 — future month guard implementation
    - .planning/phases/09-payroll-tracker/09-CONTEXT.md §D-09 — admin/super_admin only
  </read_first>
  <action>
    Create app/(portal)/actions/payroll.ts with 'use server' directive at the top.

    1. Duplicate requireAdminRole() as a private (unexported) async function — do NOT import from actions/staff.ts (per pitfall 2 in RESEARCH.md). Copy the exact same logic:
       - createClient() → getUser() → redirect('/login') if no user
       - query profiles.role where user_id = user.id → throw Error('Forbidden') if role not in ['super_admin', 'admin']
       - return supabase client

    2. Export async function markAsPaidAction(input: { profileId: string; paymentMonth: string; amountPaid: number }): Promise<{ error?: string }>
       - Call requireAdminRole() first — throws Forbidden for non-admins
       - Future month guard: construct currentMonth as toFirstOfMonth(new Date()) = YYYY-MM-01 string; if input.paymentMonth > currentMonth return { error: 'Cannot mark future months as paid.' }
       - Get caller's profile.id: use the supabase session client returned from requireAdminRole() — query profiles.id where user_id = user.id (call getUser() again or store the user from requireAdminRole)
       - createAdminClient() → insert into payroll_payments: { profile_id: input.profileId, payment_month: input.paymentMonth, amount_paid: input.amountPaid, paid_by: callerProfile?.id ?? null }
       - On error.code === '23505' return { error: 'Already marked as paid for this month.' }
       - On other error throw; catch and return { error: message }
       - On success return {}

    3. Export async function getPayrollDataAction(month: string): Promise<{ staff: StaffPayrollRow[]; total: number; error?: string }>
       - Call requireAdminRole() (read gate — only admins can fetch payroll data)
       - Call getActiveStaffWithPaymentStatus(month) and getMonthlyPayrollTotal(month) in parallel via Promise.all
       - Return { staff, total }
       - On error return { staff: [], total: 0, error: message }

    toFirstOfMonth helper (unexported):
      function toFirstOfMonth(date: Date): string
        const y = date.getFullYear()
        const m = String(date.getMonth() + 1).padStart(2, '0')
        return `${y}-${m}-01`
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep "actions/payroll"</automated>
  </verify>
  <acceptance_criteria>
    - app/(portal)/actions/payroll.ts exists with 'use server' at line 1
    - Exports: markAsPaidAction, getPayrollDataAction
    - Does NOT import requireAdminRole from actions/staff.ts — contains its own private copy
    - markAsPaidAction: contains future-month guard comparing paymentMonth to current month string
    - markAsPaidAction: handles error.code === '23505' with specific error message 'Already marked as paid for this month.'
    - markAsPaidAction: uses createAdminClient() for the insert (not session supabase client)
    - getPayrollDataAction: calls getActiveStaffWithPaymentStatus and getMonthlyPayrollTotal
    - No TypeScript errors in this file (tsc --noEmit output contains no errors for actions/payroll)
  </acceptance_criteria>
  <done>app/(portal)/actions/payroll.ts exports markAsPaidAction and getPayrollDataAction with admin gate, future-month guard, and duplicate-payment handling. No TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | profileId, paymentMonth, amountPaid inputs from client — must validate server-side |
| Server Action → Supabase | adminClient writes bypass RLS; requireAdminRole() is the sole access gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Elevation of Privilege | markAsPaidAction — receptionist/doctor callers | mitigate | requireAdminRole() throws 'Forbidden' for non-admin roles; duplicated inline per pitfall 2 |
| T-09-05 | Tampering | marking future-month payments | mitigate | Server-side date guard: paymentMonth > toFirstOfMonth(new Date()) → return error; client-side ChevronRight is disabled but server guard is the authoritative control |
| T-09-06 | Tampering | double-payment via rapid clicks / duplicate requests | mitigate | UNIQUE constraint on (profile_id, payment_month) catches race condition; action returns specific error on code 23505 |
| T-09-07 | Tampering | amountPaid negative value | mitigate | amountPaid: Math.max(0, input.amountPaid) in action — reject negative amounts |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
- app/(portal)/actions/payroll.ts exists with 'use server' directive
- TypeScript compilation: no errors in this file
- markAsPaidAction rejects future paymentMonth values server-side
- markAsPaidAction returns specific error string for code 23505
- getPayrollDataAction calls both query functions
</verification>

<success_criteria>
app/(portal)/actions/payroll.ts provides markAsPaidAction (admin-gated, future-month-guarded, duplicate-safe) and getPayrollDataAction (re-fetch for month navigation) — both compile without TypeScript errors.
</success_criteria>

<output>
Create `.planning/phases/09-payroll-tracker/09-03-SUMMARY.md` when done
</output>

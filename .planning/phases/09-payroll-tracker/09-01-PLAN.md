---
phase: 09-payroll-tracker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260613_payroll_payments.sql
autonomous: true
requirements:
  - PAY-01
  - PAY-02

must_haves:
  truths:
    - "payroll_payments table exists in the database with the correct schema"
    - "UNIQUE constraint on (profile_id, payment_month) prevents duplicate payment records"
    - "RLS is enabled on payroll_payments; no SELECT/INSERT policies (adminClient-only access)"
  artifacts:
    - path: "supabase/migrations/20260613_payroll_payments.sql"
      provides: "DDL for payroll_payments table"
      contains: "UNIQUE (profile_id, payment_month)"
  key_links:
    - from: "payroll_payments.profile_id"
      to: "profiles.id"
      via: "FK REFERENCES profiles(id) ON DELETE CASCADE"
      pattern: "REFERENCES profiles\\(id\\)"
    - from: "payroll_payments.paid_by"
      to: "profiles.id"
      via: "FK REFERENCES profiles(id) ON DELETE SET NULL"
      pattern: "REFERENCES profiles\\(id\\) ON DELETE SET NULL"
---

<objective>
Create the payroll_payments database table and push the schema to Supabase.

Purpose: Establishes the single source of truth for monthly salary payment records — one row per staff member per calendar month.
Output: Migration file committed + schema live in Supabase Postgres.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-payroll-tracker/09-CONTEXT.md
@.planning/phases/09-payroll-tracker/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write payroll_payments migration SQL</name>
  <files>supabase/migrations/20260613_payroll_payments.sql</files>
  <read_first>
    - supabase/migrations/20260613_patient_records.sql — RLS pattern: RLS enabled, no policies (adminClient-only access)
    - supabase/migrations/20260612_staff_schema.sql — confirms profiles.id column type (uuid) and profiles.salary numeric(10,2)
    - .planning/phases/09-payroll-tracker/09-CONTEXT.md §D-01, D-02, D-03 — exact schema spec
  </read_first>
  <action>
    Create supabase/migrations/20260613_payroll_payments.sql with the following table DDL (per D-01):

    Table name: payroll_payments
    Columns:
      - id: uuid PRIMARY KEY DEFAULT gen_random_uuid()
      - profile_id: uuid NOT NULL REFERENCES profiles(id) ON DELETE CASCADE
      - payment_month: date NOT NULL  (stores first day of month, e.g. 2026-06-01, per D-02)
      - amount_paid: numeric(10,2) NOT NULL DEFAULT 0  (salary snapshot at time of payment, per D-03)
      - paid_at: timestamptz NOT NULL DEFAULT now()
      - paid_by: uuid REFERENCES profiles(id) ON DELETE SET NULL  (nullable — who marked it paid)
      CONSTRAINT payroll_payments_unique_month UNIQUE (profile_id, payment_month)

    After the CREATE TABLE:
      ALTER TABLE payroll_payments ENABLE ROW LEVEL SECURITY;
      (No SELECT/INSERT/UPDATE policies — all access via adminClient service role, matching patient_records pattern.)

    Add a comment block at the top: Phase 9 — payroll_payments migration. One row per staff member per calendar month they were marked paid.
  </action>
  <verify>
    <automated>grep -c "UNIQUE (profile_id, payment_month)" "supabase/migrations/20260613_payroll_payments.sql"</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260613_payroll_payments.sql exists
    - Contains: CREATE TABLE IF NOT EXISTS payroll_payments
    - Contains: profile_id uuid NOT NULL REFERENCES profiles(id) ON DELETE CASCADE
    - Contains: payment_month date NOT NULL
    - Contains: amount_paid numeric(10,2) NOT NULL DEFAULT 0
    - Contains: paid_at timestamptz NOT NULL DEFAULT now()
    - Contains: paid_by uuid REFERENCES profiles(id) ON DELETE SET NULL
    - Contains: CONSTRAINT payroll_payments_unique_month UNIQUE (profile_id, payment_month)
    - Contains: ALTER TABLE payroll_payments ENABLE ROW LEVEL SECURITY
    - Does NOT contain any CREATE POLICY statements
  </acceptance_criteria>
  <done>Migration file exists with correct schema, UNIQUE constraint, and RLS enabled (no policies).</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push schema to Supabase</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260613_payroll_payments.sql — the file just created (verify it exists before pushing)
  </read_first>
  <action>
    Run the Supabase schema push command to apply the migration to the database.
    Command: supabase db push

    If the command requires interactive confirmation (e.g. "Are you sure?"), set SUPABASE_ACCESS_TOKEN env var from the project environment and re-run.
    If the push fails with a connection error, flag as autonomous: false and request the user run `supabase db push` manually from the project root.

    After the push succeeds, verify the table exists by running:
    supabase db diff --schema public
    (Expect: no diff, meaning the migration was applied cleanly.)
  </action>
  <verify>
    <automated>supabase db push</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` exits with code 0
    - No error output referencing payroll_payments
    - `supabase db diff --schema public` returns empty diff (or only comments — no structural changes pending)
  </acceptance_criteria>
  <done>payroll_payments table is live in the Supabase Postgres database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → Supabase Postgres | DDL applied via service role; no user input in SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | payroll_payments table — direct client access | mitigate | RLS enabled; no session-role SELECT/INSERT policies; all access via adminClient (service role) |
| T-09-02 | Tampering | duplicate payment via race condition | mitigate | UNIQUE constraint on (profile_id, payment_month) — DB-level atomicity; application catches error code 23505 |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
- supabase/migrations/20260613_payroll_payments.sql exists and contains the required DDL
- `supabase db push` completed without errors
- payroll_payments table is queryable via adminClient
</verification>

<success_criteria>
payroll_payments table is live in Supabase with correct columns, UNIQUE(profile_id, payment_month) constraint, and RLS enabled with no permissive policies.
</success_criteria>

<output>
Create `.planning/phases/09-payroll-tracker/09-01-SUMMARY.md` when done
</output>

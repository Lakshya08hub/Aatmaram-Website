---
phase: 12-homepage-content-curation
plan: 04
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
  - 12-03
files_modified:
  - lib/data/departments.ts
  - lib/data/doctors.ts
  - lib/data/services.ts
autonomous: false
requirements: []

must_haves:
  truths:
    - "Supabase schema has is_featured and featured_order columns on departments, doctors, facilities"
    - "Homepage renders Supabase-sourced data end-to-end with no static file imports"
    - "lib/data/departments.ts and lib/data/doctors.ts are deleted"
    - "lib/data/services.ts is deleted only if it has zero importers in the public site"
  artifacts:
    - path: "supabase/migrations/20260614_featured_columns.sql"
      provides: "Applied migration (via supabase db push)"
  key_links:
    - from: "homepage"
      to: "supabase DB"
      via: "getFeatured* functions after migration is applied"
      pattern: "is_featured"
---

<objective>
Push the migration to Supabase, verify the homepage renders live data, then delete the stale static data files per D-07.

Purpose: Commits the schema change to the live database, closes the loop on D-07 (file deletion), and provides a human checkpoint to confirm the homepage works end-to-end before the phase is marked complete.
Output: Applied migration, deleted stale files, verified working homepage.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-homepage-content-curation/12-CONTEXT.md
@.planning/phases/12-homepage-content-curation/12-02-SUMMARY.md
@.planning/phases/12-homepage-content-curation/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push migration and verify import graph before file deletion</name>
  <files>lib/data/departments.ts, lib/data/doctors.ts, lib/data/services.ts</files>
  <read_first>
    supabase/migrations/20260614_featured_columns.sql — confirm file is present and contains the expected ALTER TABLE statements before pushing.
  </read_first>
  <action>
    Step 1 — Schema push (BLOCKING):
    Run: supabase db push
    This applies 20260614_featured_columns.sql to the remote Supabase project. If the command fails or prompts for confirmation, resolve before proceeding. Do not skip this step.

    Step 2 — Import graph check for lib/data/services.ts (per D-07):
    Run a grep across the codebase to find all files that import from lib/data/services or @/lib/data/services:
      grep -r "lib/data/services\|@/lib/data/services" app/ components/ lib/ --include="*.ts" --include="*.tsx" -l
    If the result is empty (zero importers): delete lib/data/services.ts.
    If any importer is found: do NOT delete lib/data/services.ts; instead add a comment at the top of the file: // TODO: Phase 12 — check this import before deletion.

    Step 3 — Delete confirmed stale files (per D-07):
    Delete lib/data/departments.ts — import graph was cleared in Plan 02 (homepage is the only consumer; it now uses lib/db/departments.ts).
    Delete lib/data/doctors.ts — same reason.

    Confirm with grep after deletion that no file in app/ or components/ still imports from @/lib/data/departments or @/lib/data/doctors.
  </action>
  <verify>
    <automated>powershell -Command "Test-Path 'lib/data/departments.ts'"</automated>
  </verify>
  <acceptance_criteria>
    - supabase db push exited 0 (migration applied successfully)
    - lib/data/departments.ts no longer exists
    - lib/data/doctors.ts no longer exists
    - lib/data/services.ts: deleted if zero importers were found; preserved with TODO comment otherwise
    - grep for "@/lib/data/departments" across app/ and components/ returns zero matches
    - grep for "@/lib/data/doctors" across app/ and components/ returns zero matches
    - npx tsc --noEmit passes (no broken imports)
  </acceptance_criteria>
  <done>Migration is applied to Supabase. Stale static data files are deleted. TypeScript compiles cleanly with no dangling imports.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Homepage now serves Supabase-sourced departments, doctors, and facilities. The new Facilities section appears between the Doctors section and the Appointment CTA. Portal pages at /content/departments, /content/doctors, and /content/facilities each have inline Featured toggle and featured_order inputs.
  </what-built>
  <how-to-verify>
    1. Start the dev server: npm run dev
    2. Visit http://localhost:3000 (or the Hindi locale: http://localhost:3000/hi)
    3. Confirm: Departments section renders cards from the database (not hardcoded names)
    4. Confirm: Doctors section renders doctor cards from the database
    5. Confirm: A "Facilities" section appears after Doctors and before the Appointment CTA band
    6. Visit http://localhost:3000/portal/content/departments
    7. Confirm: Each department row shows a Featured toggle (switch) and an Order number input
    8. Toggle Featured on one department — the switch flips and the page does not crash
    9. Visit http://localhost:3000 again — the toggled department should appear (if it was the only featured item)
    10. Visit http://localhost:3000/portal/content/doctors and http://localhost:3000/portal/content/facilities — confirm same Featured + Order columns appear
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supabase db push → remote Supabase | Admin CLI action modifying live schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Tampering | supabase db push | mitigate | Migration uses ADD COLUMN IF NOT EXISTS — idempotent; running twice causes no harm |
| T-12-09 | Tampering | file deletion (D-07) | mitigate | Import graph grep runs BEFORE deletion; if any importer found, deletion is blocked |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- supabase db push exits 0
- `powershell -Command "Test-Path 'lib/data/departments.ts'"` returns False
- `powershell -Command "Test-Path 'lib/data/doctors.ts'"` returns False
- `npx tsc --noEmit` passes
- Human checkpoint approved
</verification>

<success_criteria>
Migration applied. Stale static data files deleted (departments, doctors; services if zero importers). Homepage confirmed working end-to-end with live Supabase data by human verification. Phase 12 complete.
</success_criteria>

<output>
Create `.planning/phases/12-homepage-content-curation/12-04-SUMMARY.md` when done
</output>

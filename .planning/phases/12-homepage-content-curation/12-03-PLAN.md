---
phase: 12-homepage-content-curation
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - app/(portal)/content/departments/DepartmentsClient.tsx
  - app/(portal)/content/doctors/DoctorsClient.tsx
  - app/(portal)/content/facilities/FacilitiesClient.tsx
  - app/(portal)/actions/content.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Portal /content/departments list shows Featured toggle and featured_order input inline per row"
    - "Portal /content/doctors list shows Featured toggle and featured_order input inline per row"
    - "Portal /content/facilities list shows Featured toggle and featured_order input inline per row"
    - "Toggling Featured or changing order immediately updates the DB and revalidates homepage"
    - "Featured count label shows X of Y featured for departments and doctors"
  artifacts:
    - path: "app/(portal)/actions/content.ts"
      provides: "toggleFeatured and setFeaturedOrder Server Actions for all three content types"
      exports: ["toggleFeatured", "setFeaturedOrder"]
    - path: "app/(portal)/content/departments/DepartmentsClient.tsx"
      provides: "Departments table with Featured Switch + featured_order input columns"
    - path: "app/(portal)/content/doctors/DoctorsClient.tsx"
      provides: "Doctors table with Featured Switch + featured_order input columns"
    - path: "app/(portal)/content/facilities/FacilitiesClient.tsx"
      provides: "Facilities table with Featured Switch + featured_order input columns"
  key_links:
    - from: "app/(portal)/content/departments/DepartmentsClient.tsx"
      to: "app/(portal)/actions/content.ts"
      via: "toggleFeatured Server Action import"
      pattern: "toggleFeatured"
    - from: "app/(portal)/actions/content.ts"
      to: "supabase (via createAdminClient)"
      via: "UPDATE departments SET is_featured, featured_order"
      pattern: "is_featured"
---

<objective>
Add inline Featured toggle (shadcn Switch) and featured_order number input to the three portal content list pages, backed by new Server Actions that persist changes to Supabase and revalidate both the portal page and the public homepage.

Purpose: Gives the portal admin the ability to curate which departments, doctors, and facilities appear on the homepage — the control plane for the homepage data delivered by Plan 02.
Output: Three updated Client Components with toggle UI, and new Server Actions in content.ts.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-homepage-content-curation/12-CONTEXT.md
@.planning/phases/12-homepage-content-curation/12-UI-SPEC.md
@.planning/phases/12-homepage-content-curation/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add toggleFeatured and setFeaturedOrder Server Actions to content.ts</name>
  <files>app/(portal)/actions/content.ts</files>
  <read_first>
    app/(portal)/actions/content.ts — read the entire file to understand: import pattern (requireCmsRole, createAdminClient), revalidatePath usage, and the shape of existing toggle actions (e.g., toggleDepartmentActive or similar) to replicate exactly.
    app/(portal)/content/departments/DepartmentsClient.tsx — skim to confirm how existing Server Actions are called from Client Components (e.g., inline function call, FormData vs plain args).
  </read_first>
  <action>
    Append to app/(portal)/actions/content.ts (do NOT replace existing actions):

    1. toggleFeatured(table: 'departments' | 'doctors' | 'facilities', id: string, value: boolean): Promise<void>
       - Call requireCmsRole() at top (existing auth guard pattern)
       - Use createAdminClient() to get Supabase client
       - UPDATE the specified table SET is_featured = value WHERE id = id
       - revalidatePath('/content/' + table) — revalidates the portal list page
       - revalidatePath('/en') and revalidatePath('/hi') — revalidates both locale homepages
       - Throw on Supabase error

    2. setFeaturedOrder(table: 'departments' | 'doctors' | 'facilities', id: string, order: number): Promise<void>
       - Same auth guard and client pattern
       - UPDATE the specified table SET featured_order = order WHERE id = id
       - Same revalidatePath calls as above

    Both functions must be marked with 'use server' directive (or the file already has it at top — do not add a second one).

    Max caps enforcement (discretion per CONTEXT.md):
    - In toggleFeatured when value=true for doctors: run a count query first; if current featured count >= 3, throw new Error('Maximum 3 featured doctors allowed').
    - Same cap for departments: >= 8 throws 'Maximum 8 featured departments allowed'.
    - Facilities: no hard cap enforced in Server Action (admin decides freely per CONTEXT.md).
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'app/(portal)/actions/content.ts' -Pattern 'toggleFeatured|setFeaturedOrder' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - toggleFeatured exported from app/(portal)/actions/content.ts
    - setFeaturedOrder exported from app/(portal)/actions/content.ts
    - Both functions call requireCmsRole() before any DB operation
    - Both functions revalidate /en AND /hi paths
    - toggleFeatured enforces max-3 cap for doctors and max-8 cap for departments
    - No TypeScript errors (npx tsc --noEmit)
    - Existing exports in content.ts are unchanged
  </acceptance_criteria>
  <done>Two new Server Actions exported from content.ts. toggleFeatured and setFeaturedOrder update the DB, enforce caps for doctors/departments, and revalidate both locale homepages.</done>
</task>

<task type="auto">
  <name>Task 2: Add Featured column UI to all three portal content Client Components</name>
  <files>app/(portal)/content/departments/DepartmentsClient.tsx, app/(portal)/content/doctors/DoctorsClient.tsx, app/(portal)/content/facilities/FacilitiesClient.tsx</files>
  <read_first>
    app/(portal)/content/departments/DepartmentsClient.tsx — read entire file. Note: the table structure, how the is_active Switch is rendered (this is the exact pattern to replicate for is_featured), how optimistic state is managed (useState or useOptimistic), and how existing Server Actions are called inline.
    app/(portal)/content/doctors/DoctorsClient.tsx — read entire file for same pattern.
    app/(portal)/content/facilities/FacilitiesClient.tsx — read entire file; may have a different structure if facilities was added in a later phase.
  </read_first>
  <action>
    For EACH of the three Client Components (DepartmentsClient, DoctorsClient, FacilitiesClient), apply the same set of changes:

    1. Import toggleFeatured and setFeaturedOrder from @/app/(portal)/actions/content.
    2. Import Switch from @/components/ui/switch (shadcn — already installed).
    3. Add optimistic local state for is_featured and featured_order per row. Use the same useState/useOptimistic pattern already used for is_active in that file.

    4. Add two new columns to the table header row (append after existing columns, before any action column):
       - "Featured" column header
       - "Order" column header

    5. In each table data row, add two new cells:
       a. Featured cell: <Switch checked={item.is_featured} onCheckedChange={async (checked) => { /* optimistic update */ await toggleFeatured('departments'|'doctors'|'facilities', item.id, checked); }} />
          The Switch must have a minimum touch target of 44px height — wrap in a div with min-h-[44px] flex items-center if needed.
       b. Order cell: <input type="number" min="0" max="99" defaultValue={item.featured_order} className="w-14 border rounded px-1 text-sm" onBlur={async (e) => { await setFeaturedOrder('departments'|'doctors'|'facilities', item.id, Number(e.target.value)); }} />
          Use uncontrolled input with defaultValue (not value) to avoid re-render issues on blur.

    6. Add featured count label above the table:
       - Compute: const featuredCount = items.filter(i => i.is_featured).length;
       - Render: <p className="text-sm text-slate-500">{featuredCount} of {maxFeatured} featured</p>
       - maxFeatured values: DepartmentsClient=8, DoctorsClient=3, FacilitiesClient omit the "of X" part (just show "{count} featured")

    Use 'departments', 'doctors', or 'facilities' as the literal table string passed to Server Actions — match exactly the table names used in content.ts.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'app/(portal)/content/departments/DepartmentsClient.tsx','app/(portal)/content/doctors/DoctorsClient.tsx','app/(portal)/content/facilities/FacilitiesClient.tsx' -Pattern 'toggleFeatured|setFeaturedOrder' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - All three Client Component files import toggleFeatured and setFeaturedOrder
    - All three files render a Switch for is_featured per row
    - All three files render a number input for featured_order per row
    - DepartmentsClient shows "X of 8 featured" count label
    - DoctorsClient shows "X of 3 featured" count label
    - FacilitiesClient shows "X featured" count label (no cap)
    - Input uses onBlur (not onChange) to call setFeaturedOrder
    - Switch uses onCheckedChange to call toggleFeatured
    - npx tsc --noEmit passes with no new errors
  </acceptance_criteria>
  <done>All three portal content pages show inline Featured switch and order input. Changes call Server Actions immediately with optimistic local state updates matching the existing is_active toggle pattern.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Portal Client → Server Action | Authenticated portal user sends toggle/order mutations |
| Server Action → Supabase | Admin client writes to departments/doctors/facilities tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Elevation of Privilege | toggleFeatured / setFeaturedOrder | mitigate | requireCmsRole() called at top of each Server Action — non-CMS users get 403 before any DB write |
| T-12-06 | Tampering | setFeaturedOrder input value | mitigate | Server Action receives Number(value) — non-numeric inputs coerce to NaN; add guard: if (isNaN(order) or order < 0) return early without DB write |
| T-12-07 | Denial of Service | revalidatePath on every toggle | accept | Portal admin actions are low-frequency; revalidating both locale paths per toggle is acceptable |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- `powershell -Command "Select-String -Path 'app/(portal)/actions/content.ts' -Pattern 'toggleFeatured|setFeaturedOrder' | Measure-Object"` — count >= 2
- `powershell -Command "Select-String -Path 'app/(portal)/content/departments/DepartmentsClient.tsx' -Pattern 'is_featured|toggleFeatured'"` — matches
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
Server Actions for featured toggling and order setting are in content.ts with role guards and homepage revalidation. All three portal content list pages have inline Featured Switch and order input. Cap enforcement prevents > 3 featured doctors and > 8 featured departments at the Server Action level.
</success_criteria>

<output>
Create `.planning/phases/12-homepage-content-curation/12-03-SUMMARY.md` when done
</output>

---
phase: 12-homepage-content-curation
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - components/public/FacilityCard.tsx
  - app/[locale]/(public)/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Homepage Departments section renders Supabase-sourced department cards (not hardcoded data)"
    - "Homepage Doctors section renders Supabase-sourced doctor cards"
    - "Homepage Facilities section exists and renders between Doctors section and Appointment CTA"
    - "FacilityCard component renders name, description, and Building2 fallback icon"
    - "Homepage never shows an empty section — fallback to all records if no featured items"
  artifacts:
    - path: "components/public/FacilityCard.tsx"
      provides: "FacilityCard presentational component"
      exports: ["FacilityCard"]
    - path: "app/[locale]/(public)/page.tsx"
      provides: "Updated homepage with force-dynamic and three Supabase-sourced sections"
      contains: "force-dynamic"
  key_links:
    - from: "app/[locale]/(public)/page.tsx"
      to: "lib/db/departments.ts"
      via: "getFeaturedDepartments() import"
      pattern: "getFeaturedDepartments"
    - from: "app/[locale]/(public)/page.tsx"
      to: "lib/db/facilities.ts"
      via: "getFeaturedFacilities() import"
      pattern: "getFeaturedFacilities"
    - from: "app/[locale]/(public)/page.tsx"
      to: "components/public/FacilityCard.tsx"
      via: "FacilityCard import"
      pattern: "FacilityCard"
---

<objective>
Create the FacilityCard component and update the homepage Server Component to replace all hardcoded static imports with live Supabase queries, adding a Facilities section between Doctors and Appointment CTA.

Purpose: Delivers the user-visible change — homepage sections now reflect portal-curated content instead of static data files.
Output: FacilityCard component, updated homepage with force-dynamic and three async data fetches.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-homepage-content-curation/12-CONTEXT.md
@.planning/phases/12-homepage-content-curation/12-UI-SPEC.md
@.planning/phases/12-homepage-content-curation/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FacilityCard component</name>
  <files>components/public/FacilityCard.tsx</files>
  <read_first>
    components/public/DepartmentCard.tsx — read entire file to understand the card structure, icon fallback pattern (Building2), Tailwind classes used, and prop interface shape. FacilityCard replicates this structure.
    components/public/DoctorCard.tsx — skim for any shared class patterns used across public cards.
  </read_first>
  <action>
    Create components/public/FacilityCard.tsx as a Client Component (or Server Component — match whatever DepartmentCard is). Props interface:
    - name: string (required)
    - description: string (required)
    - image_url?: string (optional, unused in v1 — accept but do not render)

    Visual structure mirrors DepartmentCard exactly:
    - Building2 from lucide-react as the icon (always, no conditional — per D-04 pattern)
    - name rendered as text-xl font-semibold
    - description rendered with line-clamp-3 (Tailwind class) and text-slate-600
    - Same card wrapper classes as DepartmentCard (border, rounded, padding, hover shadow)

    Export: export function FacilityCard({ name, description, image_url }: FacilityCardProps)

    Do NOT accept an icon prop — Building2 is always used (per D-05 which says "reuse/adapt DepartmentCard").
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'components/public/FacilityCard.tsx' -Pattern 'Building2|line-clamp-3|FacilityCard' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at components/public/FacilityCard.tsx
    - Exports FacilityCard function
    - Imports Building2 from lucide-react
    - Props interface includes name (string), description (string), image_url? (string | undefined)
    - description rendered with line-clamp-3 class
    - Visual structure matches DepartmentCard (same card wrapper, same heading size text-xl)
    - No TypeScript errors (npx tsc --noEmit)
  </acceptance_criteria>
  <done>FacilityCard component renders a facility row with Building2 icon, name, and clamped description — structurally identical to DepartmentCard.</done>
</task>

<task type="auto">
  <name>Task 2: Wire homepage to Supabase — replace static data with getFeatured* calls</name>
  <files>app/[locale]/(public)/page.tsx</files>
  <read_first>
    app/[locale]/(public)/page.tsx — read the entire file to understand current structure: which static imports to remove, existing section order (Hero, Stats, Departments, Doctors, Appointment CTA), and how props flow into DepartmentCard / DoctorCard.
    app/[locale]/(public)/departments/page.tsx — read to confirm the force-dynamic pattern and how DepartmentCard is called without the icon prop.
    app/[locale]/(public)/doctors/page.tsx — read to confirm how DoctorCard receives name, initials, specialty.
  </read_first>
  <action>
    Modify app/[locale]/(public)/page.tsx with these surgical changes:

    1. REMOVE: import statements from @/lib/data/departments and @/lib/data/doctors (the hardcoded static files).

    2. ADD at top of file (after existing imports): export const dynamic = 'force-dynamic';

    3. ADD imports: getFeaturedDepartments from @/lib/db/departments; getFeaturedDoctors from @/lib/db/doctors; getFeaturedFacilities from @/lib/db/facilities; FacilityCard from @/components/public/FacilityCard.

    4. Make the page component async (add async keyword if not already present). At the top of the component body, await all three:
       const departments = await getFeaturedDepartments();
       const doctors = await getFeaturedDoctors();
       const facilities = await getFeaturedFacilities();

    5. In the Departments section: map over the `departments` array. Pass name and description to DepartmentCard. Do NOT pass icon prop (per D-04).

    6. In the Doctors section: map over the `doctors` array. For each doctor row, derive:
       - name: doctor.full_name
       - initials: doctor.full_name.split(' ').map((n: string) => n[0]).join('').slice(0, 2).toUpperCase()
       - specialty: doctor.specialization
       Pass these to DoctorCard along with any existing bookLabel prop.

    7. ADD a new Facilities section AFTER the Doctors section and BEFORE the Appointment CTA band. Structure:
       - Section wrapper: same py-12 or py-16 padding used by Departments section
       - SectionHeading with the translated "Our Facilities" key (use t('facilities.heading') or equivalent — match the i18n key pattern used by other sections; if the key does not yet exist in translation files, use the string literal "Our Facilities" and add a TODO comment)
       - Grid: className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6"
       - Map over `facilities` array, render FacilityCard with name and description for each

    8. REMOVE generateStaticParams() if it exists in this file.

    Do not change Hero, Stats, or Appointment CTA sections. Preserve all existing i18n useTranslations calls.
  </action>
  <verify>
    <automated>powershell -Command "(Get-Content 'app/[locale]/(public)/page.tsx') -match 'lib/data/(departments|doctors)' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - No import from @/lib/data/departments or @/lib/data/doctors remains in the file
    - export const dynamic = 'force-dynamic' is present
    - Page component function has async keyword
    - getFeaturedDepartments(), getFeaturedDoctors(), getFeaturedFacilities() are all awaited at top of component
    - FacilityCard import is present
    - Facilities section exists in JSX between the Doctors section and Appointment CTA
    - FacilityCard is rendered inside a grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 container
    - DoctorCard receives initials computed from full_name
    - npx tsc --noEmit passes with no new errors
    - grep count for "lib/data/" in page.tsx returns 0
  </acceptance_criteria>
  <done>Homepage is fully Supabase-sourced with three async queries, a new Facilities section, and no remaining static data file imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase DB → homepage Server Component | Data fetched server-side; never exposed as client API |
| getFeatured* results → JSX render | Array data mapped to presentational components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Information Disclosure | app/[locale]/(public)/page.tsx | accept | Homepage only exposes department/doctor/facility names and descriptions — public-facing info, no PII |
| T-12-04 | Denial of Service | getFeatured* on every homepage request | accept | force-dynamic adds per-request DB reads; acceptable for hospital site traffic volume |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- `powershell -Command "(Get-Content 'app/[locale]/(public)/page.tsx') -match 'lib/data'" | Measure-Object` — must return 0
- `powershell -Command "Select-String -Path 'app/[locale]/(public)/page.tsx' -Pattern 'force-dynamic'"` — must match
- `powershell -Command "Select-String -Path 'app/[locale]/(public)/page.tsx' -Pattern 'FacilityCard|getFeaturedFacilities'"` — must match
- `npx tsc --noEmit` passes with no new errors
</verification>

<success_criteria>
Homepage imports zero static data files. Three Supabase-backed sections (Departments, Doctors, Facilities) render in correct order. FacilityCard component exists and is used in the new Facilities section. force-dynamic is set on the homepage. TypeScript compiles cleanly.
</success_criteria>

<output>
Create `.planning/phases/12-homepage-content-curation/12-02-SUMMARY.md` when done
</output>

---
phase: 12-homepage-content-curation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260614_featured_columns.sql
  - lib/db/departments.ts
  - lib/db/doctors.ts
  - lib/db/facilities.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "getFeaturedDepartments() returns featured departments ordered by featured_order ASC, falling back to all departments if none are featured"
    - "getFeaturedDoctors() returns active featured doctors, falling back to all active doctors if none are featured"
    - "getFeaturedFacilities() returns featured facilities, falling back to all active facilities if none are featured"
    - "Migration file adds is_featured and featured_order columns to all three tables idempotently"
  artifacts:
    - path: "supabase/migrations/20260614_featured_columns.sql"
      provides: "ALTER TABLE statements for departments, doctors, facilities"
    - path: "lib/db/departments.ts"
      provides: "getFeaturedDepartments() export"
      exports: ["getFeaturedDepartments"]
    - path: "lib/db/doctors.ts"
      provides: "getFeaturedDoctors() export"
      exports: ["getFeaturedDoctors"]
    - path: "lib/db/facilities.ts"
      provides: "getFeaturedFacilities() export"
      exports: ["getFeaturedFacilities"]
  key_links:
    - from: "lib/db/departments.ts"
      to: "supabase/migrations/20260614_featured_columns.sql"
      via: "column names is_featured, featured_order"
      pattern: "is_featured"
---

<objective>
Add is_featured and featured_order columns to departments, doctors, and facilities tables via a new migration, then extend the three DB query modules with getFeatured* functions that implement the fallback logic required by D-02.

Purpose: Homepage Server Component needs typed query functions before it can be wired. Portal toggle Server Actions also depend on these columns existing. This plan delivers the data layer foundation that Wave 2 plans build on.
Output: One migration SQL file, three extended query modules each exporting a new getFeatured* function.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-homepage-content-curation/12-CONTEXT.md
@.planning/phases/12-homepage-content-curation/12-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration SQL for featured columns</name>
  <files>supabase/migrations/20260614_featured_columns.sql</files>
  <read_first>
    supabase/migrations/20260612_cms_tables.sql — read the full DDL to confirm exact table names (departments, doctors, facilities) and existing column names so the ALTER TABLE statements do not conflict.
  </read_first>
  <behavior>
    - Migration must use ADD COLUMN IF NOT EXISTS so it is safe to re-run
    - Three tables: departments, doctors, facilities
    - Each gets: is_featured BOOLEAN NOT NULL DEFAULT false, featured_order INTEGER NOT NULL DEFAULT 0
    - File must be valid PostgreSQL; no PL/pgSQL required
  </behavior>
  <action>
    Create supabase/migrations/20260614_featured_columns.sql with six ALTER TABLE statements (two per table). Use the pattern:

    ALTER TABLE departments ADD COLUMN IF NOT EXISTS is_featured BOOLEAN NOT NULL DEFAULT false;
    ALTER TABLE departments ADD COLUMN IF NOT EXISTS featured_order INTEGER NOT NULL DEFAULT 0;

    Repeat for doctors and facilities. Do NOT include CREATE TABLE, DROP, or TRUNCATE statements. Do NOT modify any existing column from 20260612_cms_tables.sql.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'supabase/migrations/20260614_featured_columns.sql' -Pattern 'ADD COLUMN IF NOT EXISTS' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at supabase/migrations/20260614_featured_columns.sql
    - Contains exactly 6 occurrences of ADD COLUMN IF NOT EXISTS (2 per table x 3 tables)
    - All three table names appear: departments, doctors, facilities
    - Both column names appear in each table block: is_featured, featured_order
    - No DROP or TRUNCATE statements present
  </acceptance_criteria>
  <done>Migration file is valid SQL with idempotent ALTER TABLE statements for all three tables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add getFeatured* query functions to lib/db modules</name>
  <files>lib/db/departments.ts, lib/db/doctors.ts, lib/db/facilities.ts</files>
  <read_first>
    lib/db/departments.ts — read the entire file to understand the existing getDepartments() pattern (Supabase client creation, typed return, error handling).
    lib/db/doctors.ts — read for getActiveDoctors() pattern; note the is_active filter.
    lib/db/facilities.ts — read the full file; note existing getFacilities() if present.
  </read_first>
  <behavior>
    - Per D-06 and RESEARCH.md recommendation: use a single query ordered by (is_featured DESC, featured_order ASC, created_at ASC). In application code, slice to only featured rows if any exist; otherwise return the full result set. This avoids a second DB round trip per request.
    - getFeaturedDepartments(): SELECT * FROM departments ORDER BY is_featured DESC, featured_order ASC, created_at ASC — if any row has is_featured=true, return only those rows; else return all rows (D-02 fallback).
    - getFeaturedDoctors(): Same pattern but also filter is_active=true in the query (always). Featured filter applied in application code after fetch.
    - getFeaturedFacilities(): Same single-query pattern as departments.
    - Return type must be the existing typed interface from each module (e.g., Department[], Doctor[], Facility[]). Do NOT use `any`.
    - Do NOT replace or modify getDepartments(), getActiveDoctors(), or getFacilities() — they are still used by the /departments and /doctors public pages.
    - Export the new functions as named exports appended to the bottom of each file.
    - Use the same createClient (supabase/server.ts) pattern as the existing functions in each file.
  </behavior>
  <action>
    In lib/db/departments.ts: append async function getFeaturedDepartments() that fetches all departments ordered by is_featured DESC, featured_order ASC, created_at ASC, then returns rows where is_featured=true if any exist, else returns all rows.

    In lib/db/doctors.ts: append async function getFeaturedDoctors() that fetches all doctors where is_active=true ordered by is_featured DESC, featured_order ASC, created_at ASC, then applies the same featured-slice-or-all logic in application code.

    In lib/db/facilities.ts: append async function getFeaturedFacilities() following the identical pattern as getFeaturedDepartments(). If getFacilities() does not already exist in this file, do not create it — only add getFeaturedFacilities().

    All three functions must be async, return the module's existing typed array (or Promise of it), and follow the exact same Supabase client usage pattern as the existing query functions in each file.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'lib/db/departments.ts','lib/db/doctors.ts','lib/db/facilities.ts' -Pattern 'export.*getFeatured' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - lib/db/departments.ts exports getFeaturedDepartments (grep confirms)
    - lib/db/doctors.ts exports getFeaturedDoctors (grep confirms)
    - lib/db/facilities.ts exports getFeaturedFacilities (grep confirms)
    - Each function uses is_featured DESC ordering in its Supabase query
    - Each function applies application-level slice: if any item has is_featured=true, return only those; else return all
    - Existing exports (getDepartments, getActiveDoctors, getFacilities) still present and unchanged
    - No TypeScript `any` type used in the new functions
  </acceptance_criteria>
  <done>Three getFeatured* functions exported from their respective lib/db/ modules with correct fallback logic per D-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → Supabase DB | SQL executed with admin privileges during db push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | supabase/migrations/20260614_featured_columns.sql | mitigate | Use ADD COLUMN IF NOT EXISTS — idempotent, safe to re-run; no destructive statements |
| T-12-02 | Information Disclosure | lib/db/getFeatured* | accept | Functions read non-sensitive schema metadata (is_featured, featured_order); no PII involved |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- Run: `powershell -Command "Select-String -Path 'supabase/migrations/20260614_featured_columns.sql' -Pattern 'ADD COLUMN IF NOT EXISTS' | Measure-Object | Select-Object -ExpandProperty Count"` — must output 6
- Run: `powershell -Command "Select-String -Path 'lib/db/departments.ts','lib/db/doctors.ts','lib/db/facilities.ts' -Pattern 'export.*getFeatured' | Measure-Object | Select-Object -ExpandProperty Count"` — must output 3
- TypeScript compile check: `npx tsc --noEmit` passes with no new errors
</verification>

<success_criteria>
Migration SQL exists with 6 idempotent ALTER TABLE statements. Three new getFeatured* functions are exported from lib/db/ modules, each implementing single-query fetch with application-level featured-slice fallback per D-02. No existing query functions are modified.
</success_criteria>

<output>
Create `.planning/phases/12-homepage-content-curation/12-01-SUMMARY.md` when done
</output>

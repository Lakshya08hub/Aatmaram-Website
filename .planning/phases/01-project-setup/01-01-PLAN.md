---
phase: 01-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - app/globals.css
  - app/layout.tsx
  - app/[locale]/layout.tsx
  - app/[locale]/(public)/page.tsx
  - app/(portal)/layout.tsx
  - app/(portal)/login/page.tsx
  - app/(portal)/dashboard/page.tsx
  - app/api/.gitkeep
  - i18n/.gitkeep
  - messages/.gitkeep
  - lib/supabase/.gitkeep
  - proxy.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "npm run dev starts on localhost:3000 without compile errors"
    - "Tailwind CSS utility classes are processed — a div with bg-blue-500 renders blue"
    - "The correct folder structure exists: app/[locale]/(public)/, app/(portal)/, app/api/, i18n/, messages/, lib/supabase/"
    - "TypeScript compiles clean — npm run build exits 0"
    - "proxy.ts exists at project root (not middleware.ts)"
  artifacts:
    - path: "proxy.ts"
      provides: "Next.js 16 middleware file — locale routing entry point"
      contains: "createMiddleware"
    - path: "app/[locale]/layout.tsx"
      provides: "[locale] layout with NextIntlClientProvider stub"
      contains: "await params"
    - path: "app/(portal)/layout.tsx"
      provides: "Portal layout stub"
    - path: "app/globals.css"
      provides: "Tailwind v4 import"
      contains: "@import \"tailwindcss\""
    - path: "app/layout.tsx"
      provides: "Root layout"
  key_links:
    - from: "proxy.ts"
      to: "i18n/routing.ts"
      via: "import routing"
      pattern: "from.*i18n/routing"
    - from: "next.config.ts"
      to: "i18n/request.ts"
      via: "createNextIntlPlugin"
      pattern: "withNextIntl"
---

<objective>
Scaffold the complete Next.js 16 project with the required folder structure, install all dependencies, and verify Tailwind CSS v4 renders correctly on a placeholder page.

Purpose: Every subsequent phase drops files into the folder structure created here. The structure must be correct on day one — it cannot be restructured later without cascading changes.
Output: A running Next.js 16 project with the three routing zones wired (public locale routes, portal routes, API routes), Tailwind v4 processing CSS, and TypeScript compiling clean.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup/01-RESEARCH.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install | Third-party packages enter the project dependency graph here |
| .env.local | Secrets injected at runtime — must never reach version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.local | mitigate | .gitignore must list .env.local (create-next-app generates this — verify before first commit) |
| T-01-02 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY | mitigate | Never prefix with NEXT_PUBLIC_ — server-only env var; verify in .env.local |
| T-01-03 | Information Disclosure | GEMINI_API_KEY | mitigate | Never use NEXT_PUBLIC_GEMINI_API_KEY — API route only (Phase 11) |
| T-01-SC | Tampering | npm package installs | mitigate | All packages in [ASSUMED] state — Task 1 requires human verification checkpoint before install |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package Legitimacy Gate — verify [ASSUMED] packages before install</name>
  <what-built>
    The RESEARCH.md Package Legitimacy Audit lists all packages as [ASSUMED] because slopcheck was not executable at research time. Before installing any packages, the executor must confirm legitimacy of the seven packages.
  </what-built>
  <how-to-verify>
    Confirm each of the following packages is legitimate by checking the registry page (npmjs.com) and verifying it belongs to the stated organization:

    1. https://www.npmjs.com/package/next — vercel org, 15 yrs, weekly downloads >6M
    2. https://www.npmjs.com/package/typescript — microsoft org, 14 yrs
    3. https://www.npmjs.com/package/tailwindcss — tailwindlabs org, 9 yrs
    4. https://www.npmjs.com/package/@tailwindcss/postcss — tailwindlabs org
    5. https://www.npmjs.com/package/next-intl — amannn (individual), 6 yrs, 1M+ weekly downloads
    6. https://www.npmjs.com/package/@supabase/supabase-js — supabase org, 6 yrs
    7. https://www.npmjs.com/package/@supabase/ssr — supabase org

    Expected: all packages are from their stated orgs with ages and download counts matching research data.
  </how-to-verify>
  <resume-signal>Type "packages verified" to proceed with install</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold project and install all dependencies</name>
  <files>package.json, package-lock.json, tsconfig.json, postcss.config.mjs, app/globals.css, app/layout.tsx</files>
  <read_first>
    - .planning/phases/01-project-setup/01-RESEARCH.md (Standard Stack section, Pattern 9: Tailwind CSS v4 Setup, Anti-Patterns to Avoid)
  </read_first>
  <action>
    Run create-next-app from the project root directory. The project already exists as a git repo at the working directory — scaffold INTO the current directory, not a subdirectory.

    Command: npx create-next-app@latest . --typescript --tailwind --eslint --app --import-alias "@/*" --no-src-dir

    Flags explained:
    - `.` — scaffold into current directory
    - `--no-src-dir` — root-level app/ directory, not src/app/ (per locked decision in CLAUDE.md)
    - `--import-alias "@/*"` — enables @/* path alias

    After scaffolding completes, install the i18n and Supabase packages:

    Command: npm install next-intl@4.13.0 @supabase/supabase-js@2.108.1 @supabase/ssr@0.12.0

    Pin exact versions as listed above — these are the verified versions from RESEARCH.md.

    After install, verify postcss.config.mjs uses @tailwindcss/postcss (not tailwind-postcss). create-next-app generates this correctly for v4 but confirm it matches Pattern 9 in RESEARCH.md.

    Verify app/globals.css contains `@import "tailwindcss"` (the v4 import syntax). If create-next-app generated the old `@tailwind base; @tailwind components; @tailwind utilities;` syntax (v3), replace with `@import "tailwindcss";`.
  </action>
  <verify>
    <automated>cd "D:\Git Hub\Aatmaram Website" && node -e "const p = require('./package.json'); ['next-intl','@supabase/supabase-js','@supabase/ssr'].forEach(d => { if(!p.dependencies[d]) throw new Error('Missing: ' + d); }); console.log('deps ok');"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains next-intl, @supabase/supabase-js, and @supabase/ssr in dependencies
    - app/globals.css first non-empty line is `@import "tailwindcss";`
    - postcss.config.mjs exports an object with plugins: { '@tailwindcss/postcss': {} }
    - tsconfig.json paths includes "@/*": ["./*"]
    - .gitignore contains .env.local (create-next-app generates this — verify)
  </acceptance_criteria>
  <done>All packages installed, Tailwind v4 CSS import in place, TypeScript path aliases configured</done>
</task>

<task type="auto">
  <name>Task 3: Create the three-zone folder structure and placeholder pages</name>
  <files>
    proxy.ts,
    app/[locale]/layout.tsx,
    app/[locale]/(public)/page.tsx,
    app/(portal)/layout.tsx,
    app/(portal)/login/page.tsx,
    app/(portal)/dashboard/page.tsx,
    app/api/.gitkeep,
    i18n/routing.ts,
    i18n/request.ts,
    messages/en.json,
    messages/hi.json,
    lib/supabase/client.ts,
    lib/supabase/server.ts,
    lib/supabase/middleware.ts
  </files>
  <read_first>
    - .planning/phases/01-project-setup/01-RESEARCH.md (Recommended Project Structure, Pattern 1 through Pattern 8, Pitfall 1 through Pitfall 7)
    - app/layout.tsx (the root layout scaffolded by create-next-app — do not delete it, adjust it)
  </read_first>
  <action>
    Create the following files. Do NOT use fenced code blocks in this action — all file contents are described as identifiers and directives.

    CRITICAL: The middleware file is named proxy.ts at the project root, NOT middleware.ts. This is a Next.js 16 breaking change. See Pitfall 1 in RESEARCH.md.

    --- proxy.ts (project root) ---
    Import createMiddleware from next-intl/middleware. Import routing from ./i18n/routing. Export default as createMiddleware(routing) — do NOT use the async composed version yet (that is Phase 4). Export config with a matcher array containing one pattern that excludes portal, api, _next/static, _next/image, favicon.ico, sitemap.xml, and robots.txt using a negative lookahead: /((?!portal|api|_next/static|_next/image|favicon.ico|sitemap.xml|robots.txt).*).

    --- i18n/routing.ts ---
    Import defineRouting from next-intl/routing. Export const routing = defineRouting with locales: ['hi', 'en'], defaultLocale: 'hi', and localePrefix: 'always'. Use 'always' not 'as-needed' — this avoids the known redirect bug documented in Pitfall 4 of RESEARCH.md.

    --- i18n/request.ts ---
    Import getRequestConfig from next-intl/server. Import routing from ./routing. Export default getRequestConfig with an async callback receiving requestLocale. Await requestLocale into a let locale variable. Validate: if !locale or routing.locales does not include locale, set locale = routing.defaultLocale. Return an object with BOTH locale AND messages properties. Omitting locale causes "Unable to find next-intl locale" error (Pitfall 3). Load messages via dynamic import of ../../messages/${locale}.json and access .default.

    --- messages/en.json ---
    Minimal JSON object: { "site": { "title": "Atmaram Child Care and Critical Care", "tagline": "Compassionate Healthcare in Kanpur" } }

    --- messages/hi.json ---
    Minimal JSON object: { "site": { "title": "आत्माराम चाइल्ड केयर एंड क्रिटिकल केयर", "tagline": "कानपुर में करुणामय स्वास्थ्य सेवा" } }

    --- app/layout.tsx (root layout — replace create-next-app's version) ---
    Minimal root layout. Does NOT import next-intl here (that is the [locale] layout's job). No html or body tags with locale here. Export metadata with title: "Atmaram Child Care and Critical Care". Export default function RootLayout with children: React.ReactNode props. Return children directly — the [locale] layout below renders html and body.

    Wait — actually the root layout MUST render html and body (Next.js requires exactly one html element in the tree). Update approach: The root layout renders html lang="hi" and body. The [locale] layout overrides lang. Set the root layout to render html lang="hi" and body tags, and import the globals.css here.

    --- app/[locale]/layout.tsx ---
    Async function. Props type: children React.ReactNode, params Promise of { locale: string }. Await params to get locale. Check if locale is in routing.locales — call notFound() if not (import from next/navigation). Call getMessages() from next-intl/server. Return html element with lang={locale}, body, and NextIntlClientProvider wrapping children. Pass messages={messages} to NextIntlClientProvider. This is the pattern from RESEARCH.md Pattern 6. IMPORTANT: do NOT render a second html tag here if root layout already renders one — adjust so [locale]/layout.tsx returns a fragment or just body+provider wrapping children, and the root layout handles the outer html element. Use the Next.js App Router nested layout model correctly: root layout = html+body shell, [locale]/layout.tsx = provider wrapper. The html lang attribute should be set by the [locale] layout, not the root layout. Solution: root layout renders html (lang defaults to "hi") and body; [locale] layout uses generateStaticParams if needed and renders NextIntlClientProvider only.

    Actually — the correct App Router pattern for next-intl: root layout renders a minimal html+body, and [locale]/layout.tsx renders NextIntlClientProvider + sets the lang attribute on a wrapping element OR the root layout defers lang to a client component. The cleanest approach for next-intl with App Router is: root layout = `<html><body>{children}</body></html>` with no lang attr hard-coded; [locale]/layout.tsx = an async layout that calls `await params`, validates locale, gets messages, and returns `<NextIntlClientProvider messages={messages}>{children}</NextIntlClientProvider>`. The html lang attribute is set in the [locale] layout by using generateStaticParams and putting the html element there instead. Reference Pattern 6 in RESEARCH.md exactly — it shows [locale]/layout.tsx rendering the full `<html lang={locale}>` and `<body>` elements, with root app/layout.tsx being minimal or absent. Follow Pattern 6 exactly as written in RESEARCH.md.

    --- app/[locale]/(public)/page.tsx ---
    A simple server component. Import useTranslations from next-intl (or getTranslations for server). Render an h1 with the site.title translation and a p with site.tagline. Add a div with className="bg-blue-500 text-white p-4" to verify Tailwind CSS is working. This page is the visual smoke test for Tailwind v4.

    --- app/(portal)/layout.tsx ---
    Simple layout stub for the portal. English-only. Returns a div wrapping children. Add a comment: "Portal layout stub — auth guard added in Phase 4". No i18n, no NextIntlClientProvider.

    --- app/(portal)/login/page.tsx ---
    Placeholder page. Returns a div with "Portal Login — Coming in Phase 4" text and className="p-8".

    --- app/(portal)/dashboard/page.tsx ---
    Placeholder page. Returns a div with "Portal Dashboard — Coming in Phase 4" text and className="p-8".

    --- app/api/.gitkeep ---
    Empty file to track the api directory in git.

    --- lib/supabase/client.ts ---
    Import createBrowserClient from @supabase/ssr. Export function createClient() that returns createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!). This is Pattern 7 from RESEARCH.md.

    --- lib/supabase/server.ts ---
    Import createServerClient from @supabase/ssr. Import cookies from next/headers. Export async function createClient(). Inside: const cookieStore = await cookies(). Return createServerClient with NEXT_PUBLIC_SUPABASE_URL!, NEXT_PUBLIC_SUPABASE_ANON_KEY!, and a cookies object with getAll() returning cookieStore.getAll() and setAll(cookiesToSet) that iterates cookiesToSet calling cookieStore.set(name, value, options) wrapped in try/catch. Use only getAll/setAll — never the old get/set/remove API (Pitfall 2 in RESEARCH.md).

    --- lib/supabase/middleware.ts ---
    Import createServerClient from @supabase/ssr. Import NextRequest and NextResponse from next/server. Export async function updateSession(request: NextRequest). Create supabaseResponse = NextResponse.next({ request }). Create supabase client with getAll reading from request.cookies.getAll() and setAll writing to both request.cookies and a new NextResponse.next({ request }), then updating supabaseResponse. Call await supabase.auth.getUser() — not getSession() (Pitfall in RESEARCH.md Anti-Patterns). Return supabaseResponse.

    --- next.config.ts ---
    Replace the scaffolded version. Import NextConfig from next. Import createNextIntlPlugin from next-intl/plugin. const withNextIntl = createNextIntlPlugin('./i18n/request.ts'). const nextConfig: NextConfig = {}. Export default withNextIntl(nextConfig). This is Pattern 5 from RESEARCH.md.
  </action>
  <verify>
    <automated>powershell.exe -Command "Test-Path 'D:\Git Hub\Aatmaram Website\proxy.ts'" </automated>
    <automated>powershell.exe -Command "@('proxy.ts','app/[locale]/layout.tsx','app/[locale]/(public)/page.tsx','app/(portal)/layout.tsx','app/(portal)/login/page.tsx','app/(portal)/dashboard/page.tsx','i18n/routing.ts','i18n/request.ts','messages/en.json','messages/hi.json','lib/supabase/client.ts','lib/supabase/server.ts','lib/supabase/middleware.ts','next.config.ts') | ForEach-Object { if(!(Test-Path \"D:\Git Hub\Aatmaram Website\$_\")) { Write-Error \"Missing: $_\" } }; Write-Host 'all files present'"</automated>
    <automated>cd "D:\Git Hub\Aatmaram Website" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - proxy.ts exists at project root (not middleware.ts — verify with file system check)
    - proxy.ts contains the string "createMiddleware" and the matcher excludes "portal"
    - i18n/routing.ts contains "localePrefix: 'always'"
    - i18n/request.ts return statement includes "locale" property (grep for "locale," in the return object)
    - app/[locale]/layout.tsx contains "await params" (not params.locale without await)
    - app/[locale]/layout.tsx contains "NextIntlClientProvider"
    - messages/en.json and messages/hi.json both exist and are valid JSON (node -e "require('./messages/en.json')" exits 0)
    - lib/supabase/client.ts contains "createBrowserClient"
    - lib/supabase/server.ts contains "getAll" and "setAll" — does NOT contain ".get(" or ".set(" or ".remove(" in cookie methods
    - lib/supabase/middleware.ts contains "updateSession" and "getUser" — does NOT contain "getSession"
    - next.config.ts contains "withNextIntl"
    - npm run build exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>
    All required directories and files exist. TypeScript compiles clean. proxy.ts uses correct Next.js 16 filename with locale-excluding matcher. i18n/routing.ts specifies localePrefix: 'always'. i18n/request.ts returns locale in its config object. Supabase clients use getAll/setAll API exclusively.
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify the phase 1 plan 01 success state:

1. npm run build exits 0
2. All required paths exist: app/[locale]/(public)/, app/(portal)/, i18n/, messages/, lib/supabase/
3. proxy.ts is at project root (NOT middleware.ts)
4. app/globals.css contains @import "tailwindcss"
5. No NEXT_PUBLIC_ prefix on SUPABASE_SERVICE_ROLE_KEY or GEMINI_API_KEY in any file
</verification>

<success_criteria>
- npm run build exits 0 — TypeScript clean compile
- proxy.ts exists at root with createMiddleware and portal-excluding matcher
- app/[locale]/layout.tsx uses await params and NextIntlClientProvider
- All three routing zones exist as directories in app/
- Tailwind v4 processes — app/globals.css has @import "tailwindcss" and postcss.config.mjs uses @tailwindcss/postcss
- Supabase client files exist in lib/supabase/ using getAll/setAll cookie API only
</success_criteria>

<output>
Create `.planning/phases/01-project-setup/01-01-SUMMARY.md` when done
</output>

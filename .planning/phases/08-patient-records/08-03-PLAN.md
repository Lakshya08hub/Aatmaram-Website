---
phase: 08-patient-records
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - components/portal/PatientClient.tsx
  - app/(portal)/patients/page.tsx
autonomous: true
requirements: [PAT-01, PAT-02, PAT-03, PAT-04, PAT-05]
must_haves:
  truths:
    - "Receptionist sees all patient records, can add new record and edit all fields except clinical_notes"
    - "Doctor sees only their assigned patients, can edit clinical_notes only (all other fields read-only)"
    - "Admin and Super Admin see all records (same view as receptionist)"
    - "Client-side search filters by patient_name or phone instantly"
    - "Add Patient and Edit open a Sheet consistent with StaffClient.tsx pattern"
    - "Empty state shows appropriate message based on role"
  artifacts:
    - path: "components/portal/PatientClient.tsx"
      provides: "Full patient list + Sheet add/edit UI with role-aware field rendering"
      exports: ["PatientClient"]
    - path: "app/(portal)/patients/page.tsx"
      provides: "Server Component that fetches records + role, passes to PatientClient"
      exports: ["default (page)"]
  key_links:
    - from: "app/(portal)/patients/page.tsx"
      to: "getPatientRecords()"
      via: "server-side fetch, passes PatientRecord[] as prop"
      pattern: "getPatientRecords"
    - from: "PatientClient.tsx (doctor sheet)"
      to: "updateClinicalNotesAction"
      via: "useActionState hook"
      pattern: "updateClinicalNotesAction"
    - from: "PatientClient.tsx (receptionist sheet)"
      to: "createPatientAction / updatePatientAction"
      via: "useActionState hook"
      pattern: "createPatientAction|updatePatientAction"
---

<objective>
Build the PatientClient component and portal page — the complete patient records UI with role-aware field rendering.

Purpose: Delivers the visible, verifiable end of all five PAT requirements. After this plan, receptionists can create records, doctors can add clinical notes, and admins can view everything.
Output: components/portal/PatientClient.tsx (client component) + app/(portal)/patients/page.tsx (server component page).
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-patient-records/08-CONTEXT.md
@.planning/phases/08-patient-records/08-UI-SPEC.md
@.planning/phases/08-patient-records/08-02-SUMMARY.md

<interfaces>
<!-- Key types and action signatures the executor must use. -->

From lib/db/patient_records.ts (created in plan 08-02):
```typescript
export interface PatientRecord {
  id: string;
  patient_name: string;
  age: number;
  phone: string;
  reason: string;
  assigned_doctor_id: string | null;
  visit_date: string;
  clinical_notes: string | null;
  created_at: string;
  updated_at: string;
  assigned_doctor_name?: string | null;  // joined from doctors table
}

export function getPatientRecords(role: StaffRole, authUserId: string): Promise<PatientRecord[]>;
export function getActiveDoctorsForDropdown(): Promise<{ id: string; full_name: string }[]>;
```

From app/(portal)/actions/patients.ts (created in plan 08-02):
```typescript
export async function createPatientAction(prevState: unknown, formData: FormData): Promise<{ error?: string }>;
export async function updatePatientAction(id: string, prevState: unknown, formData: FormData): Promise<{ error?: string }>;
export async function updateClinicalNotesAction(id: string, prevState: unknown, formData: FormData): Promise<{ error?: string }>;
```

From app/(portal)/staff/StaffClient.tsx (pattern to mirror):
- Sheet from @/components/ui/sheet for add/edit
- useRouter().refresh() after successful action to re-fetch server data
- toast.success() / toast.error() via sonner
- client-side filter with useState on search string

From app/(portal)/appointments/page.tsx (server component + client component pattern):
- createClient() for auth session
- profiles.role lookup
- Pass data as props to client component
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Component page — fetch + role resolution</name>
  <files>app/(portal)/patients/page.tsx</files>
  <read_first>
    - app/(portal)/appointments/page.tsx — Server Component fetch + client component handoff pattern
    - app/(portal)/staff/StaffClient.tsx — top-level component props shape
    - lib/db/patient_records.ts — getPatientRecords, getActiveDoctorsForDropdown signatures
    - lib/supabase/server.ts — createClient() for session
  </read_first>
  <action>
    Create app/(portal)/patients/page.tsx as an async Server Component.

    Steps:
    1. createClient() to get session; call supabase.auth.getUser(). Redirect to /login if no user.
    2. Look up profile: supabase.from('profiles').select('role').eq('user_id', user.id).single(). Extract role as StaffRole.
    3. Call getPatientRecords(role, user.id) and getActiveDoctorsForDropdown() in parallel via Promise.all.
    4. Render PatientClient with props: records, doctors, role, currentUserId (= user.id).

    Page metadata: export const metadata = { title: 'Patient Records | Atmaram Portal' }.

    Import PatientClient from '@/components/portal/PatientClient'.
    Import getPatientRecords, getActiveDoctorsForDropdown from '@/lib/db/patient_records'.
    Import createClient from '@/lib/supabase/server'.
    Import redirect from 'next/navigation'.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "patients/page"</automated>
  </verify>
  <done>app/(portal)/patients/page.tsx compiles; server-side fetches role and records; passes typed props to PatientClient.</done>
</task>

<task type="auto">
  <name>Task 2: PatientClient component — table + Sheet + role-aware fields</name>
  <files>components/portal/PatientClient.tsx</files>
  <read_first>
    - app/(portal)/staff/StaffClient.tsx — FULL FILE — Sheet open/close pattern, useRouter refresh, toast, Table structure, form submit with useActionState
    - .planning/phases/08-patient-records/08-UI-SPEC.md — 2-panel layout, table columns, empty states, role-specific sheet fields
    - .planning/phases/08-patient-records/08-CONTEXT.md — D-10 (Sheet), D-12 (receptionist hides clinical_notes; doctor sees only clinical_notes editable)
  </read_first>
  <action>
    Create components/portal/PatientClient.tsx as a 'use client' component.

    Props interface PatientClientProps:
    - records: PatientRecord[]
    - doctors: { id: string; full_name: string }[]
    - role: StaffRole
    - currentUserId: string

    State:
    - search: string — filters records by patient_name or phone (case-insensitive, client-side)
    - sheetOpen: boolean
    - editingRecord: PatientRecord | null — null means "Add" mode
    - isPending: boolean from useTransition (for sheet submit button loading state)

    Layout (per UI-SPEC):
    - Page header: h1 "Patient Records" + "Add Patient" Button (variant="default" accent) — hidden for doctor role (doctors cannot create records)
    - Search Input below header with placeholder "Search by name or phone..." — filters filteredRecords = records.filter(r => r.patient_name.toLowerCase().includes(search.toLowerCase()) || r.phone.includes(search))
    - Table with columns: Patient (patient_name + age badge), Visit Date (visit_date formatted as DD MMM YYYY), Doctor (assigned_doctor_name ?? "Unassigned"), Reason (truncated to 40 chars), Actions (Pencil icon Button — always visible to all roles)

    Empty states:
    - role === 'doctor' && records.length === 0: "No patients assigned to you yet."
    - All other roles && records.length === 0: "No patient records yet." + "Add Patient" Button
    - filteredRecords.length === 0 && search !== '': "No records match your search."

    Sheet (single sheet for both Add and Edit, open state driven by sheetOpen):
    - SheetTitle: editingRecord ? "Edit Patient Record" : "Add Patient Record"

    Sheet content — RECEPTIONIST / ADMIN / SUPER_ADMIN (role !== 'doctor'):
    - Editable fields via hidden inputs + visible form controls: patient_name (Input), age (Input type="number"), phone (Input), reason (Textarea), visit_date (Input type="date"), assigned_doctor_id (Select populated from doctors prop — include an "Unassigned" option with value "")
    - clinical_notes field is NOT rendered at all (per D-12)
    - Submit calls createPatientAction (Add mode) or updatePatientAction bound with editingRecord.id (Edit mode) via useActionState
    - On success (state.error is undefined after transition): toast.success("Record saved"), setSheetOpen(false), router.refresh()
    - On error: toast.error(state.error)

    Sheet content — DOCTOR (role === 'doctor'):
    - All fields (patient_name, age, phone, reason, visit_date, assigned_doctor_name) rendered as read-only text (not inputs) — use dl/dt/dd or simple p tags with labels
    - clinical_notes rendered as editable Textarea with label "Clinical Notes"
    - Submit calls updateClinicalNotesAction bound with editingRecord!.id via useActionState
    - On success: toast.success("Clinical notes saved"), setSheetOpen(false), router.refresh()
    - On error: toast.error(state.error)

    Imports:
    - PatientRecord from '@/lib/db/patient_records'
    - StaffRole from '@/lib/portal/roles'
    - createPatientAction, updatePatientAction, updateClinicalNotesAction from '@/app/(portal)/actions/patients'
    - shadcn: Table, TableBody, TableCell, TableHead, TableHeader, TableRow, Sheet, SheetContent, SheetFooter, SheetHeader, SheetTitle, Select, SelectContent, SelectItem, SelectTrigger, SelectValue, Input, Button, Badge, Textarea
    - useRouter from 'next/navigation', useState, useActionState, useTransition from 'react'
    - toast from 'sonner'
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep "PatientClient"</automated>
  </verify>
  <done>PatientClient.tsx compiles without TypeScript errors; receptionist sheet hides clinical_notes; doctor sheet makes all fields except clinical_notes read-only; search filters by name and phone; empty states render correctly by role.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component → Server Action | FormData submitted from PatientClient crosses here |
| Doctor UI → updateClinicalNotesAction | Doctor can only see and edit their assigned patients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information Disclosure | PatientClient records prop | mitigate | Server Component passes only role-filtered records — doctor never receives other patients' data |
| T-08-06 | Elevation of Privilege | "Add Patient" button for doctor | mitigate | Button hidden in UI when role === 'doctor'; server action also rejects doctor role |
| T-08-07 | Tampering | client-side search | accept | Search is display-only filtering on already-fetched records; no security boundary crossed |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — all shadcn components already installed |
</threat_model>

<verification>
- npx tsc --noEmit passes with no errors across the full project
- npm run build completes without errors
- Visiting /patients as receptionist shows "Add Patient" button and full table
- Visiting /patients as doctor shows only assigned records with clinical_notes textarea in edit sheet
- Client-side search filters visible rows instantly
</verification>

<success_criteria>
All five PAT requirements verifiable: receptionist creates records (PAT-01), doctor sees only assigned patients (PAT-02), doctor edits clinical_notes (PAT-03), admin sees all (PAT-04), receptionist sees all (PAT-05). Role-scoped views enforced at both data layer and UI layer.
</success_criteria>

<output>
Create `.planning/phases/08-patient-records/08-03-SUMMARY.md` when done
</output>

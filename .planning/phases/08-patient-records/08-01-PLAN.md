---
phase: 08-patient-records
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260613_patient_records.sql
autonomous: false
requirements: [PAT-01, PAT-02, PAT-03, PAT-04, PAT-05]
must_haves:
  truths:
    - "patient_records table exists in Supabase with correct columns and FK"
    - "assigned_doctor_id FK uses ON DELETE SET NULL so deleting a doctor row does not cascade-delete visit records"
    - "All code in plans 08-02 and 08-03 can reference patient_records without a missing-table error"
  artifacts:
    - path: "supabase/migrations/20260613_patient_records.sql"
      provides: "DDL for patient_records table"
      contains: "CREATE TABLE patient_records"
  key_links:
    - from: "patient_records.assigned_doctor_id"
      to: "doctors.id"
      via: "FK ON DELETE SET NULL"
      pattern: "REFERENCES doctors\\(id\\) ON DELETE SET NULL"
---

<objective>
Create the patient_records database migration and push it to Supabase.

Purpose: The schema is the blocking dependency for all other plans in this phase. No application code can run against patient_records until this table exists in the database.
Output: Migration SQL file committed to repo + table live in Supabase (after manual push).
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-patient-records/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration SQL</name>
  <files>supabase/migrations/20260613_patient_records.sql</files>
  <read_first>
    - .planning/phases/08-patient-records/08-CONTEXT.md — D-01 through D-03 define the schema exactly
    - lib/db/doctors.ts — confirms doctors table name and that id is the PK referenced by the FK
  </read_first>
  <action>
    Create supabase/migrations/20260613_patient_records.sql with a single CREATE TABLE IF NOT EXISTS statement for patient_records.

    Columns (per D-02):
    - id uuid PRIMARY KEY DEFAULT gen_random_uuid()
    - patient_name text NOT NULL
    - age integer NOT NULL CHECK (age >= 0 AND age <= 150)
    - phone text NOT NULL
    - reason text NOT NULL
    - assigned_doctor_id uuid REFERENCES doctors(id) ON DELETE SET NULL — nullable (per D-03 + research finding 4)
    - visit_date date NOT NULL DEFAULT CURRENT_DATE
    - clinical_notes text — nullable (doctor fills this, per D-02)
    - created_at timestamptz NOT NULL DEFAULT now()
    - updated_at timestamptz NOT NULL DEFAULT now()

    After the CREATE TABLE, add a trigger that auto-updates updated_at on row update — use the moddatetime extension trigger pattern already present in the DB (CREATE TRIGGER set_updated_at BEFORE UPDATE ON patient_records FOR EACH ROW EXECUTE FUNCTION moddatetime(updated_at)).

    If moddatetime is not available, use: CREATE OR REPLACE FUNCTION update_patient_records_updated_at() RETURNS TRIGGER AS $$ BEGIN NEW.updated_at = now(); RETURN NEW; END; $$ LANGUAGE plpgsql;
    Then CREATE TRIGGER set_updated_at BEFORE UPDATE ON patient_records FOR EACH ROW EXECUTE FUNCTION update_patient_records_updated_at();

    Add a comment: -- Phase 8: Patient Records migration
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE" "D:/Git Hub/Aatmaram Website/supabase/migrations/20260613_patient_records.sql"</automated>
  </verify>
  <done>Migration file exists, contains CREATE TABLE patient_records with all 10 columns, FK ON DELETE SET NULL, and updated_at trigger.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>[BLOCKING] Task 2: Apply migration via Supabase Dashboard</name>
  <read_first>
    - supabase/migrations/20260613_patient_records.sql — the SQL to paste
  </read_first>
  <what-built>Migration SQL file is written and committed. It must now be applied to the live Supabase database before any application code in plans 08-02 and 08-03 can run.</what-built>
  <how-to-verify>
    1. Open Supabase Dashboard → your project → SQL Editor.
    2. Paste the full contents of supabase/migrations/20260613_patient_records.sql.
    3. Click "Run".
    4. Confirm the output shows no errors.
    5. Navigate to Table Editor — verify patient_records table appears with all columns.
  </how-to-verify>
  <resume-signal>Type "migration applied" once the table is visible in Supabase Table Editor.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → Supabase Postgres | DDL runs with service-role privileges — schema changes are permanent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | patient_records DDL | mitigate | FK ON DELETE SET NULL prevents orphan records; CHECK constraint on age prevents nonsense values |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- supabase/migrations/20260613_patient_records.sql exists and contains CREATE TABLE patient_records
- FK REFERENCES doctors(id) ON DELETE SET NULL is present
- Table is visible in Supabase Dashboard after manual push
</verification>

<success_criteria>
patient_records table live in Supabase with all columns from D-02, FK to doctors with ON DELETE SET NULL, and updated_at auto-trigger.
</success_criteria>

<output>
Create `.planning/phases/08-patient-records/08-01-SUMMARY.md` when done
</output>

---
phase: 03-bilingual-system
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - components/layout/LanguageToggle.tsx
  - components/layout/Header.tsx
  - components/layout/MobileNav.tsx
autonomous: true
requirements:
  - I18N-02
  - I18N-03
  - I18N-04
must_haves:
  truths:
    - "A visitor clicking HI or EN in the header navigates to the same page in the selected locale"
    - "The active locale label is bright white and non-clickable; the inactive locale label is muted and clickable"
    - "All nav links in Header and MobileNav prepend the current locale automatically"
    - "Logo link href='/' does not double-prefix to /hi/hi when on the Hindi locale"
    - "Mobile drawer shows the language toggle in the footer section below the CTA button"
    - "Toggle is keyboard-accessible: inactive locale span has role=button, tabIndex=0, and Enter handler"
  artifacts:
    - path: "components/layout/LanguageToggle.tsx"
      provides: "Language toggle client component with nav and drawer variants"
      contains: "'use client'"
      exports: ["LanguageToggle"]
    - path: "components/layout/Header.tsx"
      provides: "Header with locale-aware navigation and functional toggle"
      contains: "from '@/i18n/navigation'"
    - path: "components/layout/MobileNav.tsx"
      provides: "Mobile drawer with locale-aware navigation and toggle"
      contains: "LanguageToggle"
  key_links:
    - from: "components/layout/LanguageToggle.tsx"
      to: "i18n/navigation.ts"
      via: "import { useRouter, usePathname } from '@/i18n/navigation'"
      pattern: "@/i18n/navigation"
    - from: "components/layout/Header.tsx"
      to: "components/layout/LanguageToggle.tsx"
      via: "import { LanguageToggle } from './LanguageToggle'"
      pattern: "LanguageToggle"
    - from: "components/layout/MobileNav.tsx"
      to: "i18n/navigation.ts"
      via: "import { Link } from '@/i18n/navigation'"
      pattern: "@/i18n/navigation"
---

<objective>
Create the LanguageToggle client component and migrate Header.tsx and MobileNav.tsx to use locale-aware navigation. This plan makes the language toggle functional and fixes all nav links to respect the current locale.

Purpose: This is the interactive layer of Phase 3. After Plan 01 created i18n/navigation.ts, this plan wires it into the UI. A visitor clicking EN or HI will navigate to the same page in the other locale, with the NEXT_LOCALE cookie persisted by middleware automatically.

Output: Three files — LanguageToggle.tsx (new), Header.tsx (modified), MobileNav.tsx (modified).
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-bilingual-system/03-CONTEXT.md
@.planning/phases/03-bilingual-system/03-RESEARCH.md
@.planning/phases/03-bilingual-system/03-PATTERNS.md
@.planning/phases/03-bilingual-system/03-UI-SPEC.md
@.planning/phases/03-bilingual-system/03-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 output and existing codebase. Read before implementing. -->

From i18n/navigation.ts (created in Plan 01):
```typescript
// Exports: Link, useRouter, usePathname, redirect, permanentRedirect, getPathname
// All derived from createNavigation(routing)
// Import path for components: '@/i18n/navigation'
```

From components/layout/Header.tsx — current state (lines to modify):
```typescript
// Line 1 (comment) + Line 2: import Link from 'next/link';  ← REPLACE with locale-aware Link
// Line 25: href="/hi"  ← REPLACE with href="/"
// Lines 49-55: static <span> EN/HI placeholder  ← REPLACE with <LanguageToggle />
```

From components/layout/MobileNav.tsx — current state (line to modify):
```typescript
// Line 3: import Link from 'next/link';  ← REPLACE with Link from '@/i18n/navigation'
// SheetClose render-prop pattern (lines 42-52) — PRESERVE this structure exactly; only swap the Link import
// Add <LanguageToggle /> in the drawer footer section (below Book Appointment CTA)
```

LanguageToggle visual spec (from UI-SPEC.md):
  Desktop (nav background #1E3A5F):
    Active locale: text-white (bright, non-clickable, no tabIndex, no role)
    Inactive locale: text-white/50 cursor-pointer hover:text-white/80 transition-colors
    Separator: text-white/30
    Container: flex items-center gap-1 text-sm font-medium
  Mobile (drawer background white):
    Active locale: text-slate-700
    Inactive locale: text-slate-400 cursor-pointer hover:text-slate-600 transition-colors
    Separator: text-slate-300
    Container: flex items-center justify-center gap-2 py-3 min-h-[44px]
  The component accepts optional variant prop: "nav" (default) | "drawer"

LanguageToggle accessibility spec (from UI-SPEC.md):
  aria-label on container div: locale=hi → "भाषा बदलें", locale=en → "Switch language"
  aria-label on inactive span: switching to hi → "हिंदी में देखें", switching to en → "Switch to English"
  Inactive span: role="button", tabIndex={0}, onKeyDown Enter handler

Header nav labels (currently hardcoded English — convert to translations):
  Header is a Server Component. Use getTranslations from 'next-intl/server' to get t().
  Nav labels use namespace 'nav': t('nav.home'), t('nav.about'), t('nav.departments'), t('nav.doctors'), t('nav.services'), t('nav.contact'), t('nav.cta')
  Header function must become async to call getTranslations (async function Header()).
  Use getLocale() from 'next-intl/server' to get current locale for getTranslations.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LanguageToggle client component</name>
  <files>components/layout/LanguageToggle.tsx</files>
  <read_first>
    - components/layout/MobileNav.tsx — read lines 1-15 for the 'use client' + named export pattern to replicate
    - components/ui/sonner.tsx — read for the hook-driven render pattern (useTheme → conditional render)
    - i18n/navigation.ts — confirm the file exists and useRouter/usePathname are exported from it
  </read_first>
  <action>
    Create components/layout/LanguageToggle.tsx. This is a new file — it must not exist yet.

    First line must be: 'use client'

    Imports:
    - useLocale from 'next-intl'
    - useRouter, usePathname from '@/i18n/navigation' (NOT from 'next/navigation' or 'next-intl/navigation')

    Component: export function LanguageToggle({ variant = 'nav' }: { variant?: 'nav' | 'drawer' })

    Logic:
    - const locale = useLocale()
    - const router = useRouter()
    - const pathname = usePathname()  — returns locale-stripped path (e.g. '/about', not '/hi/about')
    - const otherLocale = locale === 'hi' ? 'en' : 'hi'
    - function handleSwitch() { router.push(pathname, { locale: otherLocale }) }
    - The router.push with locale option also calls syncLocaleCookie internally — no custom cookie code needed

    Rendering logic — two variants:

    variant === 'nav' (desktop, on #1E3A5F navy background):
    - Container: div with className="flex items-center gap-1 text-sm font-medium" and aria-label (current locale hi → "भाषा बदलें", en → "Switch language")
    - HI span: active when locale==='hi' → className="text-white", no onClick, no role, no tabIndex
      inactive when locale!=='hi' → className="text-white/50 cursor-pointer hover:text-white/80 transition-colors", onClick=handleSwitch, role="button", tabIndex={0}, onKeyDown={(e)=>e.key==='Enter'&&handleSwitch()}, aria-label="हिंदी में देखें"
    - Separator span: className="text-white/30" content "|"
    - EN span: active when locale==='en' → className="text-white", no onClick, no role, no tabIndex
      inactive when locale!=='en' → className="text-white/50 cursor-pointer hover:text-white/80 transition-colors", onClick=handleSwitch, role="button", tabIndex={0}, onKeyDown={(e)=>e.key==='Enter'&&handleSwitch()}, aria-label="Switch to English"

    variant === 'drawer' (mobile, on white drawer background):
    - Container: div with className="flex items-center justify-center gap-2 text-sm font-medium py-3 min-h-[44px]" and same aria-label logic
    - HI span: active when locale==='hi' → className="text-slate-700"
      inactive when locale!=='hi' → className="text-slate-400 cursor-pointer hover:text-slate-600 transition-colors", onClick=handleSwitch, role="button", tabIndex={0}, onKeyDown handler, aria-label="हिंदी में देखें"
    - Separator span: className="text-slate-300" content "|"
    - EN span: active when locale==='en' → className="text-slate-700"
      inactive when locale!=='en' → className="text-slate-400 cursor-pointer hover:text-slate-600 transition-colors", onClick=handleSwitch, role="button", tabIndex={0}, onKeyDown handler, aria-label="Switch to English"

    Use named export (export function LanguageToggle), not default export. This matches the MobileNav.tsx pattern.
  </action>
  <verify>
    <automated>cd "D:\Git Hub\Aatmaram Website" && node -e "const f=require('fs'); const c=f.readFileSync('components/layout/LanguageToggle.tsx','utf8'); const checks=['use client','useLocale','@/i18n/navigation','useRouter','usePathname','handleSwitch','role=\"button\"','tabIndex','onKeyDown','LanguageToggle','variant']; checks.forEach(s=>{if(!c.includes(s))throw new Error('Missing: '+s);}); console.log('OK: all required patterns present');"</automated>
  </verify>
  <acceptance_criteria>
    - File components/layout/LanguageToggle.tsx exists
    - First line is 'use client'
    - Imports useLocale from 'next-intl' (not from 'use-intl' directly)
    - Imports useRouter and usePathname from '@/i18n/navigation' (not from 'next/navigation')
    - Exports named function LanguageToggle (not default)
    - Component accepts variant prop with type 'nav' | 'drawer', defaults to 'nav'
    - router.push(pathname, { locale: otherLocale }) is the navigation call
    - Inactive locale span has role="button", tabIndex={0}, onKeyDown with Enter check
    - Active locale span has no role, no tabIndex, no onClick
    - Container div has aria-label that changes based on current locale
    - Desktop (nav) variant uses text-white/50 for inactive, text-white for active
    - Drawer variant uses text-slate-400 for inactive, text-slate-700 for active
  </acceptance_criteria>
  <done>LanguageToggle.tsx exists with nav and drawer variants, keyboard accessibility, correct aria-labels, and router.push locale switching</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Header.tsx and MobileNav.tsx to locale-aware navigation</name>
  <files>components/layout/Header.tsx, components/layout/MobileNav.tsx</files>
  <read_first>
    - components/layout/Header.tsx — read the COMPLETE file. Note: line 1 (TODO comment), line 2 (import Link from 'next/link'), line 25 (href="/hi" on logo link), lines 49-55 (static EN/HI span placeholder). Also note current nav labels (hardcoded English strings in navLinks array).
    - components/layout/MobileNav.tsx — read the COMPLETE file. Note: line 3 (import Link from 'next/link'), lines 42-52 (SheetClose render-prop pattern — preserve exactly), and the footer section where LanguageToggle will be added.
    - components/layout/LanguageToggle.tsx — confirm it exists from Task 1 before modifying headers
  </read_first>
  <action>
    Modify Header.tsx — make three targeted changes:

    Change 1: Replace the import block at the top. Remove: import Link from 'next/link' and the TODO comment on line 1. Add:
      import { Link } from '@/i18n/navigation';
      import { LanguageToggle } from './LanguageToggle';
      import { getTranslations } from 'next-intl/server';
      import { getLocale } from 'next-intl/server';

    Change 2: Convert Header to async Server Component and wire getTranslations. The Header function should become:
      export async function Header() {
        const locale = await getLocale();
        const t = await getTranslations({ locale, namespace: 'nav' });
        ...
      }
    Update navLinks array to use t() calls:
      const navLinks = [
        { label: t('home'), href: '/' },
        { label: t('about'), href: '/about' },
        { label: t('departments'), href: '/departments' },
        { label: t('doctors'), href: '/doctors' },
        { label: t('services'), href: '/services' },
        { label: t('contact'), href: '/contact' },
      ];
    The Book Appointment CTA button label: use t('cta').

    Change 3: In the logo Link, change href="/hi" to href="/". The locale-aware Link from @/i18n/navigation prepends the current locale automatically — href="/" becomes /hi/ or /en/ as appropriate.

    Change 4: In the right-side action area (lines 49-55 currently), replace the entire <span> block that has "EN/HI" with: <LanguageToggle />

    Modify MobileNav.tsx — make three targeted changes:

    Change 1: Replace import Link from 'next/link' (line 3) with:
      import { Link } from '@/i18n/navigation';
      import { LanguageToggle } from './LanguageToggle';

    Change 2: The SheetClose render-prop pattern for nav links (lines 42-52 area) — keep the pattern exactly as-is. Only the Link import changes; the render prop structure, href values, and className strings are unchanged. Do not restructure this section.

    Change 3: In the drawer footer section (the div with "p-4 border-t border-slate-100" or similar containing the Book Appointment button), add a LanguageToggle with variant="drawer" above or below the CTA:
      <div className="flex justify-center">
        <LanguageToggle variant="drawer" />
      </div>
    Place it ABOVE the Book Appointment SheetClose link inside the border-t footer div.

    IMPORTANT anti-patterns to avoid (per RESEARCH.md):
    - Do NOT use `import Link from 'next/link'` anywhere in either file after migration
    - Do NOT construct locale-prefixed hrefs manually (no `href={`/${locale}/about`}`)
    - Do NOT use `useRouter` from 'next/navigation' — only from '@/i18n/navigation'
    - Do NOT add `href="/hi/..."` or `href="/en/..."` to any nav link — locale-aware Link handles this
  </action>
  <verify>
    <automated>cd "D:\Git Hub\Aatmaram Website" && node -e "const f=require('fs'); const h=f.readFileSync('components/layout/Header.tsx','utf8'); const m=f.readFileSync('components/layout/MobileNav.tsx','utf8'); if(h.includes(\"import Link from 'next/link'\"))throw new Error('Header still uses next/link'); if(m.includes(\"import Link from 'next/link'\"))throw new Error('MobileNav still uses next/link'); if(!h.includes(\"@/i18n/navigation\"))throw new Error('Header missing i18n/navigation import'); if(!m.includes(\"@/i18n/navigation\"))throw new Error('MobileNav missing i18n/navigation import'); if(!h.includes('LanguageToggle'))throw new Error('Header missing LanguageToggle'); if(!m.includes('LanguageToggle'))throw new Error('MobileNav missing LanguageToggle'); if(h.includes('href=\"/hi\"'))throw new Error('Header still has hardcoded /hi href'); console.log('OK: both files migrated correctly');"</automated>
  </verify>
  <acceptance_criteria>
    - Header.tsx contains import { Link } from '@/i18n/navigation' (not import Link from 'next/link')
    - Header.tsx contains import { LanguageToggle } from './LanguageToggle'
    - Header.tsx contains import { getTranslations } from 'next-intl/server'
    - Header.tsx export function Header() is async
    - Header.tsx navLinks array uses t('home'), t('about'), etc. from getTranslations — not hardcoded English strings
    - Header.tsx logo link has href="/" (not href="/hi")
    - Header.tsx renders <LanguageToggle /> where the static EN/HI span was (lines 49-55 area)
    - MobileNav.tsx contains import { Link } from '@/i18n/navigation' (not import Link from 'next/link')
    - MobileNav.tsx contains import { LanguageToggle } from './LanguageToggle'
    - MobileNav.tsx renders <LanguageToggle variant="drawer" /> in the drawer footer section
    - SheetClose render-prop structure in MobileNav.tsx is preserved exactly (only Link import changed)
    - No file contains href="/hi/..." or href="/en/..." manual locale prefixes
    - npm run dev starts without TypeScript or import errors
  </acceptance_criteria>
  <done>Header and MobileNav use locale-aware Link from @/i18n/navigation; logo link fixed; LanguageToggle rendered in both desktop header and mobile drawer; nav labels translated via getTranslations</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client component / user interaction | LanguageToggle receives user click; locale value constrained to ['hi', 'en'] by routing config — no injection surface |
| NEXT_LOCALE cookie | Written by next-intl middleware and syncLocaleCookie; value is always one of the two valid locales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | router.push locale option | accept | next-intl constrains locale to routing.locales = ['hi', 'en']; arbitrary locale values are ignored by middleware and redirect to defaultLocale |
| T-03-04 | Spoofing | NEXT_LOCALE cookie | accept | Cookie carries no authentication data — only 'hi' or 'en' string; SameSite: Lax prevents CSRF; value has no privilege |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks complete:
1. Run: npm run dev — server starts without errors
2. Run: npx tsc --noEmit — zero TypeScript errors across all three modified files
3. Visit localhost:3000 — redirects to /hi/ (default locale)
4. Header shows HI (bright) | EN (muted) in the nav bar
5. Click EN — navigates to /en/, header now shows HI (muted) | EN (bright)
6. Navigate to /en/about — verify URL is /en/about not /en/hi/about
7. Reload — stays on /en/ (NEXT_LOCALE cookie set)
8. Open mobile menu — LanguageToggle visible in drawer footer
9. Tab to toggle on desktop — inactive locale span is focusable (tabIndex=0), Enter key triggers switch
</verification>

<success_criteria>
- LanguageToggle.tsx exists, is 'use client', accepts variant prop, and uses @/i18n/navigation
- Header.tsx imports Link from @/i18n/navigation; logo href is "/"; renders LanguageToggle; nav labels use getTranslations
- MobileNav.tsx imports Link from @/i18n/navigation; renders LanguageToggle variant="drawer"; SheetClose pattern preserved
- Clicking toggle switches locale in URL correctly (/hi/about ↔ /en/about)
- No manual locale-prefixed hrefs in any modified file
- TypeScript reports no errors
</success_criteria>

<output>
Create .planning/phases/03-bilingual-system/03-03-SUMMARY.md when done
</output>

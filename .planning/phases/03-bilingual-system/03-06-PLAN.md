---
phase: 03-bilingual-system
plan: 06
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - lib/data/departments.ts
  - lib/data/doctors.ts
  - components/public/DepartmentCard.tsx
  - components/public/DoctorCard.tsx
  - app/[locale]/(public)/departments/page.tsx
  - app/[locale]/(public)/doctors/page.tsx
autonomous: true
requirements:
  - I18N-05
must_haves:
  truths:
    - "Visiting /hi/departments shows all 8 department names and descriptions in Hindi"
    - "Visiting /hi/doctors shows the page heading, subtitle, specialties, Book Appointment button, and Contact CTA in Hindi"
    - "Department and doctor cards render translated text, not the English literals from the seeded constants"
    - "Doctor proper names (Dr. Rajesh Sharma, etc.) stay in English; their specialty labels render in Hindi"
  artifacts:
    - path: "lib/data/departments.ts"
      provides: "Department constant carrying a stable i18n key instead of English name/description"
      contains: "translationKey"
    - path: "components/public/DepartmentCard.tsx"
      provides: "Card resolving department name/description via translation key"
      contains: "useTranslations"
    - path: "app/[locale]/(public)/doctors/page.tsx"
      provides: "Doctors page rendering all body strings via translations"
      contains: "getTranslations"
  key_links:
    - from: "components/public/DepartmentCard.tsx"
      to: "messages/hi.json departments namespace"
      via: "useTranslations('departments') resolving department.translationKey"
      pattern: "useTranslations\\('departments'\\)"
    - from: "app/[locale]/(public)/doctors/page.tsx"
      to: "messages/hi.json doctors + departments namespaces"
      via: "getTranslations consumed in JSX and passed to DoctorCard"
      pattern: "getTranslations"
---

<objective>
Migrate the Departments page and Doctors page (and their card components) to render translated text. These pages currently map over English-only seeded constants (lib/data/departments.ts, lib/data/doctors.ts) and render hardcoded page headings, so /hi/departments and /hi/doctors display fully English bodies.

Purpose: Closes I18N-05 for the two list pages. The department/doctor names and descriptions are already translated in hi.json (departments namespace), but the cards render raw English from the constants. This wires the stable IDs in the constants to the existing translation keys.

Output: departments.ts and doctors.ts carry stable translationKey fields; DepartmentCard and DoctorCard resolve those keys via useTranslations; both page bodies use getTranslations for headings/subtitles/CTAs.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-bilingual-system/03-CONTEXT.md
@.planning/phases/03-bilingual-system/03-04-SUMMARY.md

<interfaces>
<!-- departments namespace ALREADY translated in hi.json (Plan 02). Keys use camelCase IDs. -->
<!-- Read messages/en.json lines 76-118 for exact key paths before implementing. -->

departments namespace (en.json lines 76-111):
  departments.pageTitle = "Our Departments"
  departments.pageSubtitle = "Expert care across 8 medical specialties"
  departments.paediatrics.{name,description}
  departments.paediatricSurgery.{name,description}
  departments.criticalCare.{name,description}
  departments.generalSurgery.{name,description}
  departments.orthopaedics.{name,description}
  departments.obstetrics.{name,description}
  departments.generalMedicine.{name,description}
  departments.emergency.{name,description}

doctors namespace (en.json lines 112-118):
  doctors.pageTitle = "Our Doctors"
  doctors.pageSubtitle = "Our team of specialists is here to help."
  doctors.cta = "Have questions? Contact us →"
  doctors.bookAppointment = "Book Appointment"
  doctors.comingSoon = "Doctor profiles coming soon."

Current seeded constants (lib/data/departments.ts) use id values:
  'paediatrics','paediatric-surgery','critical-care','general-surgery',
  'orthopaedics','obstetrics','general-medicine','emergency'
These map to the camelCase translation keys above. Add an explicit translationKey field to avoid fragile id-transform logic.

doctor constants (lib/data/doctors.ts) have specialty strings that match department names. Map each doctor.specialty to a department translationKey so specialties render in Hindi. Doctor.name and Doctor.initials STAY as-is (proper nouns per D-01).

DepartmentCard currently receives `department` prop and renders department.name + department.description directly. After migration it must resolve them via t(`${department.translationKey}.name`) using useTranslations('departments'). DepartmentCard is a Server Component-compatible presentational component — but useTranslations works in client and (via getTranslations) in server. Simplest: make DepartmentCard a client component ('use client') using useTranslations('departments'), OR keep it server and pass already-resolved strings from the page. CHOOSE: pass resolved strings from the page (page already calls getTranslations) to keep cards as pure presentational props. See action for the chosen approach.

DoctorCard renders doctor.name, doctor.specialty, and a hardcoded "Book Appointment" link.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add translationKey to seeded constants and resolve in cards via props</name>
  <files>lib/data/departments.ts, lib/data/doctors.ts, components/public/DepartmentCard.tsx, components/public/DoctorCard.tsx</files>
  <read_first>
    - lib/data/departments.ts — read the COMPLETE file. Note each department's id and that name/description are English literals to be replaced by a translationKey reference.
    - lib/data/doctors.ts — read the COMPLETE file. Note specialty strings map to department names.
    - components/public/DepartmentCard.tsx — read COMPLETE. It renders department.name and department.description.
    - components/public/DoctorCard.tsx — read COMPLETE. It renders doctor.name, doctor.specialty, and a hardcoded "Book Appointment" link.
    - messages/en.json lines 76-118 — confirm exact camelCase department keys and doctors keys.
  </read_first>
  <action>
    Approach: keep cards as pure presentational components that receive already-translated strings as props. The pages (Task 2) call getTranslations and pass resolved strings down. This avoids making cards client components and keeps the existing Server Component render path.

    Step 1 — lib/data/departments.ts:
    Add a `translationKey: string` field to the Department interface and to each entry, mapping id → camelCase namespace key:
      paediatrics → 'paediatrics'
      paediatric-surgery → 'paediatricSurgery'
      critical-care → 'criticalCare'
      general-surgery → 'generalSurgery'
      orthopaedics → 'orthopaedics'
      obstetrics → 'obstetrics'
      general-medicine → 'generalMedicine'
      emergency → 'emergency'
    Keep the existing name/description fields as English fallback (do NOT delete them — they serve as the en source and a safety fallback), and keep the icon field. Just ADD translationKey.

    Step 2 — lib/data/doctors.ts:
    Add a `specialtyKey: string` field to the Doctor interface and each entry, mapping the specialty to the matching department translationKey:
      'Paediatrics & Neonatology' → 'paediatrics'
      'Paediatric Surgery' → 'paediatricSurgery'
      'Critical Care & ICU' → 'criticalCare'
      'Obstetrics & Gynaecology' → 'obstetrics'
      'Orthopaedics' → 'orthopaedics'
      'General Medicine' → 'generalMedicine'
    Keep name, initials, specialty (English fallback) fields.

    Step 3 — components/public/DepartmentCard.tsx:
    Change the props so the card receives resolved strings. Update DepartmentCardProps to:
      { name: string; description: string; icon: LucideIcon }
    OR keep `department` plus add optional `name?`/`description?` overrides. CHOOSE the cleaner option: change props to { name, description, icon } and have the page pass t(`${dept.translationKey}.name`) etc. Update the JSX to render the new `name`/`description` props. Keep all className and layout unchanged.

    Step 4 — components/public/DoctorCard.tsx:
    Change props to receive resolved specialty label and the Book Appointment label:
      { name: string; initials: string; specialty: string; bookLabel: string }
    Render doctor name (proper noun, English), initials, the translated specialty, and use bookLabel for the Book Appointment link text instead of the hardcoded "Book Appointment". Keep the Link href="/appointment" and all styling. Note: this Link is from 'next/link' currently — replace with the locale-aware Link from '@/i18n/navigation' so the appointment link is locale-prefixed (consistency with Plan 03 migration).
  </action>
  <verify>
    <automated>cd "D:\Git Hub\Aatmaram Website" && node -e "const f=require('fs');const dep=f.readFileSync('lib/data/departments.ts','utf8');const doc=f.readFileSync('lib/data/doctors.ts','utf8');const dc=f.readFileSync('components/public/DepartmentCard.tsx','utf8');const drc=f.readFileSync('components/public/DoctorCard.tsx','utf8');if(!dep.includes('translationKey'))throw new Error('departments.ts missing translationKey');if(!dep.includes('criticalCare'))throw new Error('departments.ts missing criticalCare key');if(!doc.includes('specialtyKey'))throw new Error('doctors.ts missing specialtyKey');if(dc.includes('department.name'))throw new Error('DepartmentCard still reads department.name directly');if(drc.includes(\"'Book Appointment'\")||drc.includes('>Book Appointment<'))throw new Error('DoctorCard still hardcodes Book Appointment');if(!drc.includes('@/i18n/navigation'))throw new Error('DoctorCard not using locale-aware Link');console.log('OK: constants keyed, cards take resolved props');"</automated>
  </verify>
  <acceptance_criteria>
    - departments.ts Department interface has translationKey; all 8 entries set it to correct camelCase key
    - doctors.ts Doctor interface has specialtyKey; all entries set it to the matching department key
    - DepartmentCard receives resolved name/description/icon props (no longer reads department.name/description directly)
    - DoctorCard receives resolved specialty + bookLabel props; no hardcoded "Book Appointment" literal
    - DoctorCard imports Link from '@/i18n/navigation' (not 'next/link')
    - Existing English name/description/specialty fields retained as fallback (not deleted)
    - TypeScript compiles (npx tsc --noEmit clean for these files)
  </acceptance_criteria>
  <done>Seeded constants carry stable translation keys; both cards are pure presentational components taking resolved strings; DoctorCard uses locale-aware Link</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate Departments and Doctors pages to pass translated strings into cards</name>
  <files>app/[locale]/(public)/departments/page.tsx, app/[locale]/(public)/doctors/page.tsx</files>
  <read_first>
    - app/[locale]/(public)/departments/page.tsx — read COMPLETE. Note: breadcrumb "Home › Departments", h1 "Our Departments", SectionHeading title "Our Departments" + subtitle, the departments.map rendering DepartmentCard.
    - app/[locale]/(public)/doctors/page.tsx — read COMPLETE. Note: breadcrumb "Home › Our Doctors", h1 "Our Doctors", SectionHeading title + subtitle, doctors.map rendering DoctorCard, "Have questions? Contact us →" CTA link.
    - components/public/DepartmentCard.tsx and DoctorCard.tsx — confirm the new prop shapes from Task 1.
    - messages/en.json lines 76-118 — confirm key paths.
  </read_first>
  <action>
    Both pages already call setRequestLocale(locale) and getTranslations (departments page calls getTranslations('departments') but discards it; doctors page calls getTranslations('doctors') but discards it). Wire them in.

    departments/page.tsx:
    1. const t = await getTranslations('departments'); const tNav = await getTranslations('nav');
    2. Breadcrumb "Home › Departments" → {tNav('home')} › {t('pageTitle')} (keep › literal).
    3. h1 "Our Departments" → {t('pageTitle')}.
    4. SectionHeading title → {t('pageTitle')}; subtitle → {t('pageSubtitle')}.
    5. In departments.map, pass resolved strings to DepartmentCard:
         <DepartmentCard key={dept.id} icon={dept.icon}
           name={t(`${dept.translationKey}.name`)}
           description={t(`${dept.translationKey}.description`)} />

    doctors/page.tsx:
    1. const t = await getTranslations('doctors'); const tDept = await getTranslations('departments'); const tNav = await getTranslations('nav');
    2. Breadcrumb "Home › Our Doctors" → {tNav('home')} › {t('pageTitle')}.
    3. h1 "Our Doctors" → {t('pageTitle')}.
    4. SectionHeading title → {t('pageTitle')}; subtitle → {t('pageSubtitle')}.
    5. In doctors.map, pass resolved strings to DoctorCard:
         <DoctorCard key={doc.id} name={doc.name} initials={doc.initials}
           specialty={tDept(`${doc.specialtyKey}.name`)}
           bookLabel={t('bookAppointment')} />
    6. CTA link "Have questions? Contact us →" → {t('cta')} (en.json doctors.cta already includes the → arrow; remove the duplicate literal arrow in JSX so it is not doubled).

    Do NOT leave any English literal text node. Numerals and the › separator glyph are the only allowed literals.
  </action>
  <verify>
    <automated>cd "D:\Git Hub\Aatmaram Website" && node -e "const f=require('fs');const dep=f.readFileSync('app/[locale]/(public)/departments/page.tsx','utf8');const doc=f.readFileSync('app/[locale]/(public)/doctors/page.tsx','utf8');if(!dep.includes('translationKey'))throw new Error('departments page not passing translationKey to card');if(dep.includes('Our Departments'))throw new Error('departments page still has hardcoded \"Our Departments\"');if(dep.includes('Expert care across 8'))throw new Error('departments page still hardcodes subtitle');if(!doc.includes('specialtyKey'))throw new Error('doctors page not passing specialtyKey');if(doc.includes('Have questions? Contact us'))throw new Error('doctors page still hardcodes CTA');if(doc.includes('Our Doctors')&&!doc.includes(\"getTranslations('doctors')\"))throw new Error('doctors page still hardcodes heading');console.log('OK: both list pages migrated');"</automated>
  </verify>
  <acceptance_criteria>
    - departments/page.tsx consumes getTranslations('departments') + ('nav') in JSX
    - departments/page.tsx passes name/description resolved via t(`${dept.translationKey}...`) into DepartmentCard
    - "Our Departments" and "Expert care across 8 medical specialties" no longer appear as literals in departments/page.tsx
    - doctors/page.tsx consumes getTranslations('doctors') + ('departments') + ('nav')
    - doctors/page.tsx passes specialty via tDept(`${doc.specialtyKey}.name`) and bookLabel via t('bookAppointment')
    - "Have questions? Contact us" no longer appears as a literal in doctors/page.tsx
    - Doctor proper names (Dr. ...) still render (passed as doc.name)
    - npm run build exits 0 with no missing-key warnings
  </acceptance_criteria>
  <done>Departments and Doctors pages render all headings, subtitles, CTAs, card names, descriptions, and specialties from translations; /hi shows Hindi, /en shows English; doctor names stay English</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static seeded constants | lib/data/*.ts are build-time data; no user input |
| Server Component render | getTranslations server-side; locale constrained to ['hi','en'] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | lib/data constants | accept | Source-controlled static arrays; no runtime mutation |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
1. npm run build — exits 0, no missing-key warnings
2. Visit /hi/departments — all 8 department names + descriptions in Hindi, heading + subtitle in Hindi
3. Visit /en/departments — same in English
4. Visit /hi/doctors — heading, subtitle, specialties, Book Appointment buttons, Contact CTA in Hindi; Dr. names stay English
5. Toggle locale on each — content switches correctly
</verification>

<success_criteria>
- Department and doctor list pages fully bilingual
- Card components render resolved translated strings, not seeded English
- Doctor proper names preserved in English per D-01
- Build clean, no missing-key warnings
</success_criteria>

<output>
Create .planning/phases/03-bilingual-system/03-06-SUMMARY.md when done
</output>

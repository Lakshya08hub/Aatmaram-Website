---
phase: 03-bilingual-system
plan: 05
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - messages/en.json
  - messages/hi.json
  - app/[locale]/(public)/page.tsx
  - app/[locale]/(public)/about/page.tsx
autonomous: true
requirements:
  - I18N-05
must_haves:
  truths:
    - "Visiting /hi shows the Home page hero, trust-band labels, section headings, and CTA band in Hindi"
    - "Visiting /en shows the same Home page surfaces in English"
    - "Visiting /hi/about shows the About page header, story body, stat labels, mission, and the three value cards in Hindi"
    - "No hardcoded English literal remains in page.tsx or about/page.tsx — every visible string flows through t()"
  artifacts:
    - path: "app/[locale]/(public)/page.tsx"
      provides: "Home page rendering all body strings via useTranslations/getTranslations"
      contains: "getTranslations"
    - path: "app/[locale]/(public)/about/page.tsx"
      provides: "About page rendering all body strings via translations"
      contains: "getTranslations"
  key_links:
    - from: "app/[locale]/(public)/page.tsx"
      to: "messages/hi.json home namespace"
      via: "getTranslations('home') consumed in JSX"
      pattern: "t\\('hero"
    - from: "app/[locale]/(public)/about/page.tsx"
      to: "messages/hi.json about namespace"
      via: "getTranslations('about') consumed in JSX"
      pattern: "t\\('intro"
---

<objective>
Migrate the Home page and About page bodies to consume the existing translation keys. These two pages currently render hardcoded English literals even on /hi, despite the home and about namespaces already being fully translated in hi.json.

Purpose: This closes the core gap in I18N-05 for the two highest-traffic pages. The 03-04 verification passed only because it checked the navbar and form fields — the page bodies were never wired to translations. ROADMAP Phase 3 Success Criterion 3 requires every heading, body, label, and CTA on every page to have an EN and HI version that actually renders.

Output: page.tsx and about/page.tsx fully migrated to t() calls. messages/en.json and messages/hi.json get one added key each (the home secondary-link arrow text and the trust-band stat labels are already present; no new keys expected for these two pages — verify against en.json before adding any).
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-bilingual-system/03-CONTEXT.md
@.planning/phases/03-bilingual-system/03-04-SUMMARY.md

<interfaces>
<!-- The home and about namespaces ALREADY EXIST in both en.json and hi.json. -->
<!-- Read messages/en.json lines 23-74 for the exact key paths. Do NOT invent new keys if one exists. -->

home namespace keys (en.json lines 23-46):
  home.hero.headline, home.hero.tagline, home.hero.cta, home.hero.secondaryLink
  home.stats.beds/bedsLabel, home.stats.doctors/doctorsLabel, home.stats.specialties/specialtiesLabel, home.stats.emergency/emergencyLabel
  home.sections.departments, home.sections.doctors, home.sections.appointmentCta, home.sections.appointmentCtaDesc

about namespace keys (en.json lines 47-74):
  about.pageTitle
  about.intro.heading, about.intro.body
  about.stats.beds/bedsLabel, about.stats.doctors/doctorsLabel, about.stats.specialties/specialtiesLabel, about.stats.years/yearsLabel
  about.mission.heading, about.mission.body
  about.values.care/careDesc, about.values.safety/safetyDesc, about.values.community/communityDesc

common namespace (shared CTAs): common.requestAppointment, common.learnMoreServices, common.meetAllDoctors

Server Component translation pattern (already used in Header.tsx from Plan 03):
  import { getTranslations } from 'next-intl/server';
  const t = await getTranslations('home');  // or pass {locale, namespace}
  ...then t('hero.headline')
  setRequestLocale(locale) must be called BEFORE getTranslations (already present in both pages).

NOTE: en.json has NO "Beds"/"Doctors"/"Specialties"/"Emergency" standalone — it uses home.stats.bedsLabel etc. and about.stats.bedsLabel etc. Use those. The "Home › About Us" breadcrumb text and "Home › Departments" style breadcrumbs have no key — see action for handling.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add missing breadcrumb keys to en.json and hi.json</name>
  <files>messages/en.json, messages/hi.json</files>
  <read_first>
    - messages/en.json — read the COMPLETE file to confirm which keys already exist before adding. The breadcrumb prefix "Home" exists as nav.home but the breadcrumb separator phrasing does not exist as a key.
    - messages/hi.json — read the about and common namespaces to confirm structure mirror.
  </read_first>
  <action>
    The About page renders a breadcrumb "Home › About Us" and the Home page has no breadcrumb. The breadcrumb is composed of nav.home ("Home"/"होम") + separator + the page title (about.pageTitle).

    Do NOT add a new key for the breadcrumb. Instead, in Task 2 the breadcrumb will be composed in JSX as `{tNav('home')} › {t('pageTitle')}` where tNav = getTranslations('nav'). The › separator is a literal Unicode character, not translatable text.

    Therefore THIS task adds only ONE thing if and only if it is missing after reading en.json: confirm `about.pageTitle` and `nav.home` both exist in en.json AND hi.json. They do (en.json line 48 about.pageTitle, line 7 nav.home). 

    Result: NO new keys need to be added for these two pages. All required keys already exist. This task is a verification-only no-op write — if after reading you confirm every key listed in the interfaces block exists in BOTH en.json and hi.json with identical paths, make NO edit and record "all keys present" in the summary.

    If (and only if) any listed key is genuinely absent from hi.json, add the Hindi translation mirroring en.json structure, using formal आप-form per D-01, keeping proper nouns (Ayushman Bharat, PM-JAY, ICU, OPD, numerals) in English.
  </action>
  <verify>
    <automated>cd "D:\Git Hub\Aatmaram Website" && node -e "const en=JSON.parse(require('fs').readFileSync('messages/en.json','utf8'));const hi=JSON.parse(require('fs').readFileSync('messages/hi.json','utf8'));const paths=['home.hero.headline','home.hero.secondaryLink','home.sections.appointmentCtaDesc','about.intro.body','about.values.communityDesc','nav.home','common.requestAppointment'];function get(o,p){return p.split('.').reduce((a,k)=>a&&a[k],o);}paths.forEach(p=>{if(get(en,p)===undefined)throw new Error('en missing '+p);if(get(hi,p)===undefined)throw new Error('hi missing '+p);});console.log('OK: all required keys present in both files');"</automated>
  </verify>
  <acceptance_criteria>
    - Every key in the interfaces block exists in BOTH en.json and hi.json with identical dotted paths
    - If no key was missing, neither file was modified
    - If a key was added to hi.json, it uses Devanagari (U+0900–U+097F) and mirrors en.json structure exactly
    - JSON in both files remains valid
  </acceptance_criteria>
  <done>All home/about/common keys confirmed present in both locale files; any missing hi.json key filled with formal Hindi</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate Home page and About page to t() calls</name>
  <files>app/[locale]/(public)/page.tsx, app/[locale]/(public)/about/page.tsx</files>
  <read_first>
    - app/[locale]/(public)/page.tsx — read the COMPLETE file. Note every hardcoded literal: hero h1 "Quality Care...", tagline, "Request an Appointment" button (x2), "Learn about our services →", trust band "90/Beds, 25+/Doctors, 8/Specialties, 24x7/Emergency", "Our Departments" + subtitle, "Our Doctors" + subtitle, "Meet All Doctors →", "Ready to visit us?", "Our team is here to help. Request an appointment today."
    - app/[locale]/(public)/about/page.tsx — read the COMPLETE file. Note: breadcrumb "Home › About Us", h1 "About Us", "Caring for Kanpur Since Day One", long intro body, stat cards (90/Beds, 25+/Doctors, 8/Specialties, 10+/Years of Service), "Our Mission" + body, three value cards (Patient-First Care/Clinical Safety/Community Trust + their descriptions).
    - components/layout/Header.tsx — read to copy the exact getTranslations Server Component pattern already proven working.
  </read_first>
  <action>
    Migrate BOTH pages so that every visible string is rendered from a translation key. Do not change layout, className, icons, or component structure — only replace the text node literals with t() calls.

    For app/[locale]/(public)/page.tsx (HomePage):
    1. Add at top of the function body (after setRequestLocale(locale)):
         const t = await getTranslations('home');
         const tCommon = await getTranslations('common');
    2. Hero: h1 → {t('hero.headline')}; tagline p → {t('hero.tagline')}; first "Request an Appointment" button → {t('hero.cta')}; "Learn about our services →" link → {tCommon('learnMoreServices')} (note: en.json common.learnMoreServices = "Learn about our services" without arrow — keep the &rarr; as a literal JSX arrow after the text, or use {t('hero.secondaryLink')} which already includes "→". Prefer t('hero.secondaryLink') since it bundles the arrow.)
    3. Trust band: replace "90"→{t('stats.beds')} renders "90 Beds"? NO — home.stats.beds = "90 Beds" (combined). The band shows number and label separately. Use {t('stats.bedsLabel')} for the "Beds" label and keep the numeral "90" as a literal (numerals stay per D-01). Same for doctors/specialties/emergency: numerals "25+","8","24x7" stay literal; labels use t('stats.doctorsLabel'), t('stats.specialtiesLabel'), t('stats.emergencyLabel').
    4. Departments section: SectionHeading title → {t('sections.departments')}; subtitle "Expert care across 8 medical specialties" has no home key — it lives under departments.pageSubtitle. Import a second namespace: const tDept = await getTranslations('departments'); subtitle → {tDept('pageSubtitle')}.
    5. Doctors section: SectionHeading title → {t('sections.doctors')}; subtitle "Our team of specialists is here to help." lives under doctors.pageSubtitle → const tDoc = await getTranslations('doctors'); subtitle → {tDoc('pageSubtitle')}. "Meet All Doctors →" link → {tCommon('meetAllDoctors')} (en.json = "Meet All Doctors" without arrow; append &rarr; literal as the existing JSX already has).
    6. Appointment CTA band: h2 "Ready to visit us?" → {t('sections.appointmentCta')}; p → {t('sections.appointmentCtaDesc')}; second "Request an Appointment" button → {t('hero.cta')}.

    For app/[locale]/(public)/about/page.tsx (AboutPage):
    1. Add after setRequestLocale(locale):
         const t = await getTranslations('about');
         const tNav = await getTranslations('nav');
    2. Breadcrumb: "Home › About Us" → {tNav('home')} › {t('pageTitle')} (keep › as literal Unicode separator).
    3. h1 "About Us" → {t('pageTitle')}.
    4. Story: h2 → {t('intro.heading')}; intro body p → {t('intro.body')}.
    5. Stat cards: numerals "90","25+","8","10+" stay literal; labels → {t('stats.bedsLabel')}, {t('stats.doctorsLabel')}, {t('stats.specialtiesLabel')}, {t('stats.yearsLabel')}.
    6. Mission: SectionHeading title → {t('mission.heading')}; body p → {t('mission.body')}.
    7. Value cards: h3 → {t('values.care')}/{t('values.safety')}/{t('values.community')}; descriptions → {t('values.careDesc')}/{t('values.safetyDesc')}/{t('values.communityDesc')}.

    Keep getTranslations import present (about/page.tsx currently imports only setRequestLocale — add getTranslations to the import from 'next-intl/server'). page.tsx currently imports only setRequestLocale — add getTranslations to its 'next-intl/server' import too.

    Do NOT leave any English literal text node in either file. The only literal text allowed: numerals (90, 25+, 8, 24x7, 10+), the › / → separator glyphs, and proper nouns already inside translated strings.
  </action>
  <verify>
    <automated>cd "D:\Git Hub\Aatmaram Website" && node -e "const f=require('fs');const home=f.readFileSync('app/[locale]/(public)/page.tsx','utf8');const about=f.readFileSync('app/[locale]/(public)/about/page.tsx','utf8');[['home',home,['getTranslations','hero.headline','sections.appointmentCta','stats.bedsLabel']],['about',about,['getTranslations','intro.body','mission.heading','values.community']]].forEach(([n,c,keys])=>{keys.forEach(k=>{if(!c.includes(k))throw new Error(n+' missing '+k);});});const banned=['Quality Care for Every Child','Ready to visit us?','Caring for Kanpur Since Day One','Patient-First Care','Our team is here to help. Request'];banned.forEach(b=>{if(home.includes(b)||about.includes(b))throw new Error('Hardcoded literal still present: '+b);});console.log('OK: both pages migrated, no banned literals');"</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports getTranslations from 'next-intl/server'
    - page.tsx calls getTranslations('home'), ('common'), ('departments'), ('doctors') and consumes them in JSX
    - about/page.tsx imports getTranslations and calls getTranslations('about') + ('nav'), consumed in JSX
    - String "Quality Care for Every Child and Family" no longer appears as a literal in page.tsx
    - String "Ready to visit us?" no longer appears as a literal in page.tsx
    - String "Caring for Kanpur Since Day One" no longer appears as a literal in about/page.tsx
    - String "Patient-First Care" no longer appears as a literal in about/page.tsx
    - Numerals (90, 25+, 8, 24x7, 10+) remain as literals (per D-01)
    - npm run build exits 0 with no missing-key warnings
  </acceptance_criteria>
  <done>Home and About page bodies render entirely from translation keys; /hi shows Hindi body content, /en shows English; no hardcoded English text nodes remain</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static translation file | en.json/hi.json are build-time assets; no user input, no runtime mutation |
| Server Component render | getTranslations runs server-side; locale constrained to ['hi','en'] by routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | messages/*.json | accept | Only public UI strings; no PII or secrets |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
1. npm run build — exits 0, no "missing message" warnings
2. Visit /hi — hero, trust band labels, section headings, CTA band all in Hindi
3. Visit /en — same surfaces in English
4. Visit /hi/about — breadcrumb, story, stat labels, mission, value cards in Hindi
5. Toggle to EN on /hi/about — content switches to English
</verification>

<success_criteria>
- Home and About pages consume translations for every visible string
- /hi and /en render the correct language for both pages
- Numerals and proper nouns preserved per D-01
- Build clean, no missing-key warnings
</success_criteria>

<output>
Create .planning/phases/03-bilingual-system/03-05-SUMMARY.md when done
</output>

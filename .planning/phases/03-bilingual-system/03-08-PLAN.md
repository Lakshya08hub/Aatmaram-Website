---
phase: 03-bilingual-system
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - components/public/AppointmentForm.tsx
autonomous: true
requirements:
  - I18N-05
user_setup: []

must_haves:
  truths:
    - "AppointmentForm field labels render in the visitor's locale (Hindi or English)"
    - "All form placeholders and button text respect the language toggle"
    - "No hardcoded English strings remain in the form component"
  artifacts:
    - path: "components/public/AppointmentForm.tsx"
      provides: "i18n-enabled appointment form with locale-aware labels and placeholders"
      min_lines: 220
  key_links:
    - from: "components/public/AppointmentForm.tsx"
      to: "messages/{locale}.json"
      via: "useTranslations('appointment')"
      pattern: "t\\('appointment\\.(fields|submit|success|errors).*'\\)"
---

<objective>
Close the AppointmentForm i18n gap by replacing 5 hardcoded English field labels and supporting text with dynamic translations from `useTranslations('appointment')`.

Purpose: I18N-05 requirement — all public page body text bilingual. The form component currently renders English-only labels regardless of locale.

Output: AppointmentForm component with all user-facing strings wired to translations, maintaining form validation and behavior.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-bilingual-system/03-CONTEXT.md
@.planning/phases/03-bilingual-system/03-RESEARCH.md

**Translation keys already confirmed to exist in both en.json and hi.json:**
- `appointment.fields.patientName`
- `appointment.fields.phone`
- `appointment.fields.preferredDoctor`
- `appointment.fields.preferredDoctorPlaceholder`
- `appointment.fields.noPreference`
- `appointment.fields.preferredDate`
- `appointment.fields.reason`
- `appointment.fields.reasonPlaceholder`
- `appointment.submit`

**Form validation messages already in translations:**
- `appointment.errors.required`
- `appointment.errors.phone`
- `appointment.errors.pastDate`

**Success toast already in translations:**
- `appointment.success.title`
- `appointment.success.description`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AppointmentForm labels to translations</name>
  <files>components/public/AppointmentForm.tsx</files>
  <action>
Import `useTranslations` from 'next-intl' at the top of the component. Call `const t = useTranslations('appointment')` inside the AppointmentForm function body (before the form setup).

Replace all 8 hardcoded English strings with their translation equivalents:

1. **Line 91** — FormLabel for patientName: Replace `"Patient Name"` with `t('fields.patientName')`
2. **Line 113** — FormLabel for phone: Replace `"Phone Number"` with `t('fields.phone')`
3. **Line 135** — FormLabel for preferredDoctor: Replace `"Preferred Doctor"` with `t('fields.preferredDoctor')`
4. **Line 144** — SelectValue placeholder: Replace `"Select a doctor"` with `t('fields.preferredDoctorPlaceholder')`
5. **Line 148** — SelectItem for no-preference: Replace `"No preference"` with `t('fields.noPreference')`
6. **Line 170** — FormLabel for preferredDate: Replace `"Preferred Date"` with `t('fields.preferredDate')`
7. **Line 192** — FormLabel for reason: Replace `"Reason / Chief Complaint"` with `t('fields.reason')`
8. **Line 197** — Textarea placeholder: Replace `"Briefly describe..."` with `t('fields.reasonPlaceholder')`
9. **Line 213** — Submit Button text: Replace `"Submit Request"` with `t('submit')`

Also update the success toast (lines 67–71) to use translations:
- Replace hardcoded title `'Request Received'` with `t('success.title')`
- Replace hardcoded description with `t('success.description')`

The component remains `'use client'` — `useTranslations` works correctly in client components per next-intl v4 documentation.
  </action>
  <verify>
    <automated>grep -c "t('fields\." components/public/AppointmentForm.tsx</automated>
  </verify>
  <done>
All 9 user-facing strings in AppointmentForm replaced with translation calls. Component imports `useTranslations` and calls it correctly. File saves without syntax errors. Grep confirms at least 7 t('fields.*') calls present. No hardcoded English field labels, placeholders, or button text remain in the form definition.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (browser) ↔ Translation data | Translations loaded by next-intl middleware; no user input in label strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Information Disclosure | Translation keys exposed in client bundle | accept | Translation keys are static, non-sensitive UI labels; no PII or secrets stored |
| T-03-08-02 | Tampering | Form labels overwritten by XSS | mitigate | All labels sourced from next-intl i18n system (server-rendered initially, then client-controlled); no user input in label strings |

</threat_model>

<verification>
1. **Automated check:** Grep confirms t('fields.*') calls are present in the component.
2. **Manual browser test:** Visit `/hi/appointment` and `/en/appointment` in dev server; verify all form labels appear in the correct language (Hindi on `/hi`, English on `/en`).
3. **Form functionality check:** Submit the form with valid and invalid data; confirm error messages and success toast appear in the correct locale.
</verification>

<success_criteria>
- AppointmentForm imports and uses `useTranslations('appointment')`
- All 9 hardcoded English strings replaced with t() calls
- Form renders labels, placeholders, button text in the visitor's locale
- Toast success message displays in the correct language
- No syntax errors; component compiles cleanly
- Manual test: Form displays correctly in both `/hi/appointment` and `/en/appointment`
</success_criteria>

<output>
Create `.planning/phases/03-bilingual-system/03-08-SUMMARY.md` when done.
</output>

---
phase: 03-bilingual-system
plan: 07
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - messages/en.json
  - messages/hi.json
  - lib/data/services.ts
  - app/[locale]/(public)/services/page.tsx
  - app/[locale]/(public)/contact/page.tsx
  - app/[locale]/(public)/appointment/page.tsx
autonomous: true
requirements:
  - I18N-05
must_haves:
  truths:
    - "Visiting /hi/services shows the page heading, service list names, Ayushman Bharat section body, What-is-PM-JAY text, and facility names in Hindi"
    - "Visiting /hi/contact shows headings, Address/Phone/OPD/Emergency labels, OPD timings, and map placeholder in Hindi"
    - "Visiting /hi/appointment shows the page header and subtitle in Hindi (form fields were already translated)"
    - "Phone numbers, ICU/NICU/OPD/PM-JAY, ₹5 lakh, and the address remain in English per D-01"
  artifacts:
    - path: "messages/en.json"
      provides: "New services.list and services.facilities item keys + contact section completeness"
      contains: "\"list\""
    - path: "lib/data/services.ts"
      provides: "Service/Facility constants carrying stable i18n keys"
      contains: "translationKey"
    - path: "app/[locale]/(public)/services/page.tsx"
      provides: "Services page rendering all body strings via translations"
      contains: "getTranslations"
  key_links:
    - from: "app/[locale]/(public)/services/page.tsx"
      to: "messages/hi.json services namespace"
      via: "getTranslations('services') resolving service.translationKey"
      pattern: "getTranslations\\('services'\\)"
    - from: "app/[locale]/(public)/contact/page.tsx"
      to: "messages/hi.json contact namespace"
      via: "getTranslations('contact') consumed in JSX"
      pattern: "getTranslations\\('contact'\\)"
---

<objective>
Migrate the Services, Contact, and Appointment pages to consume translations. Contact and Appointment-header strings already exist in hi.json (contact and appointment namespaces). Services needs NEW keys for the 8 individual service names and 6 facility names, because en.json's services namespace currently has only headings — the per-item names live only in the English-only lib/data/services.ts constant.

Purpose: Closes I18N-05 for the final three pages. After this plan, every public page body renders bilingual. This is the last gap blocking ROADMAP Phase 3 Success Criterion 3.

Output: services/contact/appointment pages migrated to t(); lib/data/services.ts carries translationKey fields; en.json and hi.json gain services.list.* and services.facilities.items.* keys.
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-bilingual-system/03-CONTEXT.md
@.planning/phases/03-bilingual-system/03-04-SUMMARY.md

<interfaces>
<!-- contact + appointment namespaces ALREADY exist & translated (Plan 02). services namespace needs item keys ADDED. -->
<!-- Read messages/en.json lines 119-177 before implementing. -->

services namespace (en.json lines 119-131) — CURRENT (headings only, NO per-item names):
  services.pageTitle = "Services & Facilities"
  services.pageSubtitle = "Comprehensive care for every patient"
  services.ayushman.{heading,body,whatIs,whatIsBody}
  services.facilities.heading = "Our Facilities"
  → MISSING: individual service names and facility names. ADD them (see Task 1).

contact namespace (en.json lines 132-153) — COMPLETE, already translated:
  contact.pageTitle, contact.address.{heading,value}, contact.phone.{heading,value},
  contact.opd.{heading,value}, contact.emergency.{heading,value}, contact.map.placeholder
  NOTE: en.json contact has NO "Get in Touch" SectionHeading key and NO "Interactive map will be available soon" subtext key. Add contact.getInTouch and contact.map.subtext (see Task 1).

appointment namespace (en.json lines 154-177) — COMPLETE:
  appointment.pageTitle, appointment.pageSubtitle, appointment.fields.*, appointment.submit, appointment.success.*, appointment.errors.*
  The AppointmentForm already consumes these. Only the appointment PAGE WRAPPER header/subtitle/breadcrumb are hardcoded.

Current lib/data/services.ts service ids:
  paediatric-care, paediatric-surgery, critical-care, general-surgery,
  orthopaedic-care, maternity-care, general-medicine, emergency-trauma
facility ids:
  icu, nicu, operation-theatre, emergency, laboratory, pharmacy

Per D-01 — keep in English: ICU, NICU, OPD, PM-JAY, Ayushman Bharat, Pradhan Mantri Jan Arogya Yojana, ₹5 lakh, phone numbers, the Naubasta/Kidwai Nagar address, "Atmaram Child Care and Critical Care".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add service/facility item keys + contact extras to en.json and hi.json; key the services constant</name>
  <files>messages/en.json, messages/hi.json, lib/data/services.ts</files>
  <read_first>
    - messages/en.json lines 119-153 — read services and contact namespaces to confirm exactly which keys exist and which are missing.
    - messages/hi.json — read services and contact namespaces to mirror structure when adding.
    - lib/data/services.ts — read COMPLETE. Note service ids and facility ids and that names are English literals.
  </read_first>
  <action>
    Step 1 — Add to en.json services namespace a "list" object (8 service names) and a "facilities.items" object (6 facility names), plus contact extras:

    services.list:
      paediatricCare = "Paediatric & Neonatal Care"
      paediatricSurgery = "Paediatric Surgery"
      criticalCare = "Critical Care & ICU"
      generalSurgery = "General Surgery"
      orthopaedicCare = "Orthopaedic Care"
      maternityCare = "Maternity & Women's Health"
      generalMedicine = "General Medicine"
      emergencyTrauma = "Emergency & Trauma Care"

    services.facilities.items (keep the existing services.facilities.heading):
      icu = "Intensive Care Unit (ICU)"
      nicu = "NICU (Neonatal ICU)"
      operationTheatre = "Operation Theatre"
      emergency = "24x7 Emergency"
      laboratory = "Diagnostic Laboratory"
      pharmacy = "Pharmacy"

    contact additions:
      contact.getInTouch = "Get in Touch"
      contact.map.subtext = "Interactive map will be available soon"

    Step 2 — Add the SAME key structure to hi.json with formal Hindi translations (per D-01). Suggested Hindi (apply formal आप-form, keep proper nouns/abbreviations in English):
      services.list.paediatricCare = "बाल एवं नवजात देखभाल"
      services.list.paediatricSurgery = "बाल शल्य चिकित्सा"
      services.list.criticalCare = "गहन चिकित्सा एवं ICU"
      services.list.generalSurgery = "सामान्य शल्य चिकित्सा"
      services.list.orthopaedicCare = "हड्डी रोग देखभाल"
      services.list.maternityCare = "मातृत्व एवं महिला स्वास्थ्य"
      services.list.generalMedicine = "सामान्य चिकित्सा"
      services.list.emergencyTrauma = "आपातकाल एवं आघात देखभाल"
      services.facilities.items.icu = "गहन चिकित्सा इकाई (ICU)"
      services.facilities.items.nicu = "NICU (नवजात ICU)"
      services.facilities.items.operationTheatre = "ऑपरेशन थिएटर"
      services.facilities.items.emergency = "24x7 आपातकाल"
      services.facilities.items.laboratory = "नैदानिक प्रयोगशाला"
      services.facilities.items.pharmacy = "फार्मेसी"
      contact.getInTouch = "संपर्क करें"
      contact.map.subtext = "इंटरैक्टिव मानचित्र जल्द ही उपलब्ध होगा"

    Step 3 — lib/data/services.ts: add translationKey to Service and Facility interfaces and entries mapping id → camelCase key:
      services: paediatric-care→paediatricCare, paediatric-surgery→paediatricSurgery, critical-care→criticalCare, general-surgery→generalSurgery, orthopaedic-care→orthopaedicCare, maternity-care→maternityCare, general-medicine→generalMedicine, emergency-trauma→emergencyTrauma
      facilities: icu→icu, nicu→nicu, operation-theatre→operationTheatre, emergency→emergency, laboratory→laboratory, pharmacy→pharmacy
    Keep existing English name fields as fallback. Keep icon fields.

    Both JSON files must remain valid and stay structural mirrors (same key paths).
  </action>
  <verify>
    <automated>cd "D:\Git Hub\Aatmaram Website" && node -e "const en=JSON.parse(require('fs').readFileSync('messages/en.json','utf8'));const hi=JSON.parse(require('fs').readFileSync('messages/hi.json','utf8'));const svc=require('fs').readFileSync('lib/data/services.ts','utf8');function countKeys(o){return Object.keys(o).reduce((a,k)=>typeof o[k]==='object'&&o[k]!==null?a+countKeys(o[k]):a+1,0);}if(countKeys(en)!==countKeys(hi))throw new Error('Key count mismatch en='+countKeys(en)+' hi='+countKeys(hi));['list.criticalCare','facilities.items.icu'].forEach(p=>{const path='services.'+p;const get=(o)=>path.split('.').reduce((a,k)=>a&&a[k],o);if(get(en)===undefined)throw new Error('en missing services.'+p);if(get(hi)===undefined)throw new Error('hi missing services.'+p);});if(en.contact.getInTouch===undefined||hi.contact.getInTouch===undefined)throw new Error('contact.getInTouch missing');if(!svc.includes('translationKey'))throw new Error('services.ts missing translationKey');console.log('OK: keys added, structural mirror intact, constant keyed');"</automated>
  </verify>
  <acceptance_criteria>
    - en.json has services.list with 8 entries and services.facilities.items with 6 entries
    - en.json has contact.getInTouch and contact.map.subtext
    - hi.json mirrors all added keys with Devanagari values (ICU/NICU/PM-JAY kept English inside Hindi strings)
    - en.json and hi.json have identical key counts (structural mirror)
    - lib/data/services.ts Service and Facility interfaces + entries carry translationKey
    - Both JSON files valid
  </acceptance_criteria>
  <done>Service/facility item keys and contact extras exist in both locale files; services constant carries stable translation keys</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate Services, Contact, and Appointment pages to t()</name>
  <files>app/[locale]/(public)/services/page.tsx, app/[locale]/(public)/contact/page.tsx, app/[locale]/(public)/appointment/page.tsx</files>
  <read_first>
    - app/[locale]/(public)/services/page.tsx — read COMPLETE. Note breadcrumb, h1, SectionHeading title/subtitle, services.map (renders service.name), Ayushman section heading+body+whatIs+whatIsBody, facilities SectionHeading + facilities.map (renders facility.name).
    - app/[locale]/(public)/contact/page.tsx — read COMPLETE. Note breadcrumb, h1, "Get in Touch", 4 card headings (Address/Phone/OPD Timings/24x7 Emergency) + their values, "Map loading...", "Interactive map will be available soon".
    - app/[locale]/(public)/appointment/page.tsx — read COMPLETE. Note breadcrumb "Home › Request an Appointment", h1 "Request an Appointment", subtitle "Fill in the form below...". The AppointmentForm component already handles form fields — do NOT touch it.
    - lib/data/services.ts — confirm translationKey fields from Task 1.
    - messages/en.json lines 119-177 — confirm key paths.
  </read_first>
  <action>
    services/page.tsx:
    1. const t = await getTranslations('services'); const tNav = await getTranslations('nav');
       (services page currently imports only setRequestLocale — add getTranslations to the 'next-intl/server' import.)
    2. Breadcrumb "Home › Services & Facilities" → {tNav('home')} › {t('pageTitle')}.
    3. h1 → {t('pageTitle')}.
    4. SectionHeading title → {t('pageTitle')}; subtitle → {t('pageSubtitle')}.
    5. services.map span → {t(`list.${service.translationKey}`)}.
    6. Ayushman section: SectionHeading title → {t('ayushman.heading')}; body p → {t('ayushman.body')}; h3 → {t('ayushman.whatIs')}; body p → {t('ayushman.whatIsBody')}.
    7. Facilities: SectionHeading title → {t('facilities.heading')}; facility name p → {t(`facilities.items.${facility.translationKey}`)}.

    contact/page.tsx:
    1. const t = await getTranslations('contact'); const tNav = await getTranslations('nav');
       (add getTranslations to the 'next-intl/server' import.)
    2. Breadcrumb "Home › Contact Us" → {tNav('home')} › {t('pageTitle')}.
    3. h1 → {t('pageTitle')}.
    4. SectionHeading "Get in Touch" → {t('getInTouch')}.
    5. Address card: heading → {t('address.heading')}; value → {t('address.value')} (value stays the address text — already English-only proper noun in both locales).
    6. Phone card: heading → {t('phone.heading')}; value → {t('phone.value')}.
    7. OPD card: heading → {t('opd.heading')}; value → {t('opd.value')} (hi.json has Hindi day/time).
    8. Emergency card: heading → {t('emergency.heading')}; value → {t('emergency.value')}.
    9. Map placeholder "Map loading..." → {t('map.placeholder')}; "Interactive map will be available soon" → {t('map.subtext')}.

    appointment/page.tsx:
    1. const t = await getTranslations('appointment'); const tNav = await getTranslations('nav');
       (add getTranslations to the 'next-intl/server' import.)
    2. Breadcrumb "Home › Request an Appointment" → {tNav('home')} › {t('pageTitle')}.
    3. h1 → {t('pageTitle')}.
    4. subtitle "Fill in the form below..." → {t('pageSubtitle')}.
    5. Do NOT modify <AppointmentForm /> — it is already translated.

    Allowed literals only: the › separator glyph, phone numbers/address (which come through translated values anyway), ICU/NICU/etc. inside translated strings.
  </action>
  <verify>
    <automated>cd "D:\Git Hub\Aatmaram Website" && node -e "const f=require('fs');const s=f.readFileSync('app/[locale]/(public)/services/page.tsx','utf8');const c=f.readFileSync('app/[locale]/(public)/contact/page.tsx','utf8');const a=f.readFileSync('app/[locale]/(public)/appointment/page.tsx','utf8');if(!s.includes(\"getTranslations('services')\"))throw new Error('services page not using getTranslations');if(s.includes('Comprehensive care for every patient'))throw new Error('services subtitle still hardcoded');if(s.includes('flagship public health insurance'))throw new Error('services whatIsBody still hardcoded');if(!c.includes(\"getTranslations('contact')\"))throw new Error('contact page not using getTranslations');if(c.includes('Get in Touch')&&!c.includes(\"t('getInTouch')\"))throw new Error('contact Get in Touch hardcoded');if(c.includes('Interactive map will be available soon')&&!c.includes(\"map.subtext\"))throw new Error('contact map subtext hardcoded');if(!a.includes(\"getTranslations('appointment')\"))throw new Error('appointment page not using getTranslations');if(a.includes('Fill in the form below and our team'))throw new Error('appointment subtitle hardcoded');console.log('OK: services, contact, appointment migrated');"</automated>
  </verify>
  <acceptance_criteria>
    - services/page.tsx consumes getTranslations('services') + ('nav'); service and facility names via t(`list...`)/t(`facilities.items...`)
    - "Comprehensive care for every patient" and the PM-JAY whatIsBody no longer appear as literals in services/page.tsx
    - contact/page.tsx consumes getTranslations('contact') + ('nav'); all 4 card headings + values + map texts via t()
    - "Get in Touch" and "Interactive map will be available soon" no longer appear as raw literals in contact/page.tsx
    - appointment/page.tsx header h1 and subtitle use t(); AppointmentForm untouched
    - "Fill in the form below and our team will call you to confirm." no longer a literal in appointment/page.tsx
    - Phone numbers, address, ICU/NICU/OPD/PM-JAY preserved (flow through translated values)
    - npm run build exits 0 with no missing-key warnings
  </acceptance_criteria>
  <done>Services, Contact, and Appointment-header render entirely from translations; /hi shows Hindi, /en shows English; all public pages now bilingual</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static translation + seeded data | en.json/hi.json/services.ts are build-time assets; no user input |
| Server Component render | getTranslations server-side; locale constrained to ['hi','en'] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | messages/*.json, services.ts | accept | Only public UI strings; no PII/secrets (phone is public placeholder) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
1. npm run build — exits 0, no missing-key warnings
2. Visit /hi/services — heading, service list, Ayushman body, What-is-PM-JAY, facilities all Hindi; ICU/NICU/PM-JAY/₹5 lakh stay English
3. Visit /hi/contact — headings, labels, OPD timing, map texts Hindi; phone/address stay as-is
4. Visit /hi/appointment — header + subtitle Hindi; form fields already Hindi
5. Toggle to /en on each — content switches to English
6. Confirm /hi/{all 7 pages} have zero English body text except proper nouns/numerals
</verification>

<success_criteria>
- Services, Contact, Appointment pages fully bilingual
- New service/facility/contact keys present and translated in both locale files
- All 7 public pages now render bilingual bodies (completing I18N-05)
- Build clean, no missing-key warnings
</success_criteria>

<output>
Create .planning/phases/03-bilingual-system/03-07-SUMMARY.md when done
</output>

---
phase: 05-content-management
plan: 05
type: execute
wave: 3
depends_on: [05-02, 05-03, 05-04]
files_modified:
  - app/[locale]/(public)/departments/page.tsx
  - app/[locale]/(public)/doctors/page.tsx
  - app/[locale]/(public)/services/page.tsx
  - app/[locale]/(public)/contact/page.tsx
autonomous: true
requirements: [DYN-01, DYN-02, DYN-03, DYN-04]
must_haves:
  truths:
    - "Public Departments page reads from Supabase getDepartments() — no static import"
    - "Public Doctors page reads from Supabase getActiveDoctors() — only is_active=true doctors shown"
    - "Public Services page reads from Supabase getFacilities() — no static import"
    - "Public Contact page reads hospital_info row from Supabase getHospitalInfo()"
    - "All four pages export const dynamic = 'force-dynamic' and have NO generateStaticParams export"
    - "DB read failures on public pages are caught silently — empty list shown, no error to visitor"
    - "No department, doctor, facility, or hospital info value is hardcoded in any of the four pages"
  artifacts:
    - path: "app/[locale]/(public)/departments/page.tsx"
      provides: "Dynamic server page reading from departments table"
      contains: "export const dynamic = 'force-dynamic'"
    - path: "app/[locale]/(public)/doctors/page.tsx"
      provides: "Dynamic server page reading from doctors table (active only)"
      contains: "export const dynamic = 'force-dynamic'"
    - path: "app/[locale]/(public)/services/page.tsx"
      provides: "Dynamic server page reading from facilities table"
      contains: "export const dynamic = 'force-dynamic'"
    - path: "app/[locale]/(public)/contact/page.tsx"
      provides: "Dynamic server page reading hospital_info row"
      contains: "export const dynamic = 'force-dynamic'"
  key_links:
    - from: "app/[locale]/(public)/departments/page.tsx"
      to: "lib/db/departments.ts"
      via: "import getDepartments"
      pattern: "getDepartments"
    - from: "app/[locale]/(public)/doctors/page.tsx"
      to: "lib/db/doctors.ts"
      via: "import getActiveDoctors"
      pattern: "getActiveDoctors"
    - from: "app/[locale]/(public)/services/page.tsx"
      to: "lib/db/facilities.ts"
      via: "import getFacilities"
      pattern: "getFacilities"
    - from: "app/[locale]/(public)/contact/page.tsx"
      to: "lib/db/hospital-info.ts"
      via: "import getHospitalInfo"
      pattern: "getHospitalInfo"
---

<objective>
Convert all four public pages from static TypeScript constants to Supabase DB reads. Each page gets `export const dynamic = 'force-dynamic'`, removes `generateStaticParams`, and replaces static data imports with lib/db calls wrapped in try/catch.

Purpose: Delivers DYN-01 through DYN-04. After this plan, no hardcoded content remains on the public site — all content flows from Supabase through the portal.
Output: Four converted public pages with dynamic rendering and silent-fail error handling
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-content-management/05-RESEARCH.md
@.planning/phases/05-content-management/05-01-SUMMARY.md
@.planning/phases/05-content-management/05-02-SUMMARY.md
@.planning/phases/05-content-management/05-03-SUMMARY.md
@.planning/phases/05-content-management/05-04-SUMMARY.md
</context>

<interfaces>
<!-- lib/db exports available after plans 05-02, 05-03, 05-04: -->
// import { getDepartments, Department } from '@/lib/db/departments'
// import { getActiveDoctors, Doctor } from '@/lib/db/doctors'
// import { getFacilities, Facility } from '@/lib/db/facilities'
// import { getHospitalInfo, HospitalInfo } from '@/lib/db/hospital-info'

<!-- Pattern for public page conversion (from 05-RESEARCH.md Pattern 4): -->
// REMOVE: export function generateStaticParams() { ... }
// ADD:    export const dynamic = 'force-dynamic';
// WRAP:   let items = []; try { items = await getData(); } catch (err) { console.error(...) }
// RENDER: use items directly — DB strings displayed as-is (no translation key lookup for content)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Convert Departments and Doctors public pages</name>
  <files>
    app/[locale]/(public)/departments/page.tsx,
    app/[locale]/(public)/doctors/page.tsx
  </files>
  <read_first>
    - app/[locale]/(public)/departments/page.tsx — full current file; identify: static import source, generateStaticParams export, where the static array is used in JSX
    - app/[locale]/(public)/doctors/page.tsx — same
    - lib/db/departments.ts — Department type shape
    - lib/db/doctors.ts — Doctor type shape
  </read_first>
  <action>
    For app/[locale]/(public)/departments/page.tsx:
    1. Add at top of file (after existing imports): export const dynamic = 'force-dynamic';
    2. Remove the generateStaticParams export function entirely (it was generating locale variants; not needed with force-dynamic)
    3. Remove the static data import (e.g. import { departments } from '@/lib/data/departments' or similar)
    4. Add import: import { getDepartments } from '@/lib/db/departments'; import type { Department } from '@/lib/db/departments';
    5. In the page function body, before the return statement: declare let departments: Department[] = []; then wrap await getDepartments() in try/catch — on catch, console.error('[DepartmentsPage] fetch failed:', err) and leave departments as []
    6. Pass departments to the existing DepartmentCard or map rendering, replacing the static array reference
    7. Update DepartmentCard calls to use dept.name and dept.description directly (not t('key.name') — DB strings are displayed as-is per research Pattern 7)
    8. Keep all existing next-intl calls (setRequestLocale, getTranslations) for page chrome (headings, CTAs, nav labels)

    For app/[locale]/(public)/doctors/page.tsx:
    1. Same steps 1-8, using getActiveDoctors() (returns only is_active=true doctors)
    2. Replace static doctor array with Doctor[] from DB
    3. DoctorCard props: use doctor.full_name, doctor.specialization, doctor.photo_url, doctor.qualification directly from DB type

    Important: do not remove any next-intl page chrome translation calls — only remove the static CONTENT data imports (department/doctor arrays). The page heading, subtitle, CTA text, and navigation all stay translated via next-intl.
  </action>
  <verify>
    <automated>grep -c "export const dynamic = 'force-dynamic'" "D:/Git Hub/Aatmaram Website/app/[locale]/(public)/departments/page.tsx"</automated>
    <automated>grep -c "generateStaticParams" "D:/Git Hub/Aatmaram Website/app/[locale]/(public)/departments/page.tsx"</automated>
    <automated>grep -c "getDepartments" "D:/Git Hub/Aatmaram Website/app/[locale]/(public)/departments/page.tsx"</automated>
    <automated>grep -c "export const dynamic = 'force-dynamic'" "D:/Git Hub/Aatmaram Website/app/[locale]/(public)/doctors/page.tsx"</automated>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    Departments page exports dynamic='force-dynamic', has NO generateStaticParams export, imports getDepartments(), wraps it in try/catch. Same for Doctors page using getActiveDoctors(). TypeScript compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert Services and Contact public pages</name>
  <files>
    app/[locale]/(public)/services/page.tsx,
    app/[locale]/(public)/contact/page.tsx
  </files>
  <read_first>
    - app/[locale]/(public)/services/page.tsx — full current file; identify static data import and usage
    - app/[locale]/(public)/contact/page.tsx — full current file; identify static hospital info constants (phone number, address, timings, etc.) and where they are rendered
    - lib/db/facilities.ts — Facility type shape
    - lib/db/hospital-info.ts — HospitalInfo type shape
  </read_first>
  <action>
    For app/[locale]/(public)/services/page.tsx:
    1. Add: export const dynamic = 'force-dynamic';
    2. Remove generateStaticParams export
    3. Remove static services/facilities array import
    4. Add: import { getFacilities } from '@/lib/db/facilities'; import type { Facility } from '@/lib/db/facilities';
    5. let facilities: Facility[] = []; try { facilities = await getFacilities(); } catch (err) { console.error('[ServicesPage] fetch failed:', err) }
    6. Render facility cards/list from facilities array using facility.name, facility.description, facility.category directly
    7. Keep next-intl calls for page-level chrome

    For app/[locale]/(public)/contact/page.tsx:
    1. Add: export const dynamic = 'force-dynamic';
    2. Remove generateStaticParams export
    3. Remove any static hospital info constants (hardcoded phone number, address, OPD timings)
    4. Add: import { getHospitalInfo } from '@/lib/db/hospital-info'; import type { HospitalInfo } from '@/lib/db/hospital-info';
    5. let hospitalInfo: HospitalInfo | null = null; try { hospitalInfo = await getHospitalInfo(); } catch (err) { console.error('[ContactPage] fetch failed:', err) }
    6. Render: hospital name (keep as static — it doesn't change), phone number from hospitalInfo?.emergency_number ?? '', address from hospitalInfo?.address_line1 + hospitalInfo?.address_line2 ?? '', OPD timings from hospitalInfo?.opd_timings ?? '', maps embed from hospitalInfo?.maps_embed_url
    7. If hospitalInfo is null: show the contact page with empty/fallback strings (not an error — UI-SPEC: fail silently on public side)
    8. Keep next-intl calls for page chrome (page title, section headings, form labels)

    After both edits, verify no static data imports remain for department/doctor/service/contact content in these four files.
  </action>
  <verify>
    <automated>grep -c "export const dynamic = 'force-dynamic'" "D:/Git Hub/Aatmaram Website/app/[locale]/(public)/services/page.tsx"</automated>
    <automated>grep -c "export const dynamic = 'force-dynamic'" "D:/Git Hub/Aatmaram Website/app/[locale]/(public)/contact/page.tsx"</automated>
    <automated>grep -c "getHospitalInfo" "D:/Git Hub/Aatmaram Website/app/[locale]/(public)/contact/page.tsx"</automated>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
    <automated>npx next build --no-lint 2>&1 | tail -15</automated>
  </verify>
  <done>
    All four public pages export dynamic='force-dynamic', have no generateStaticParams, import from lib/db, and wrap reads in try/catch. TypeScript compiles. next build succeeds with no errors. No static content constants remain in any of the four page files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase → Public Server Component | DB data rendered server-side; anon key used; no auth required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Information Disclosure | Public pages show DB content via anon key | accept | Content is intentionally public (departments, doctors, facilities, contact) — no PII in these tables |
| T-05-13 | Denial of Service | Supabase unavailable — public page crashes | mitigate | try/catch on all four pages; empty array fallback shown instead of 500 error |
| T-05-14 | Tampering | Stale Next.js cache overrides force-dynamic | mitigate | export const dynamic = 'force-dynamic' prevents any page-level caching; no ISR |
| T-05-SC | Tampering | npm installs | accept | No new npm packages in this plan |
</threat_model>

<verification>
After applying migration (05-01 checkpoint) and completing portal CRUD plans (05-02, 05-03, 05-04):
1. Add a department via /portal/content/departments
2. Reload /en/departments in browser — new department appears without rebuild
3. Add a facility via /portal/content/facilities
4. Reload /en/services — new facility appears
5. Edit hospital info OPD timings via /portal/content/hospital-info
6. Reload /en/contact — updated timings appear
7. npx next build --no-lint exits 0
8. grep returns 0 for any hardcoded department/doctor/facility names in the four page files
</verification>

<success_criteria>
All four public pages render content from Supabase. Changing a value in the portal is immediately reflected on the public site without any build or deploy step. DB failures produce empty states, not error pages. TypeScript compiles and next build succeeds.
</success_criteria>

<output>
Create .planning/phases/05-content-management/05-05-SUMMARY.md when done
</output>

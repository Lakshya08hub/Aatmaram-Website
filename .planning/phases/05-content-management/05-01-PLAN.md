---
phase: 05-content-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260612_cms_tables.sql
  - lib/supabase/admin.ts
  - .env.example
  - lib/portal/roles.ts
  - app/(portal)/content/layout.tsx
autonomous: false
requirements: [DYN-01, DYN-02, DYN-03, DYN-04, CMS-01, CMS-02, CMS-03, CMS-04]
must_haves:
  truths:
    - "Four CMS tables exist in Supabase: departments, doctors, facilities, hospital_info"
    - "RLS SELECT policies allow anon + authenticated reads on all four tables"
    - "hospital_info has exactly one seeded row"
    - "doctors table has nullable staff_user_id FK referencing profiles(id)"
    - "lib/supabase/admin.ts exports a createAdminClient() that uses SUPABASE_SERVICE_ROLE_KEY"
    - "lib/portal/roles.ts exports 'content' section visible to super_admin and admin"
    - "app/(portal)/content/layout.tsx redirects unauthenticated or non-admin users before any content sub-page renders"
  artifacts:
    - path: "supabase/migrations/20260612_cms_tables.sql"
      provides: "Schema for departments, doctors, facilities, hospital_info + RLS + seed row"
    - path: "lib/supabase/admin.ts"
      provides: "Service-role Supabase client for portal Server Actions"
    - path: "lib/portal/roles.ts"
      provides: "Updated ALL_SECTIONS + ROLE_SECTIONS with content section"
    - path: ".env.example"
      provides: "Documents SUPABASE_SERVICE_ROLE_KEY env var"
    - path: "app/(portal)/content/layout.tsx"
      provides: "Role-guard layout: redirects unauthenticated users to /login and non-admin roles to /portal/dashboard"
  key_links:
    - from: "lib/supabase/admin.ts"
      to: "SUPABASE_SERVICE_ROLE_KEY"
      via: "process.env lookup"
      pattern: "SUPABASE_SERVICE_ROLE_KEY"
    - from: "lib/portal/roles.ts ROLE_SECTIONS"
      to: "content section"
      via: "super_admin + admin arrays"
      pattern: "'content'"
    - from: "app/(portal)/content/layout.tsx"
      to: "profiles table role column"
      via: "getUser() + supabase.from('profiles').select('role')"
      pattern: "getUser"
---

<objective>
Lay the foundation for Phase 5: create and apply the Supabase schema, add the service-role admin client, add SUPABASE_SERVICE_ROLE_KEY to env.example and .env.local, extend the portal roles configuration with the new content section, and create the role-guard layout that protects all /portal/content/* routes.

Purpose: All subsequent plans in this phase depend on the four CMS tables existing in the database. The admin client is required by the Server Actions plans (05-02, 05-03, 05-04). The roles update unlocks the Content section in the portal sidebar. The content layout.tsx enforces that only super_admin and admin roles can access any /portal/content/* route — the sidebar link alone is not sufficient protection.
Output: Migration SQL file, admin Supabase client, updated roles.ts, updated .env.example, role-guard content layout
</objective>

<execution_context>
@C:/Users/lakshya_new/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lakshya_new/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-content-management/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CMS migration SQL + admin client + env.example update</name>
  <files>
    supabase/migrations/20260612_cms_tables.sql,
    lib/supabase/admin.ts,
    .env.example
  </files>
  <read_first>
    - lib/supabase/server.ts — copy the client creation pattern; admin.ts mirrors it with SUPABASE_SERVICE_ROLE_KEY
    - .env.example — append the new key without removing existing lines
    - supabase/migrations/ — check existing migration filenames to avoid conflicts
  </read_first>
  <action>
    Create supabase/migrations/20260612_cms_tables.sql with the exact SQL from 05-RESEARCH.md Pattern 1 and Pattern 2. The file must contain, in order:

    1. CREATE TABLE departments (id uuid PK, name text NOT NULL, description text NOT NULL, image_url text, created_at timestamptz DEFAULT now(), updated_at timestamptz DEFAULT now())
    2. CREATE TABLE doctors (id uuid PK, full_name text NOT NULL, specialization text NOT NULL, qualification text NOT NULL, photo_url text, bio text, availability_days text[], is_active boolean NOT NULL DEFAULT true, staff_user_id uuid REFERENCES profiles(id) ON DELETE SET NULL, created_at timestamptz DEFAULT now(), updated_at timestamptz DEFAULT now())
    3. CREATE TYPE facility_category AS ENUM ('OPD', 'ICU', 'Diagnostics', 'Surgery', 'Other')
    4. CREATE TABLE facilities (id uuid PK, name text NOT NULL, description text NOT NULL, category facility_category NOT NULL DEFAULT 'Other', created_at timestamptz DEFAULT now(), updated_at timestamptz DEFAULT now())
    5. CREATE TABLE hospital_info (id uuid PK, about_text text NOT NULL DEFAULT '', opd_timings text NOT NULL DEFAULT '', emergency_number text NOT NULL DEFAULT '', address_line1 text NOT NULL DEFAULT '', address_line2 text, city text NOT NULL DEFAULT 'Kanpur', maps_embed_url text, updated_at timestamptz DEFAULT now())
    6. INSERT INTO hospital_info DEFAULT VALUES; (seed exactly one row)
    7. ALTER TABLE + ENABLE ROW LEVEL SECURITY on all four tables
    8. CREATE POLICY "departments: public read" ON departments FOR SELECT TO anon, authenticated USING (true)
    9. Same SELECT policy for doctors, facilities, hospital_info

    Create lib/supabase/admin.ts: copy the structure of lib/supabase/server.ts but use process.env.SUPABASE_SERVICE_ROLE_KEY as the second argument to createClient (instead of the anon key). Export a function named createAdminClient(). No cookie handling needed — service role clients are stateless. Add a guard: if SUPABASE_SERVICE_ROLE_KEY is not set, throw an Error('SUPABASE_SERVICE_ROLE_KEY is not configured').

    Append to .env.example (do not overwrite the file; read it first and add after the last line):
    SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

    Also add SUPABASE_SERVICE_ROLE_KEY to .env.local if it exists. If the key is already present, skip.
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE departments" "D:/Git Hub/Aatmaram Website/supabase/migrations/20260612_cms_tables.sql"</automated>
    <automated>grep -c "staff_user_id" "D:/Git Hub/Aatmaram Website/supabase/migrations/20260612_cms_tables.sql"</automated>
    <automated>grep -c "INSERT INTO hospital_info" "D:/Git Hub/Aatmaram Website/supabase/migrations/20260612_cms_tables.sql"</automated>
    <automated>grep -c "createAdminClient" "D:/Git Hub/Aatmaram Website/lib/supabase/admin.ts"</automated>
    <automated>grep -c "SUPABASE_SERVICE_ROLE_KEY" "D:/Git Hub/Aatmaram Website/.env.example"</automated>
  </verify>
  <done>
    Migration file exists with all four CREATE TABLE statements, staff_user_id FK on doctors, RLS policies, and the seed INSERT. admin.ts exports createAdminClient(). .env.example documents SUPABASE_SERVICE_ROLE_KEY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update lib/portal/roles.ts — add content section</name>
  <files>lib/portal/roles.ts</files>
  <read_first>
    - lib/portal/roles.ts — full file (already read above; use the current content)
  </read_first>
  <action>
    Edit lib/portal/roles.ts:

    1. In ALL_SECTIONS array, insert a new entry AFTER the dashboard entry and BEFORE staff:
       { key: 'content', label: 'Content', href: '/content' }

    2. In ROLE_SECTIONS:
       - super_admin: add 'content' to its array (keep all other keys; insert 'content' after 'dashboard')
       - admin: add 'content' to its array (insert 'content' after 'dashboard')
       - receptionist: no change (does not get content access)
       - doctor: no change (does not get content access)

    Update the JSDoc comment above ROLE_SECTIONS to reflect that super_admin and admin now have 8 and 7 sections respectively (content section added).

    Do not remove or reorder any existing sections.
  </action>
  <verify>
    <automated>node -e "const r = require('./lib/portal/roles.ts'); console.log('ok')" 2>&1 || npx tsx -e "import { ALL_SECTIONS, ROLE_SECTIONS } from './lib/portal/roles'; console.log(ALL_SECTIONS.find(s => s.key === 'content'), ROLE_SECTIONS.super_admin.includes('content'), ROLE_SECTIONS.admin.includes('content'), ROLE_SECTIONS.receptionist.includes('content'))"</automated>
    <automated>grep -c "'content'" "D:/Git Hub/Aatmaram Website/lib/portal/roles.ts"</automated>
  </verify>
  <done>
    ALL_SECTIONS contains { key: 'content', label: 'Content', href: '/content' }. ROLE_SECTIONS.super_admin and ROLE_SECTIONS.admin both include 'content'. ROLE_SECTIONS.receptionist and ROLE_SECTIONS.doctor do NOT include 'content'.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create app/(portal)/content/layout.tsx — role-guard for all content routes</name>
  <files>app/(portal)/content/layout.tsx</files>
  <read_first>
    - app/(portal)/layout.tsx — copy the role-guard pattern: getUser(), fetch from profiles table, redirect logic
  </read_first>
  <action>
    Create app/(portal)/content/layout.tsx as a Next.js Server Component (no 'use client' directive).

    The layout must enforce role-based access for all /portal/content/* routes. Pattern mirrors app/(portal)/layout.tsx:

    1. Call createClient() from @/lib/supabase/server to get the Supabase server client.
    2. Call supabase.auth.getUser(). If user is null or there is an error, redirect('/login').
    3. Query the profiles table: supabase.from('profiles').select('role, is_active').eq('user_id', user.id).single(). If the query errors or returns no row, redirect('/login').
    4. If profile.is_active is false, redirect('/login').
    5. If profile.role is NOT 'super_admin' and NOT 'admin', redirect('/portal/dashboard').
    6. Render children: return <>{children}</>.

    The layout wraps all sub-routes under app/(portal)/content/ — any direct URL access to /portal/content/departments, /portal/content/doctors, etc. by a Receptionist or Doctor will be caught and redirected before the page renders.

    Do NOT add any visible UI (no nav, no wrapper divs) — this is a security-only layout. The portal root layout already provides the sidebar and chrome.
  </action>
  <verify>
    <automated>grep -c "getUser" "D:/Git Hub/Aatmaram Website/app/(portal)/content/layout.tsx"</automated>
    <automated>grep -c "redirect" "D:/Git Hub/Aatmaram Website/app/(portal)/content/layout.tsx"</automated>
    <automated>grep -c "super_admin" "D:/Git Hub/Aatmaram Website/app/(portal)/content/layout.tsx"</automated>
  </verify>
  <done>
    app/(portal)/content/layout.tsx exists. It calls getUser(), fetches profile role, redirects to /login if unauthenticated or inactive, redirects to /portal/dashboard if role is not super_admin or admin. npx tsc --noEmit passes.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: [BLOCKING] Apply migration via Supabase Dashboard SQL Editor</name>
  <what-built>
    Task 1 created supabase/migrations/20260612_cms_tables.sql with four CMS tables, RLS policies, and the hospital_info seed row. This migration must be applied to the live Supabase project before any subsequent plan can run.
  </what-built>
  <how-to-verify>
    1. Open the Supabase dashboard: https://supabase.com/dashboard
    2. Select the Atmaram project
    3. Navigate to: SQL Editor (left sidebar)
    4. Click "New query"
    5. Open the file D:/Git Hub/Aatmaram Website/supabase/migrations/20260612_cms_tables.sql in VS Code or Notepad
    6. Copy the entire SQL content and paste it into the SQL Editor
    7. Click "Run" (or press Ctrl+Enter)
    8. Confirm the output shows no errors — you should see "Success. No rows returned" or similar
    9. Navigate to Table Editor and verify these four tables now exist: departments, doctors, facilities, hospital_info
    10. Click on hospital_info table and confirm it has exactly one row
    11. Also open .env.local and add: SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>
        (Get this from: Supabase Dashboard → Project Settings → API → service_role secret key)
  </how-to-verify>
  <resume-signal>Type "migration applied" once the four tables are visible in Supabase Table Editor and SUPABASE_SERVICE_ROLE_KEY is in .env.local</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → Supabase | Service role key is server-only; must never be exposed to browser |
| .env.local → source control | SUPABASE_SERVICE_ROLE_KEY must remain in .gitignore |
| Browser → /portal/content/* | Any role with a valid session can attempt direct URL navigation; layout.tsx is the server-side gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | SUPABASE_SERVICE_ROLE_KEY in env | mitigate | Key documented in .env.example with placeholder value only; .env.local is gitignored — verify with `grep .env.local .gitignore` |
| T-05-02 | Tampering | RLS disabled on new tables | mitigate | Migration SQL includes ENABLE ROW LEVEL SECURITY and SELECT policies on all four tables |
| T-05-03 | Elevation of Privilege | Receptionist/Doctor accessing /portal/content/* by direct URL | mitigate | app/(portal)/content/layout.tsx enforces super_admin/admin role check server-side before any page renders |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages in this plan — shadcn components added via `npx shadcn add` in later plans |
</threat_model>

<verification>
After human checkpoint confirms migration applied:
- `SELECT count(*) FROM hospital_info` returns 1
- `SELECT count(*) FROM departments` returns 0 (empty, ready for portal seeding)
- lib/supabase/admin.ts compiles: `npx tsc --noEmit` passes
- lib/portal/roles.ts: 'content' key present in ALL_SECTIONS + super_admin + admin ROLE_SECTIONS
- app/(portal)/content/layout.tsx: getUser() + role check + redirect to /portal/dashboard for non-admin roles
</verification>

<success_criteria>
Four tables exist in Supabase with RLS SELECT policies. hospital_info has one row. admin.ts exports createAdminClient() using service role key. roles.ts exposes 'content' to super_admin and admin. SUPABASE_SERVICE_ROLE_KEY is documented in .env.example and present in .env.local. app/(portal)/content/layout.tsx blocks Receptionist and Doctor roles from all /portal/content/* routes at the server level.
</success_criteria>

<output>
Create .planning/phases/05-content-management/05-01-SUMMARY.md when done
</output>
